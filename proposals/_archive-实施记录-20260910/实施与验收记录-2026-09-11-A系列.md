# §18 A 系列实施批（A0–A10 + C1）· 2026-09-10~11

> 批次性质：用户授权的自主顺序执行批（"按序号顺序执行所有任务，每批次完成后自检，发现 bug 及时修正，无 bug 后自动继续"）。方案权威=`proposals/RW-Agent平台化改造总方案-v2.34.md`。本文件=实施与验收记录（权威），总方案只留附录 D 索引行。

## 0. 总览

| 项 | 值 |
|---|---|
| 执行方式 | 逐批实现 → 本机语法/单测 → 提交 → bundle → 服务器 `scripts/guard-deploy.sh` → `systemctl restart rw-test` → `selfcheck.mjs` → 目标 API 冒烟 → （发现 bug 即修）→ 下一批 |
| 部署纪律 | 每批 `commit → git bundle → scp → guard-deploy（干净树/无未推送/仅 fast-forward）→ push origin`；期间处理了 2 次分叉（服务器端每日自我进化任务自动提交 docs、模板导入接口自动 git 提交，均以 `git rebase origin/main` 收敛） |
| 验收合计 | selfcheck **12/12**（每批）；批次冒烟 8/8·5/5·21/21·9/9·6/6·10/10·5/5·11/11·8/8；终检全面自测 **57/57** |
| 末次部署 | 见文末「提交链」 |

## 1. A0 扩展中心数据载体

- **内容**：`extensions`（插件/MCP/应用统一资产注册表）、`shell_extensions`（壳×资产装载）、`extension_demands`（需求闭环）、`credentials_ref`（凭证引用占位）四表 + 资产注册/状态/详情、壳装载读写、需求提交与审（`ext:register`/`ext:status`/`ext:load`/`ext:demand`/`ext:demand_status` 审计）。
- **验收**：冒烟 8/8；修 1 个真实 bug——`PATCH /api/extensions/demands/:id/status` 被前面的 `PATCH /api/extensions/:type/:key/status` 先行匹配（express 首次匹配）导致 400，已将 demands 两组路由上移。
- **提交**：`0dde938`（含上述路由修复）。

## 2. A1 观测设置键与缓存命中链路

- **内容**：`settingsSchema` 删除死参数 `selfchange_budget_yuan`、新增观测组键 `cache_hit_rate_target`（0=不启用；不入 runtime 组=PUT 不 bump policy_rev）；`v_model_telemetry_daily` 视图补 `cache_hit`/`cache_miss` 聚合列；新增 `GET /api/cache-hit/summary`（当日值/近 7 日均值/目标/alert/按日序列）；Dashboard 首页状态带横条（告警/常规双态）；SettingsPanel 增「观测（不 bump 政策版本）」组。
- **验收**：selfcheck 12/12；设置写读与 bump 口径复验 5/5（观测键写入 0/15 均不 bump；对照 runtime 键 `round_cap` bump、普通参数 `temperature` 不 bump）；DB 清理 `selfchange_budget_yuan` 行。
- **提交**：`fcdd193b`。

## 3. A2 Agent 页与装配向导（含模板导出导入克隆/挂壳/按壳工具裁剪/canary）

- **内容**：
  - 模板库 export/import/clone（`server/templates.js` 文件操作 + 三个端点 + 写盘后自动 `git add/commit/push origin`，审计 template:export/import/update/clone）；UI 导出/克隆/导入。
  - 会话挂壳：`GET /api/conversations` 带 `shell_key/shell_name`；`PATCH /api/conversations/:id` 支持 `shell`（''/'default'=摘下）并审计 `conv:shell`；对话页顶栏「壳：」选择器 + 会话列表壳标签。
  - **按壳工具 schema 裁剪（§9.3② 闭合）**：`toolDefs(expose, enabled, shell)` 实现 `会话preset ∩ 壳presetBase ∪ forceOn − forceOff`（平台豁免工具只绕过启用集、仍受档位约束）、按壳 MCP 白名单（schema 层裁剪 + exec 层拦截 `mcp_*`）；`/api/chat` 组装 `__shellSchema`（读 shells.tools_preset、shell_tools、shell_extensions mcp 装载）。
  - **Agent（壳）页**：`agent-dev` 板块升级为壳列表/详情/新建 + **9 步装配向导**（step0 壳模板/step1 身份/step2 人格领域/step3 工具三态/step4 模型策略/step5 技能/step6 知识与扩展/step7 护栏/step8 验收：pack 预览→建壳→装载扩展→金标 canary）+ 页内任务模板库子区；`server/shelltemplates.js` 内置 6 个壳模板（`GET /api/shell-templates`）；旧 `ShellDev.jsx` 删除、AppsBoard 去重复嵌入。
  - **goldenSetRef canary（行为级变更即跑）**：`server/canary.js` + `eval/code.json` 起始金标集（9 项：意图词表分类 + 工具 schema 暴露断言，确定性零 LLM）；壳导入/克隆后自动跑（`maybeAutoCanary`），`POST /api/shells/:key/canary` 手动跑，审计 `canary:run`；code 壳 pack `eval.goldenSetRef="code"`。
- **验收**：本机单测 **21/21**（模板 import/clone/冲突、金标解析 9 项、工具暴露 5 例、意图 4 例、清理后工作树干净）；服务器冒烟 **9/9**（模板导出、技能列表、code pack 导入、canary passed=9/9、会话挂/摘壳、审计落痕）；终检 6/6；修复 2 个 UI bug（建壳后结果被立即关闭隐藏；shell-templates 裸 fetch 缺鉴权）。
- **提交**：`565739d`（服务端）→ `8ce4a45`（金标起始集）→ `5872894`（UI）→ `d15ab49`/`1e5090c`（UI 修复）。

## 4. A3 扩展中心

- **内容**：扩展中心页（`plugins` code 升级）：资产卡片墙（类型 Tab/搜索/状态角标/装载壳数/待审需求）、注册资产、生命周期按钮（研发→测试→上架→退役）、详情（能力声明/装载壳勾选/需求流/采纳·驳回·升级）、需求单一 intake（触发场景/期望效果/涉及壳/代码动作类型四字段齐判据）、指标 v1 两层（MCP 按 `mcp_<id>_` 前缀聚合 tool_calls：调用/失败/失败率/均耗时/活跃壳会话；插件/应用注明无调用维度）、MCP 资产化（`POST /api/extensions/mcp-sync`，登记 settings mcp_servers + 工具快照 + `untrustedInput` 提示注入标记）、发布闸门（转 published 需 capability 或 manifest_ref）、提示注入触发注。
- **验收**：冒烟 **9/9**；DB 清理测试数据（保留真实 `mcp/github/published` 资产）。
- **提交**：`3f02a23`（rebase 后 `bef95c0`）。

## 5. A4 工具集 UI v2 + 使用率看板

- **内容**：工具集 v2（Tab：基础/专业/权限高危+计数、Tab 内搜索与全选/清空、开关防抖保存 ≤300ms、失败回滚并标红、平台豁免恒开锁、去除限高内滚）；访问规则（allow/deny）移至列表上方（默认折叠）；`GET /api/toolusage`（近 7/30 天调用/失败/失败率/均耗时）+ 使用率看板；注释残留修正（meta.js 档位计数 core24/pro33/expert7、index.js「默认 28」）。
- **验收**：冒烟 **5/5**（列表/分级字段/PUT+还原/看板/非法入参 400）。
- **提交**：`24a50c4`。

## 6. A5 技能库与三层校验 + intake 硬闸门

- **内容**：`server/skillsmgr.js`（列表/详情/保存/启停/删除 + 三层校验：①静态 frontmatter·正文·占位符 ②冲突（与既有技能 description 语义重叠告警）③运行回环（真实会话载入冒烟））；`GET/POST/PATCH/DELETE /api/skills*` 与 `POST /api/skills/:name/smoke`；`enabled:false` 软停（skills_list/skill_load 跳过；删除须先停用）；技能库页（平台组，code `skills`）；**3 个 intake 技能** SKILL.md（plugin-dev-intake/app-dev-intake/shell-intake，纳入 git 追踪并同步服务器运行时技能目录）；**`intake_submit` 工具 + `intake_skill_guard` 硬闸门**（动作层：未载入对应 intake 技能拒绝提交，引导先采集四字段）。
- **验收**：冒烟 **8/8**（含硬闸拦截实测）；测试技能已清理。
- **提交**：`d49c8a8` → `e2a3bce`（intake 技能纳入 git）。

## 7. A6 知识条目状态与月度巡检

- **内容**：`knowledge.status`（active/superseded/obsolete）+ `related_component` 迁移；会话注入（F19）与 `kb_search`/`kb_del` 统一走 `kbVisibleWhere`（§9.3④ 生产化，含 status=active 过滤）；`PATCH /api/knowledge/:id`（审计 knowledge:status）+ 列表 status 过滤 + KB 页状态治理 UI；月度巡检任务幂等预置（「知识库月度巡检」cron `30 5 1 * *`，只读巡检+修订建议）。
- **验收**：冒烟 **6/6**。
- **提交**：`1d908e5`。

## 8. A7 进化集页 v2 + 难度/复测

- **内容**：新载体 `evo_goals`/`evo_goal_tasks`/`evo_memos`/`task_history`（+`reviews.difficulty` 迁移）；进化集页 v2（状态行：每日进化/启用目标/待审建议/失败告警；运行载体卡含 ▶跑一次与"执行目标"标签；进化目标卡区 + 绑定任务勾选；审批台卡区（采纳/升级/驳回/仅建议→备忘录）；备忘录区；**护栏三条**显式呈现）；模型观测页：复测登记（难度小/中/大、打回必填原因）+ 一次通过率（模型×难度）+ 复测记录带难度。
- **验收**：冒烟 **10/10**。
- **提交**：`bc9366d`（rebase 后 `2497491`）。

## 9. A8 任务独立页 + 设置收窄

- **内容**：任务页（独立板块 `tasks`：列表卡/新建/▶跑一次/启停/编辑/删除/执行历史展开/绑进化目标双向/失败告警横幅）；服务端 `POST /api/tasks/:id/run`（手动补跑不推进 next_run）、`GET /api/tasks/:id/history`、`GET /api/tasks/alerts`（近 24h 失败）；scheduler 写 `task_history` 并把绑定目标逐条拼进任务指令；设置页收窄（任务→任务页、MCP→扩展中心，`McpManager` 独立组件）；console 新增「系统」组。
- **验收**：跑一次闭环 **5/5**（历史落行 ok=1、next_run 保留）；临时任务已删除。
- **提交**：`39a3d84`。

## 10. A9 审计页与 90 天归档

- **内容**：`audit_log.conversation_id` + `audit_log_archive` 表；`GET /api/audit` 过滤器（days/category(AUDIT_CATS 七类)/shell_id/q/conversation_id/archived=0|1|all）；`GET /api/conversations/:id/trace`（audit × tool_calls 联动 + 用量合计）；`GET /api/audit/archive-stats`、`POST /api/audit/archive`、启动补跑 + 每 24h 定时归档；审计页（板块 `audit`：过滤器/详情折叠/归档切换/按会话回溯视图）；对话页顶栏「🔍 审计」跳 `/console/audit?conv=N`（Console 路径解析剥离 query）；工具/意图/路由审计写 conversation_id。
- **验收**：冒烟 **11/11**（首轮 2 项失败系用例设计问题——闲聊不产生 intent/tool 审计行，改用触发工具的消息后全过）。
- **提交**：`40f5ff7`。

## 11. A10 模型广场补强

- **内容**：`GET/PUT /api/default-models`（auto 全局默认写口；校验模型归属厂商且已启用，防伪配置；审计 model:default）、`GET /api/shells/model-overview`（跨壳默认模型一览，只读；壳级唯一写点仍=装配向导 step4）；模型广场 UI（默认模型编辑表 + 跨壳一览 + 说明）。
- **验收**：冒烟 **8/8**（含跨厂商模型被拒、未知厂商被拒、写入生效与还原、审计落痕）。
- **提交**：`240dbce`。

## 12. C1 PAT 轮换登记

- **内容**：`docs/MCP接入状态.md` 增「C1 轮换登记」节（状态=待用户执行/平台侧无法核实；轮换三步+验证动作+退役条件；关联 A3 MCP 资产化与 D10 凭证引用后置）；总方案 §11.4 运维待办同步。
- **性质**：平台外一次性操作，仅登记，不改代码。

## 13. 终检（全面自测）

- **脚本**：`/tmp/final-e2e.mjs`（一次性；覆盖 health/login/基础 API、**12 个 console 板块 HTTP 200**、A0–A10 全部数据面与关键回归）。
- **结果**：**57/57 PASS**（首轮 56/57，唯一失败项系 A1 清理时删除的设置行导致键不在位——按设计键缺失=用 schema 默认；补写 0 值后复验 5/5 通过）；selfcheck 12/12；服务器日志无 error/unhandled。
- **同步**：local = server = origin（`guard-deploy` push origin 后本地 `rebase`/`push` 收敛）。

## 14. 全面自审（第二轮：数据有效性 / 前后端 / 联调）

> 触发：用户指令"全面自测自审一遍，查证数据有效性，前端、后端，联调；场景/业务/逻辑/数据/交互是否完整、有效、一致，无冗余、无冲突、无缺陷"。
> 方法：① 静态契约核对（组件→api.js 方法、api.js 路径→server 路由、路由重复/遮蔽扫描）② 数据有效性 SQL 抽查（新载体是否真有数据、是否存在恒空/孤儿列）③ 联调实测（Chrome headless + CDP 真实渲染 12 板块/首页/对话页、壳装载读写、按壳 MCP 裁剪拦截、知识状态检索、复测难度回填、按会话回溯）④ 单元级直调（execTool 壳级拦截语义）。

### 14.1 查出并修复的缺陷（7 项）

| # | 类别 | 问题（证据） | 修复 |
|---|---|---|---|
| 1 | 缺陷/联调 | **扩展中心"壳装载"保存错误**：把**资产 key** 当**壳 key** 调 `PUT /api/shells/:key/extensions`（该端点语义=该壳扩展整表替换），装载关系写不进去/写错壳 | 改为逐壳"读现有清单→按勾选增删本资产→整表回写"（`saveShellLoads`），加 busy 态 |
| 2 | 冲突/一致性 | **按壳 MCP 未装载仍可用**：实测未装载壳仍成功调用 `mcp_github_list_commits`（因实现为"未装载=沿用全局"过渡回退），与 §9.2"不复用现状全局注册"冲突 | 非 default 壳**严格按壳裁剪**：`shell_extensions` 无 mcp 行→白名单空数组→schema 不暴露且 exec 拦截；无壳/default 会话维持全局 |
| 3 | 缺陷/稳定性 | **壳级拦截 `throw` 逸出 execTool**（force_off 与新增的按壳 MCP 白名单检查都在 try 之外）→ 整轮判"执行失败"中断（模型无法改用它法继续；该轮观测不落表） | 两处拦截改为**返回 `{error}`**（拦截语义不变，绝不执行）；保留"未知工具"throw（协议错误）。单元直调 4/4 验证 |
| 4 | 数据有效性 | **观测缺行**：`model_telemetry` 仅成功分支落表 → 出错/被拦轮次"账本有消耗、观测无行"（execs 与成本口径不一致；实测 conv 490/493 有 usage 无 telemetry） | 落表抽为闭包 `recordTelemetry()` 并接入**异常路径**；复测难度同时回填 `model_telemetry.difficulty`（§9.1 归集维度接线） |
| 5 | 完整性 | **首页状态带未接任务失败告警**（§8.10 明确"任务失败→首页状态带"）；命中率在目标=0 时完全不显示数值 | 首页状态带增加任务失败告警区；命中率无目标时也显示当日/近 7 日均值（标注"未启用目标"） |
| 6 | 完整性 | 进化集"审批台生成记录"缺任务失败记录块 | 审批台区增加"任务失败记录（近 24h，同源）"+一键转备忘录 |
| 7 | 交互/冗余 | ①对话页挂壳后本地态 `shell_id` 仍沿用旧值（与服务端不一致）②技能库"既有技能"目录名输入可改（改名会新建目录，且残留一处恒 false 的 disabled 表达式）③`api.evoGoalsByTask` 定义未用 | ①取所选壳真实 id 同步三字段 ②既有技能禁改名并说明 ③移除未用 wrapper（服务端反向查询端点保留） |

### 14.2 核查通过项（无缺陷）

- **契约一致**：组件调用 105 个 api 方法**全部有定义**；api.js 68 条路径**全部命中** server 路由；116 条路由**无重复注册**、**无参数路由遮蔽静态路由**。
- **真实渲染**：Chrome headless 注入登录态后逐页加载 —— **12/12 板块**主区渲染且**零未捕获异常/零 console error**；`/chat`（侧栏+输入栏）与 `/`（仪表盘+状态带）通过（首轮 1 次首页超时为空渲染，经 5 次重复导航取证确认为测量等待不足的假失败，非应用缺陷；审计脚本等待已提到 3.5s 后稳定通过）。
- **数据有效性 SQL 抽查**：11 项载体计数全部合理（extensions=1 真实 mcp 资产；shell_extensions/需求/目标/备忘/复测=0 无残留；task_history=1；scheduled_tasks=3 含月度巡检；视图 cache 聚合列在位；settings 观测键在位）。
- **联调实测**：壳装载读写与还原（装载→详情反映→卸载还原无残留）；按壳 MCP 装载后可调用、未装载不暴露（8/8）；知识 active 命中 / superseded 不再返回（6/6，运行时经真实 kb_search 调用验证 kbVisibleWhere 生产口径）；复测难度 → telemetry 回填（SQL 实证 usage 2 行 → telemetry 1 行 difficulty=小）；按会话回溯工具轨迹+审计动作；canary 等前序批次回归 57/57 全绿。
- **清理**：本轮测试会话 5 个、测试知识 3 条、A8 临时任务会话 1 个全部删除；`__%` 前缀历史测试会话（B1-E2E/ST-*/__batch1_verify__ 等 09-08~09 旧批次产物）保留（属历史数据，不属本轮，未擅自删除，登记备查）。

### 14.3 遗留（诚实登记）

1. 历史 `audit_log` 行（约 1.04 万条）无 `conversation_id`（该列 A9 才加）——按会话回溯对旧会话依赖 `detail LIKE '%conv=N%'` 兜底，覆盖有限。
2. `model_telemetry.difficulty` 仍是"复测时回填"，未复测的会话行为 NULL（口径=未标注难度）。
3. 任务页与进化集页各有一处"▶跑一次"、扩展中心与进化集各有一处需求采纳——**同源同一 API 的两视图**（有意为之，避免单点不可达），非重复实现。
4. 无头浏览器仅在开发机可用（服务器无 Chrome），UI 渲染自检依赖本地 Chrome + 远程 880，未纳入服务器 CI；建议后续把该脚本纳入回归附件。

### 14.4 本轮提交

```
0eae6e0 拦截语义 + 观测落表修复（含严格按壳 MCP 裁剪 797700e 前的 7ca9515/b837dbd）
b837dbd 壳装载保存 / 挂壳本地态 / 技能改名 / 难度回填 / 移除未用 wrapper
7ca9515 首页状态带接任务失败告警 + 命中率无目标也显示
797700e 按壳 MCP 严格裁剪（文档 §9.2/§10/§18 同步）
```

## 15. 提交链（本批）


```
0dde938 A0 → fcdd193b A1 → 565739d/8ce4a45/5872894/d15ab49/1e5090c A2
→ 3f02a23(bef95c0) A3 → 24a50c4 A4 → d49c8a8/e2a3bce A5 → 1d908e5 A6
→ bc9366d(2497491) A7 → 39a3d84 A8 → 40f5ff7 A9 → 240dbce A10 → v2.33 文档对账（本提交）
```

## 16. 遗留与后置（诚实登记，未隐藏）

1. **金标集内容仅起始版**：`eval/code.json` 9 项（意图 4 + 工具 5）；其余壳未配 goldenSetRef（canary 会如实标注"未配置/文件缺失→跳过"）。
2. **技能库"候选技能（待审批）"区未单建载体**：现以提案/需求待审 + 人工新建承接。
3. **插件/应用调用留痕维度缺失**：tool_calls 无 asset 维度 → 指标 v1 前两层只对 MCP 有数据。
4. **按壳 MCP 白名单口径**：**严格按壳**（非 default 壳未装载任何 MCP 资产 → 该壳不暴露任何 MCP 工具，exec 层同口径拦截；无壳/default 会话维持全局）。初版曾以"未装载=沿用全局"过渡规避存量回归，2026-09-11 全面自审联调发现其与方案 §9.2"不复用现状全局注册"冲突（实测未装载壳仍可调用 `mcp_github_*` 成功），已修正为严格裁剪并复测通过。
5. **凭证引用化（credentials_ref）**：仍随 D10 连接器试点；之前 MCP token 明文仍在 settings（C1 待轮换）。
6. **A7 护栏三条**：页面呈现 + 由人工审批保证；"提案数上限"的超限候选转备忘录区排队（未在调度器强砍产出）。
7. **提示注入防线**：仅登记触发条件（外部 MCP/网页=不可信输入），工程化后置。
8. **难度/一次通过率**：v1 由人工勾选；自动估算挂 v2。

## 17. 导航定版落地纠正批（v2.34，2026-09-11）

> 触发：用户指出"各模块未按约定方案执行（导航栏、历史遗留、位置与分组错误），每个模块都还带着老的代码/老的功能没有修订；等于在现有页面上保留了两套代码（一套老的、一套新开发的）——错误没有得到修正，只是新增了一套并存"。
> 核证结论：**用户判断成立**。蓝图 §8.2 早已定版"4 组 11 板块"，但实现（`src/console/Console.jsx`）为"4 组 12 板块 + 旧 code 直接作导航项"：① 多出「应用」独立板块（`AppsBoard`，与 §8.2"应用市场不单列导航"、§8.8"应用=扩展中心资产类型"冲突——同一功能两套入口/两套代码）；② 模型广场/模型观测错置"平台"组；③ 进化集单列"Agent"组；④ 导航用历史 code（`agent-caps`/`agent-evo`/`agent-dev`/`agent-apps`/`plugins`）与旧板块名呈现。此外发现两处同类问题：**壳详情内联改默认模型**（与 §8.9 定案 B"step4=壳级 modelPolicy 唯一写点"冲突=第二写面）；**服务端 markdown 会话导出**（与对话页本地导出重复=同一功能两套实现）。另核 v2.33 文档 §8.2"落地现状"曾把上述差异描述为"仅为分组归属微调"——属失实表述，已更正。

### 17.1 改动（对齐定版 + 合并而非新增）

| # | 类别 | 处置 |
|---|---|---|
| 1 | 导航定版 | `Console.jsx` 重写：4 组 11 板块（模型：模型广场/模型观测；平台：工具集/技能库/知识库/进化集；应用：Agent/扩展中心；系统：任务/审计/设置），顺序与命名=定版表；code 改定版短码 `models-plaza`/`models-obs`/`toolset`/`skills`/`kb`/`evo`/`agent`/`ext`/`tasks`/`audit`/`settings` |
| 2 | URL 兼容 | 旧 code 降为别名表 `ALIAS`（`agent-caps`→`toolset`、`agent-evo`→`evo`、`agent-dev`→`agent`、`plugins`→`ext`、`agent-apps`→`ext`），进入即 `history.replaceState` 归一新链；query 保留（`/console/audit?conv=N` 验证通过） |
| 3 | 去重复板块 | 删除 `AppsBoard.jsx`；应用启动能力收拢为 `src/shared/AppLaunch.jsx`（单一实现），挂载两处：应用·扩展中心（资产页子区）+ 应用·Agent 壳详情（§8.2"壳内可用应用入口放装配向导 + 壳详情"） |
| 4 | 去第二写面 | `AgentBoard` 壳详情改**只读**（描述/persona/默认模型/工具三态）+ 「装配向导（编辑）」入口；删除 `mpSel`/`saveMp`/`provs` 等承载第二写面的代码 |
| 5 | 去重复实现 | `server/index.js` 的 `GET /api/conversations/:id/export` 删除 markdown 生成分支（人工可读导出唯一实现=对话页「⬇ 导出」），保留 P26 jsonl 机器可读导出 |
| 6 | 旧代码残留 | `CapsBoard.jsx` → `ToolsetBoard.jsx`（导出名同步 `ToolsetBoard`）；删除闲置 wrapper `api.models()`；清理旧导航路径文案（`SettingsBoard`/`ModelPlaza`×2/`docs/MCP接入状态.md` 的"应用市场 → …"）与 `ProposalsManager`/`AgentBoard` 旧编号注释 |
| 7 | 文档更正 | v2.33→v2.34：§8.2 落地现状改如实描述 + 纠正留痕；新增本批记录与索引行；`docs/README.md` "现 v2.34 / console 4 组 11 板块"；全仓 `-v2.33.md` 引用同步为 `-v2.34.md`（含归档与底稿头注） |

### 17.2 验证（全部在 880 部署态实测）

- **服务端**：`selfcheck.mjs` **12/12**；`/tmp/final-e2e.mjs` **57/57**；`systemctl is-active rw-test` = active。
- **定版导航渲染审计**（Chrome headless + CDP，脚本 `/tmp/ui-nav-audit.mjs`）：11 板块逐个真实渲染 + 导航项数=11 + 选中态/标题=组/板块 + 分组结构=定版（2/4/2/3）+ 5 条旧链归一 + query 保留 + 两处应用入口 → **21/21 PASS，零未捕获异常/零 console error**。
- **应用入口联调**（脚本 `/tmp/ui-app-entry-e2e.mjs`）：扩展中心应用卡→详情→🚀 启动 →跳 `/chat?conv=511` 且草稿预填 →API 核对会话真建（title=商业模式评估）→ 测试会话已删；导出端点返回 `.jsonl`；Agent 壳详情只读（无"保存默认模型"）→ **7/7 PASS**。
- **契约核对**（stub fetch 实测 api.js 全包装）：106 包装 → 105 HTTP 请求**全部命中 server 路由**；116 条路由**无重复/无遮蔽**；组件内无绕过 api.js 的直连 fetch。前端可达性：29 文件**全部可达**，106 个 wrapper **无未调用项**（`api.models` 删除后）。
- **前端未调用的 server 路由盘点**（诚实登记，11 条）：`POST /api/chat`（经 `streamChat()` SSE 导出函数调用）、`GET /api/download/:name`（对话内文件卡片链接）为在用；`GET /api/health`（运维/自检）、`GET /api/models`（selfcheck 用，客户端 wrapper 已删）、`GET /api/evo/goals/by-task/:taskId`（A7 保留的反向查询）、`GET /api/conversations/:id/export`（P26 jsonl 机器可读，无 UI 调用方=有意 API-only）为能力型；`/api/contracts` 全家 5 条=D10 连接器契约 driver（§11.4 后置专项，服务端就绪待 UI）。

### 17.3 提交

```
38e97df console: 落地 §8.2 定版导航（4组11板块/旧code降为兼容别名）+ 取消「应用」独立板块（AppLaunch 单一实现并入扩展中心与壳详情）
0b7cad6 冲突清整：壳详情改只读（壳字段唯一写点=装配向导，去掉 modelPolicy 第二写面）+ 服务端 markdown 导出分支删除（人工可读导出唯一=对话页，保留 P26 jsonl）
（本记录 + v2.34 文档对账：本提交）
```

### 17.4 遗留（诚实登记）

1. 契约脚本按"api.js 包装"统计，故 `POST /api/chat`（SSE，经 `streamChat()` 导出函数）与 `GET /api/download/:name`（对话内文件卡片下载链接）被列为"未调用"——**非冗余**（实现方式不同，不走 request 包装），已在 17.2 注明。
2. 本批未触及的既有后置项沿用 §16（技能库候选区载体、插件/应用调用留痕、D10 连接器契约 UI、凭证引用化等）。
3. 无头浏览器渲染审计仍仅开发机可跑（服务器无 Chrome），未纳入服务器 CI。
