# M1 验收矩阵（2026-09-15，M1 引擎改造批收口）

> **用途**：把《RW-Agent 架构 v1.1》§14 的 `RA-01`–`RA-42` 逐条对照**当前实现与实测证据**给出状态，回答"M1（G1–G5 全达成）到底达没达成"。
> **判定口径**：只认证据（命令 + 实际输出 / 可复跑的探针 / 归档 §20 记录）。**没有证据的一律记 ❌ 或 🟡**，不按"应该可以"记 ✅。
> **一句话结论**：**M1 未达成**。8 步最短路径已走完、引擎机制到位，收口批又关掉了 `RA-09`/`RA-12`/`RA-14`/`RA-26` 并给 `RA-37`/`RA-05b`/`RA-35` 补齐了契约、遥测与口径；但 **G1/G3/G4/G5 各仍有一项以上硬缺口**（下表红行），且 `RA-35` 的缺口是"没有真实流量可判"而非"数字没到"。

## 一、目标级判定（G1–G5）

| 目标 | 对应验收 | 判定 | 依据 / 缺口 |
|---|---|---|---|
| **G1 可独立运行** | RA-01 ✅ · RA-02 ✅ · RA-34 ❌ | 🟡 **两项达成，一项属 M2** | RA-01 两份配置跑同一份代码（归档 §20 步2）；**RA-02 本轮实测**：不引入任何 HTTP 服务直调 `runAgent` → 1 秒返回"可以"+ 用量（`/tmp/rw-ra02-headless.mjs`）；RA-34 客户形态（无外网 + Windows Server）**未做**，属 M2 硬门禁（`OP-07`） |
| **G2 可装配** | RA-03 ✅ | ✅ **达成** | **本轮实测**：改 `tools/manifest.js` 一行 → `/api/toolset` 由 65 → 64（`repo_map` 消失），还原 → 65；**全程未重启服务**（日志 `[registry] 热重载完成：工具面 65 → 64 个（无需重启）`）。加删工具既不改核心代码、也不整体重启 |
| **G3 成本结构可控** | RA-04 ✅ · RA-05 ✅ · RA-05b ❌ · RA-06 ✅ · RA-07 ✅ · RA-08 ✅ · RA-35 ❌ | ❌ **未达成** | 机制全到位（只追加/前缀冻结/工具面冻结/溢出/折叠/**比例制阈值**/账本）；**`RA-05b` 已标定且结论明确**（阈值被 `cap=4000 字符` 几何蕴含、当前不生效，见 §三 ③）；**`RA-35` 口径与判据工具已做齐**（分档＋分段＋结构上界，§三 ①），但**新段真实流量不足 ⇒ 不可判**——这是唯一还挡着 G3 的东西 |
| **G4 可演进** | RA-36 🟡 | 🟡 **部分达成** | **升级演练本轮完成**（10 张表指纹升级前后逐项相同 · 老会话可读 · 服务与自检全绿）；**旧账过账清单已成表**（第四节），但多为"待办"、**未闭环** |
| **G5 可交互** | RA-26 ✅ · RA-27 🟡 · RA-28 ✅ · RA-37 🟡 | 🟡 **收口批推进后仍未达成** | RA-28 预算耗尽不自动续 ✅（`budget-total` 守卫）；**RA-26 本轮四面实测通过**（`scripts/ra26-waitsides.mjs`）；**RA-37 契约成文 + 真 SSE 重建实测三场景通过**（`docs/事件契约.md` / `scripts/ra37-rebuild.mjs`）；**仍差**：断线重连只到"60s 内事件环"（SSE `Last-Event-ID` 续订未实现 → M2）、`RA-27` 的"干净沙箱 + 重放摘要"无沙箱不可测 |
## 二、逐条状态（RA-01 → RA-42）

| RA | 要求（摘要） | 状态 | 证据 / 缺口 |
|---|---|---|---|
| RA-01 | 换客户环境只改配置不改代码 | ✅ | `server/env.js` 单一来源；两份配置跑同一份代码（步2） |
| RA-02 | 编排核心脱离 HTTP 单跑 | ✅ | 本轮 headless 实测（无 HTTP 服务，1s 返回） |
| RA-03 | 加删工具不改核心代码、**不整体重启** | ✅ | 本轮热重载实测（65↔64，未重启） |
| RA-04 | 前缀五纪律生效；C4＝0 | ✅ | 机检 `prefix:invalidate` 全表 **0 行**；五纪律 §5.3 全标落地 |
| RA-05 | 10 万字节结果 → 预览+定位符+省略量，可取回 | ✅ | 步6：10491 字节结果溢出落盘，模型按定位符 `fetch_spill` 取回成功 |
| RA-05b | spill 阈值（32768）标定 | ❌ | **列已上线**：`tool_calls.result_bytes`（字节，写入点在 2000 字符截断**之前**）＋回填＋标定报告；**标定结论＝该阈值在当前几何下永不生效**（见 §三 ③）。触发面为 0，是否下调需真实流量复核 |
| RA-06 | 存盘失败工具仍成功 | ✅ | 步6：ENOTDIR 时 `db_query` 仍 `status=done` + 如实降级提示 |
| RA-07 | 超限结果 → 预览+省略量+定位符，无额外 LLM 调用 | ✅ | 步6 三路径取证（stored/reader/degraded） |
| RA-08 | 换小窗口模型折叠阈值自动变小（比例制） | ✅ | **本轮落地**：`server/modelwindow.js`＋设置 `collapse_window_ratio`（默认 15%）→ 有效阈值＝`min(绝对阈值, 窗口×占比×1.5字符/token)`。部署态取证：`deepseek-v4-flash`（131072 窗口）→ **29491 字符（来源=ratio）**；`deepseek-v3`（65536）→ 14745；**未知模型 → 回退绝对 30000 并说明"窗口未知"**（不静默假装）。单测 5 条（含单调性与覆盖表优先级） |
| RA-09 | 不用知识的轮次上下文里无知识 | ✅ | **已改按需**：判定/成型抽到 `server/kbgate.js`（纯函数），三档 explicit（本轮问知识→标题+前5条摘要）/ index（本会话用过 kb_*→**只给标题**）/ none（**一个字节都不注入**）。`test/kbgate.test.mjs` **7/7**（含"12 条知识在 none 档也不得漏出标题"的负例） |
| RA-10 | 不用技能时只有技能目录 | ✅ | 技能全文仅在 `skill_load` 后注入；未载入则无 |
| RA-11 | 历史 SKU 备货量走精确查询 | 🟡 | 机制在（`db_query` 精确查询）；RW 无该业务数据，**场景项未实测** |
| RA-12 | 子代理工具清单可收窄到单个工具 | ✅ | **已落地**：`server/subtools.js`（白名单解析/叠加/执行层门禁）＋ subagent / fork / fanout 新增 `tools` 参数；**schema 层与执行层同口径**（可见即可调、不可见即拒），平台豁免底座保留。`test/subagent-scope.test.mjs` 16/16（含"schema 里出现的工具执行层都不得拒绝"的一致性夹具） |
| RA-13 | 子代理失败 → 交付物照出并标注"该块未取得" | ✅ | **端到端实测通过（真模型，两种失败形态）**：`node scripts/ra13-subagent-degrade.mjs [--mode toolfail\|apierr]`，挂在真 Express + 真 `runAgent` + 真子代理上。<br>· **toolfail**（子代理只给 `read_file`，让它读三个不存在的文件）：子代理 4 步工具全 `fail` 后**如实空手而归**（status=done）→ 父代理交付物逐项列出「读取失败: ENOENT…」、完整性 `0/3（全部缺失）`、并写明「不可用于补货决策」「未新增、未补齐任何数据」→ 判据 4/4 ✅<br>· **apierr**（子代理 model 指向不可用模型）：子代理 `status=error`（流式调用 400）→ 父代理仍照出该块并标注「未取得」→ 判据 5/5 ✅<br>· 顺带端到端验证：`RA-12` 白名单（子代理试图 `list_dir` 被**执行层**拒为 fail）、事件流完整收尾。**"诚实空手而归"与"真报错"两种形态都不伪造结论** |
| RA-14 | 子代理只烧自己额度；父级留汇总；未花完显式回收 | ✅ | **已落地**：`budgetYuan` 切分 + `effectiveSubBudget()` 与段阈值取 min（只能更严）；**轮首+轮尾双判定**（超额不再多打一次无谓 LLM 调用，可用额度 0 时一次调用都不发）；记录 `budget/spent/refund` 三项显式回收。16/16 夹具覆盖 |
| RA-15–RA-19 | 数据视窗在出口生效 / 不用提示词替代 / 子代理视窗更窄 / 等待确认时沙箱已销毁 / 凭据不在沙箱 | ❌ | **平台侧 + M2**（`OP-01` 数据出口唯一化未做；沙箱属 M2 交付） |
| RA-20–RA-24 | 外部引擎原生工具可用 / 写不出工作区 / 连不上外网 / 危险命令被拦 / 缺能力拒绝启动或显式降级 | ❌ | **M2**（`OP-07` 外部前提实测未做；本批未接外部引擎） |
| RA-25 | 沙箱内看不到原始数据 | ❌ | **M2**（同 RA-15） |
| RA-26 | 等待确认与执行中在接口/监控/计费/超时上是两个状态 | ✅ | **四面已逐面对齐并实测**（`scripts/ra26-waitsides.mjs`，真 HTTP/真 agent 循环/真审批端点，仅厂商 API 用桩）：①接口 `wait_start`/`wait_end` 入流、REST `/api/approvals`·`/api/asks` 独立可查（两片待办都查到）；②监控 等待期间账本不动（start/end 取样行数与成本完全相同）、事件环可见；③计费 等待 0 次 LLM 调用、成本不涨；④超时 等待时长入账 8097ms 并从执行用时扣除（**修：原先用墙钟当执行用时，用户思考会吃时间预算**）＋ `budgetElapsedMs` 3 条夹具 |
| RA-27 | 系统故障后从检查点重建 | 🟡 | `runtrack` 重启自检把中断任务标 `interrupted` 可恢复；**"干净沙箱 + 重放摘要"未实测**（无沙箱） |
| RA-28 | 预算耗尽不能自动续，必须追加 | ✅ | `budget-total` 守卫直接返回挂起文案，不自动续跑 |
| RA-29 | 检查点不进记忆召回候选 | ✅ | 检查点是文件（`checkpoint.js`），不进 `knowledge`/`kb_search` 召回面 |
| RA-30 | 定时任务不套空会话 | ✅ | `scheduler` 在专用执行会话里驱动，不建空对话 |
| RA-31 | agent 暴露结束原因/用量/能力清单 | 🟡 | 结束原因（`guard`/`paused`/`reason`）与用量（`usage`）已回传；**"能力清单"未成文暴露**；假完成判定依赖 `OP-02` |
| RA-32 | AgentAdapter 契约成文 + 两家引擎各接一次 | ❌ | **M2/M3**（`OP-05` 契约未写） |
| RA-33 | 三家引擎成本基线 | ❌ | **M2/M3**（`OP-06`） |
| RA-34 | 客户形态机（无外网+Windows Server）装上、自检过、账本可导出 | ❌ | **M2**（本批未做 Windows Server 适配与离线安装） |
| RA-35 | C1 ≥99%；C2 中位 ≤1k、P95 ≤5k | ❌ | **口径已定稿并逐会话审计过**（`scripts/cohort.mjs`；审计结论：79 个会话中**有 usage 行的一律落进"探针"或"真实"，无漏网**）。**仅按真实会话重算**：轮 2,337（会话 8）· **C1 11.75%** · C2 中位 **43,487** / P95 108,366 · ¥242.41；再分「人发起 1,600 轮 C1 6.96% / 定时任务 737 轮 C1 24.33%」。**两个新发现决定"怎么判"**：① 集中度极端——真实流量前 3 会话占未命中 **98.34%**、#184 一个占 **62.71%**，剔除未命中最多的 2 个后**人发起 C1 从 6.96% → 56.33%** ⇒ **累计 C1 会被历史永久锁死，不能当"现在好不好"的判据**，必须**按改造生效时刻切段**（`scripts/ra35-report.mjs`，`RA35_CUTOFF` 可覆盖）；② 口径级上界是 `1 − 会话数/总轮数`（人发起 6/1600 → **99.63%**）⇒ "99% 达不到"不成立，缺口在**地板与实测之间**（`scripts/c1-ceiling.mjs`）。**当前判定**：新段（库本地 09-15 05:00 起）真实流量仅 **2 轮且无计费 token ⇒ 不可判**；新段探针 38 轮 C1 88.73%、逐轮未命中多在 500–4,000，但那是**缓存热 + 秒级间隔**的最好情况，**不得当真实成绩**。⚠️ 时间窗一律按**库本地时间**（`time_zone=SYSTEM`＝UTC+8）；历史文档写的"06:30Z"按字面只剩 1 行。首个真实数据点：定时任务 #4 `next_run=2026-09-15 21:00:51Z` |
| RA-36 | 升级不丢数据 + 旧账过账清单闭环 | 🟡 | **升级演练已完成两次**：① `bcda2fc → b9619dd`（10 张表行数逐项相同）；② **本批部署态演练**（`25cc3df → d2f2048`，含新列 DDL）：升级前先备份，升级后 `conversations 79 / messages 304 / tool_calls 3889 / usage_stats 2753 / agent_runs 79` **逐项相同**、最老/最新会话不变（老会话可读）、`result_bytes` 列在且**全部回填**、服务 active + `selfcheck 12/12` + `e2e-final 21/21`、重启后日志零 `ReferenceError`。另有 `scripts/migration-rehearsal.mjs`（临时库真实行数 3,889 行跑 DDL，39ms 无丢数据）。**旧账过账清单已于本批定稿**（第四节：**已闭环 5 · 转 M2/M3 8 · 仍开放 6**，取证脚本 `scripts/audit-ledger.mjs`）——**"清单闭环"部分达成**；仍开放 6 项已逐项写明下一步，**升降级演练这一半与过账清单这一半现在都到位了**，故本行维持 🟡 仅因那 6 项开放项未排期 |
| RA-37 | 事件契约成文；任一客户端仅靠事件流重建全过程 | 🟡 | **契约已成文**（`docs/事件契约.md`，17 类事件逐条给字段/语义/有序性/是否带 id）＋**重建器**（`src/eventstream.js`）＋**真 HTTP·真 SSE 三场景实测**（`scripts/ra37-rebuild.mjs`）：normal / 后置追加（截断提示）/ 前置加注（假完成），**重建正文与落库逐字一致**、落库 id·轮次·全量成本逐项对账通过。为此修了三处硬缺口：SSE 帧补 `id:<seq>`；`done` 带 `totals` 与 `messageId`、**改到落库之后发**（原先是"先发 done 再 INSERT"的竞态）；新增 `run_start`/`run_end`（执行边界 + 落定回执）。**仍差**：断线重连只到"60s 内事件环"这一层（环消失后只能回落 `/messages`），SSE `Last-Event-ID` 续订未实现 → 属 M2 |
| RA-38 | 系统层+工具层逐字节一致 | ✅ | 步8 机检：同输入同字节；`[prefix-debug]` 逐轮同哈希；档位不同必不同哈希（非恒真） |
| RA-39 | 同工具同参数第 3/5 次提醒但不阻止 | ✅ | **本轮落地并机检**：3/5 各提醒一次、不阻断；挂起阈值校正为"连续 N 次"（默认 6），`test/loopguard.test.mjs` 4/4 |
| RA-40 | 折叠后交付物依据仍在 | ✅ | 原文在 `messages`（只有 INSERT）；折叠摘要 + `tool_calls` 可按 id 回查 |
| RA-41 | 被别的 agent 当 MCP 工具调用成功一次 | ❌ | **M2**（计划 ⑭ MCP server 形态） |
| RA-42 | 幂等键 / 回调 HMAC 防重放 / 死信落点 | ❌ | **M2**（对外形态未开工） |

**统计（2026-09-15 收口批更新）**：✅ **20** 条 · 🟡 **8** 条 · ❌ **14** 条（其中 12 条按里程碑属 M2/M3，非 M1 范围）。较上版：`RA-09`/`RA-12`/`RA-14`/`RA-26`/`RA-13` 由 ❌/🟡 → ✅；`RA-37` ❌ → 🟡；`RA-05b`/`RA-35` 仍 ❌ 但**已从"没做"变成"结论明确"**；`RA-36` 的**过账清单已定稿**（已闭环 5 · 转 M2/M3 8 · 仍开放 6）。

## 三、M1 收口还差什么（2026-09-15 收口批：①③④⑥ 已做，② 早已完成，⑤ 部分）

1. **RA-35（G3 的判据）** —— **口径已定稿、判据已可复跑；判定缺的是"改造后的真实流量"**。
   - **口径（定稿，逐会话审计过）**，`scripts/cohort.mjs`：
     · 探针 ＝ 标题命名族 `^(__.*__|ST-|B[1-7](-|[A-Z]|$)|PROBE$)` ∪ 该会话有 `prefix:*` 账本；
     · 孤儿 ＝ 会话已删 **或** `conversation_id IS NULL`（后者 `NOT IN` 判不到——NULL 比较是 UNKNOWN，必须显式写）；
     · 真实 ＝ 其余，再分「人发起 / 定时任务」（定时任务是平台自己跑的，混进"真实使用"会让说法失真）。
     · **两条被证伪的判据**：关键词（正文出现"探针/probe"）会把占成本 59% 的真实会话 #184 误杀；`prefix:*` 账本不能单独作判据（真实定时任务 #185 每天也落豁免）。
     · **审计证据**：79 个会话里，有 usage 行的一律落进"探针"或"真实"，**没有第三个去处**。
   - **仅按真实会话重算（累计口径）**：
     ```
     node scripts/baseline-cost.mjs
       真实流量   轮 2,337 · 会话 8 · C1 11.75% · C2 中位 43,487 / P95 108,366 · ¥242.4143
         ├ 人发起  轮 1,600 · 会话 6 · C1  6.96% · C2 中位 46,033 / P95 113,566 · ¥183.7343
         └ 定时任务 轮   737 · 会话 2 · C1 24.33% · C2 中位 36,427 / P95  64,560 · ¥ 58.6800
       探针       轮   399 · 会话 47 · C1 26.03% · C2 中位 10,341 / P95  29,693 · ¥ 12.7683
     ```
   - **两个决定"怎么判"的新发现**（都落成脚本，不再靠嘴说）：
     ① **集中度** `node scripts/c1-ceiling.mjs`：真实流量前 3 会话占未命中 **98.34%**（#184 占 62.71%、#185 占 23.24%、#393 占 12.39%）；剔除未命中最多的 2 个会话后，人发起 C1 **6.96% → 56.33%**。⇒ 累计 C1 被历史永久锁死，**不能当"现在好不好"的判据**。
     ② **结构上界** `1 − 会话数/总轮数`：全库 55/2737 → 97.99%；人发起 6/1600 → **99.63%**。⇒ "99% 达不到"不成立；缺口在**地板与实测之间**（人发起 92.67 个点）。逐会话定位见 `node scripts/c1-breakdown.mjs`（#184 C1 4.86% / #393 6.68% 是全部拖累项）。
   - **分段判据**（判 RA-35 只用这一段）：`node scripts/ra35-report.mjs`（`RA35_CUTOFF` 可覆盖切段时刻，默认库本地 `2026-09-15 05:00:00`）
     ```
     ② 新段（改造生效后）
       [探针] conv=514 轮 12 · C1 86.34%   conv=516 轮 9 · C1 93.39%   conv=515 轮 8 · C1 87.27%
               conv=517 轮  7 · C1 93.01%   conv=527 轮 2 · C1 84.00%      → 探针合计 38 轮 C1 88.73%
       [真实] conv=185 轮  2 · 命中 0 / 未命中 0（未实际调用模型）
       → 新段真实流量 2 轮 ⇒ **判定：不可判**（<30 轮，任何读数都被单轮噪声主导）
     ```
     · 逐轮未命中多在 **500–4,000**（C2 达标方向），首轮 10,826 后逐轮递减——但这是**缓存热 + 秒级间隔**的最好情况，**不能当真实流量成绩**；长会话 + 长空闲才是真实压力条件。
   - **⚠️ 中途加入测试任务会换来一个"纪元"（epoch）**：RA-37 段的 `delta` 对账与 C5 归因让**新段每个会话都落 `prefix:*` 账本**，而账本正是探针判据之一 ⇒ 新段真实流量会被判成探针。据此迭代出的判据：**归因不看账本，只看"系统提示词是否逐字节冻结"**——开工时对 `sys` 做一次 `sha256` 指纹，此后每次比对，变了就是 `seg:`（工具面变更/壳装配/平台文件改动）或 `exp:`（模型切换/长空闲/折叠边界）的新纪元，没变就是同一段。这条**同时替代**旧的口径切段（"改造生效时刻"是个人为常量，且每加一次改动就要往前挪）与账本判据。实现与取证见 §三 补充项。
   - **时间表**：定时任务 #4「每日自我进化-05:00」`next_run = 2026-09-15 21:00:51Z`（库本地 09-16 05:00）会产出改造后第一个**真实**数据点；#3「KPI周报」`next = 09-21T01:00Z`。（**注意**：本项已被"关闭 #3/#4/#5、另建人造样本任务"覆盖，见下条。）
   - **一致性更正**：本矩阵原记 RA-35 窗口 C2 中位 1,543 / P95 **8,471**，用 `baseline-cost` 同一把尺复现为 1,463 / **5,186**（C1 与成本逐位一致）。原数字来源存疑，以同一把尺为准。
2. ~~**RA-08**~~：**本轮已完成**（比例制阈值绑模型窗口；未知模型回退绝对值并说明；单测 5 条 + 部署态取证）。
3. **RA-05b** —— **遥测已上线并出标定报告，结论是"该阈值当前不生效"**。
   - 改了什么：`tool_calls.result_bytes`（字节，与 spill 的 32768 判定**同一函数同一口径**，且**在 2000 字符截断之前**量——原实现只取 result 首个 key，run_command/db_query/grep_search 会量成个位数，已修）；`server/db.js` 迁移 + 启动自检；`scripts/backfill-result-bytes.mjs`（存量回填，**未截断行精确、被截断行只能给下界**，如实标注）。
   - 复跑命令与实测输出：
     ```
     node scripts/backfill-result-bytes.mjs
       扫描 3884 行：精确 3057 行 · 下界 827 行 · 空摘要跳过 0 行
       精确最大值 3802 字节；本次写入值中 >32768 的有 0 行
     node scripts/calibrate-spill.mjs
       全量 n=3,884  p50=390  p90=2,468  p95=3,063  p99=3,723  max=4,333（字节）
       字节天花板 SPILL_BYTES=32,768：超阈值 0 行（0.00%）
       几何关系：普通工具 cap=4000 字符 ⇒ 结果字节 ∈ [4,000, 12,000]（ASCII…CJK）
                 SPILL_BYTES=32,768 **高于** cap 的字节上界 → 字符 cap 必然先触发
     ```
   - **标定结论**：`SPILL_BYTES=32768` 被 `cap=4000 字符` 在几何上**完全蕴含**，它不参与任何一次判定（触发面 0）；真正生效的是字符上限。若要让字节天花板起作用应降到 ≈12288 以下。**本轮不改常量**（不引入未经真实流量验证的行为变更），把偏差与改法一并登记；`OP-10` 据此可收口为"已标定：结论=不生效"。
   - 样本外推限制：`read_file` 类重结果在 2000 字符摘要下只能给下界，故 max=4,333 是"下界的最大值"，真实上尾更厚——**要收全分布靠新流量持续采集**（列已上线）。
4. **RA-37 / RA-26 / RA-27（G5）** —— `RA-37` 与 `RA-26` 本轮做完到"有契约、有夹具、有实测"，`RA-27` 仍受"无沙箱"限制。
   - `RA-37`：契约 `docs/事件契约.md`；重建器 `src/eventstream.js`（纯函数，浏览器/Node 共用）；夹具 `test/eventstream.test.mjs`；**真 HTTP·真 SSE 实测**：
     ```
     node scripts/ra37-rebuild.mjs                # 判定：通过（重建 11 字符 / 落库 11 字符，8 项检查全绿）
     RA37_SCENARIO=appended node scripts/ra37-rebuild.mjs   # 后置追加：重建 82 / 落库 82 → 通过
     RA37_SCENARIO=prefixed node scripts/ra37-rebuild.mjs   # 前置加注：重建  7 / 落库  7 → 通过
     ```
     为此修的硬缺口：①正文对账（C4 续写段／截断提示／假完成加注此前**从不经过 `delta`**，事件流拼出来 ≠ 落库正文）；②终结事件早于落库（`done` 先发、INSERT 后做，客户端收到 done 立刻读库读不到）；③缺执行边界与持久化 id；④`done.usage` 只有最后一轮（补 `totals` 全量）。**仍差**：60s 后事件环消失、SSE 无 `Last-Event-ID` 续订 → M2。
   - `RA-26`：四面实测 `node scripts/ra26-waitsides.mjs` → **四面通过**（见 §二 RA-26 行）；顺带修了一处真问题：**时间预算原先按墙钟算**，用户思考越久越容易触发"任务超时挂起"——保险丝接到了用户身上，现按"墙钟 − 等待时长"计。
   - `RA-27`：`runtrack` 的 `interrupted` 可恢复仍在，但"干净沙箱 + 重放摘要"没有沙箱无法实测，维持 🟡 并归 M2。
5. **RA-36（G4）** —— 升级演练（不丢数据）**已做两次且含部署态实跑**；**旧账过账清单已于本批定稿**（第四节：已闭环 5 · 转 M2/M3 8 · 仍开放 6，取证脚本 `scripts/audit-ledger.mjs`）。**仍开放的 6 项**（`OP-03` 策略失败语义 · `OP-12` 复盘不进召回面 · `OP-17` 溢出文件无清理策略（实测 5 文件/72KB，低危）· `OP-08` 子代理深度未实证 · `OP-16` 降级语义未落地成代码名词 · `hooks.js` 参数改写审计风险）已逐项写明下一步，**需排期**。
   ```
   node scripts/migration-rehearsal.mjs          # 临时库真实行数：ALTER 39ms，六张表行数逐项不变 → 通过
   node scripts/audit-ledger.mjs                 # 过账清单逐项取证（只读；已排除自身，避免探针自匹配）
   # 部署态（用户授权后）：25cc3df → d2f2048
   #   升级前后指纹逐项相同；result_bytes 列在且全部回填
   #   selfcheck 12/12 · e2e-final 21/21 · 日志零 ReferenceError
   ```
6. **RA-09 / RA-12 / RA-13 / RA-14**（非 G 目标但属 §14）—— **四条全部本轮做完**：`RA-09` 按需注入、`RA-12` 工具面收窄、`RA-14` 额度切分、**`RA-13` 真模型端到端失败夹具（两种失败形态均通过）**。
7. **M2 范围**（不在 M1 内，勿混算）：RA-15–25（沙箱/视窗/围栏）、RA-32/33（多引擎）、RA-34（客户形态）、RA-41/42（对外形态）；本轮新登记的 M2 尾巴：SSE `Last-Event-ID` 续订。

> **本批自曝的两个真 bug（已修，留痕）**：① 我加的"补流对账"把 `if (!skipStore)` 嵌成两层，`const result` 越出作用域 → **每轮对话都在 `done` 前中断**，靠**部署后的 selfcheck** 才抓到（本地测试不经过 `server/index.js` 主链路，这正是"真实启动关"该挡的）；修法＝结果提到处理器作用域 `runOutcome` + 异常补堆栈日志 + 把对账抽成 `server/streampatch.js`（6 条夹具）。② `selfcheck` 的 SSE 项本身引用未定义变量 → 流正常也恒报红，已修。**教训：部署后第一件事是跑针对本次改动的行为验证，不是只看服务 active。**

> **诚实声明**：本矩阵中 ❌ 的 14 条里，**2 条**（`RA-35` 缺真实流量、`RA-05b` 结论为负）是**本批未做完或结论为负**，**12 条**属 M2/M3 里程碑范围；另有 `RA-36` 项下**6 项开放旧账**需排期（第四节 4.3）。**M1 尚未达成，不申请完成**；下一步：`RA-35` 等真实流量、6 项开放旧账排期。

## 四、旧账过账清单（RA-36 的"闭环"部分·2026-09-15 收口批定稿）

> **取证口径**：**只看实现代码 + 运行时事实**，文档表述不算"已做"的证据。取证脚本 `scripts/audit-ledger.mjs`（只读，可复跑；已排除自身，避免"探针匹配到自己"）。
> **处置四档**：**已闭环**＝实现已在且已验证（写明确证位置）· **已降级**＝原问题已被另一机制覆盖 · **转 M2/M3**＝明确不在 M1 范围、有里程碑归属 · **仍开放**＝无人做、需排期。
> **本批未删除、未重写任何旧账**（唯一删除是步7 的 `tools/meta.js`，数据整体迁入 `manifest.js`，属重构搬迁）。

### 4.1 已闭环（5 项）

| 项 | 处置 | 明证（实现位置，可复查） |
|---|---|---|
| `OP-14` 脱敏机制 | **已闭环** | `redactSecrets()`（`server/tools/index.js`）在 **22 处**生产引用：`tool_calls`/`audit_log` 落库前脱敏、SSE 事件脱敏、`spill` 落盘前脱敏（`spill.js` 的 `meta.redact`） |
| `OP-15` 命令策略能力边界 | **已闭环** | `server/tools/hooks.js:118` `registerHook('before','run_command','danger_command_guard')` fail-closed 拦破坏性命令；配套 `scripts/security-check.mjs` 机检；本轮实测它先于审批生效（`echo` 类命令被要求改用专门工具） |
| `OP-18` 工具热装载（已完成） | **已闭环** | `server/tools/registry.js:87 reloadManifest()` + `:117 startManifestWatch()`；`RA-03` 实测 65↔64 不重启 |
| `OP-10` spill 阈值标定 | **已闭环（结论为负）** | `tool_calls.result_bytes` 上线并**全量回填**（`{"n":3926,"withBytes":"3926"}`，随真实流量增长）；标定结论＝**阈值被 `cap=4000 字符` 几何蕴含、当前不生效**（`scripts/calibrate-spill.mjs`）。**"部分失效触发点"仍未确认** → 拆出为开放项（见 4.3） |
| `tool_calls.result_summary` 只存前 2000 字符 | **已降级** | 步6 的 spill 落盘补上"可回查"（`fetch_spill`，路径围栏在 `spill.js:73`）；表内摘要仍 2000 字符属**有意的留痕取舍**，非缺陷 |

### 4.2 转 M2/M3（8 项，明确不在 M1 范围）

| 项 | 处置 | 归属与理由 |
|---|---|---|
| `OP-01` 数据出口唯一化 | 转 M2 | 对应 `RA-15`–`RA-19`/`RA-25`（沙箱 + 数据视窗）；**实现里没有任何出口统一层**（取证：`数据视窗/dataView/egressOnly` 零命中） |
| `OP-07` 外部前提实测 | 转 M2 | **M2 硬门禁**；对应 `RA-20`–`RA-24`、`RA-34`；实现里无 `gVisor`/外部引擎接入 |
| `OP-05` AgentAdapter 契约 | 转 M2 | 对应 `RA-32`；契约未写（取证：`AgentAdapter` 零命中） |
| `OP-06` 三家引擎成本基线 | 转 M3 | 对应 `RA-33`、`RA-35` 的多引擎部分；当前只有单一引擎（deepseek）真实流量 |
| `OP-09` 配额缓冲/gVisor/等价物选型 | 转 M2 | 平台侧选型，随沙箱批一起定 |
| `OP-04` 技能审核闸门 | 转 M2 | 平台侧治理；实现里无 `skillReview` |
| `OP-13` 岗位包定版 | 转 M3 | 平台侧文档类，无代码载体 |
| `OP-11` 提示注入工程化 | 转 M2 | `§5.7` 只给原则与两条手法，落地机制未定；实现里无 `injectionGuard` |

### 4.3 仍开放（6 项，需排期 —— 这是"闭环"里**真正没做完**的部分）

| 项 | 处置 | 缺什么 / 下一步 |
|---|---|---|
| `OP-03` 策略引擎失败语义 | **仍开放** | hooks 是 fail-open 默认（13 次注册中仅 **2 个** `failClosed:true`），语义未统一。下一步：要么逐个标注，要么把默认改 fail-closed 并评估影响面 |
| `OP-12` 经验/复盘层归属 | **仍开放** | `reviews` 表**能写能查**（`server/index.js:1833` INSERT、`:1848/:1851` 查询），但**不进任何召回/注入面**（`agent.js` 对 `reviews` 零命中）⇒ 复盘结论目前不会被下次任务读到，"经验复用"未闭环 |
| `OP-17` 溢出文件清理策略 | **仍开放（低危）** | `spill.js` 只有写出（`:57 mkdirSync` / `:60 writeFileSync`）与读取（`:78`），**无删除/保留策略**。实测现状：服务器 `/srv/rw-workspace/spill` **5 个文件 / 72KB**、磁盘 20% ⇒ 尚未成灾。下一步：加"按天数/总量清理"，或明确接受"由运维手工清" |
| `OP-08` 子代理深度实证 | **仍开放** | 深度上限**硬编码 3 层**（`server/subagent.js:109`），但"2 层够不够"从未实证。下一步：造一次真实三层任务，看第三层是否真被需要 |
| `OP-16` 降级语义 | **仍开放（命名层）** | 行为已有两处：溢出失败→`已按内联截断降级`（`spill.js:65`）、子代理失败→`degraded:true` + "该块未取得"（`subagent.js:79/82`）；但架构用的 `enforcement: full/partial/none` 三值**实现里零命中** ⇒ 口径未落地成代码名词。下一步：要么实现该枚举，要么把架构措辞降到与实现一致 |
| `hooks.js` 参数改写与"日志/实际执行一致" | **仍开放** | `hooks.js` 有 5 处 `payload.args` 改写与 `hookNote` 回写；**"日志记录的是改写前还是改写后"这个审计风险未处置**（本批未动） |

### 4.4 与其它项的边界（避免重复计数）

- **`OP-02`**（自动验证谁读业务数据）owner 平台、`RA-31` 依赖它 —— 归平台批，不在本清单闭环。
- **`OP-10` 的"部分失效触发点"**：与 `RA-35` 的 C4/C5 同源；本轮已把 C4 机检口径做实（`prefix:invalidate` 账本），真实触发点仍需真实流量，见 §三 ①。
