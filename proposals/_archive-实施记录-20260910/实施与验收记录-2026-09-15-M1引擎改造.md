# M1 引擎改造实施与验收记录（8 步最短路径）

> **批次性质**：用户授权自主顺序执行。总目标（DSH goal `goal-b73c3b77`）：按《RW-Agent 架构 v1.1》与《引擎改造计划》最短路径，把 RW 的 agent 引擎改造到 **M1「目标达成」**。
> **权威**：架构=`proposals/RW-Agent架构-v1.1.md`；计划=`proposals/RW-Agent引擎架构优化方案-v0.3.md`（五目标 G1–G5 / 三里程碑 M1–M3 / C1–C5 指标 / 8 步顺序）。
> **记录口径**：每步给"改了什么 + 假设与权衡 + 怎么验证的 + 证据（commit / 输出）"。

---

## 步骤 0 · 文档同步与仓库收敛（2026-09-15）

- 输入批次（另一对话产出的 00–50 编号文档 + `architecture.md`，共 52 份）整体归档至 `docs/archive/agent-arch-input-20260915/`，附索引与采用/未采用对照（**只读参考，非执行文档**）。
- 架构 **v1.1 定版**：依据四路自审（开放视角对决 DS/Codex、自省视角审文档、现状视角对代码、最终目标视角终审）修 **P0×3 + P1×12 + P2 择要**；新增 §14.0 目标→验收可追溯表、`RA-35`–`RA-42`、`RD-13`–`RD-15`、`OP-13`–`OP-16`、§16 自审与修订记录。
- 计划联动三处：沙箱失败语义（改 `enforcement: full/partial/none`）、检索口径（"语义检索"→"全文 FTS5 打底"）、账本口径（→"落账在引擎、对账在平台"）；其 §9.1 D3 登记为"修订"。
- 仓库收敛：本地与服务器分叉（服务器有 9 个每日进化提交）→ 取服务器 bundle 本地 rebase（干净无冲突）→ 服务器侧 `guard-deploy` 快进推送（**未强推**）。
- **证据**：`832a33e`（架构 + 归档）、`bb691f5`（基线回填）；selfcheck 12/12；归档目录 53 个文件在位。

## 步骤 1 · C1–C3 成本基线（只读，未改代码）

- **数据源**：`usage_stats` 2,760 行 / 2,744 轮 / 67 runs / 59 会话，窗口 2026-09-04~09-15；**缓存字段零空值**（数据有效性已核）。
- **结果**：
  - **C1 会话整体命中率**：全量 **12.52%**（deepseek 11.30% n=2591；glm 58.58% n=153）、近 7 天 **25.32%**；
  - **C2 每轮新增未命中**：近 7 天中位 **22,142**、P95 87,936（全量中位 35,996）；
  - **C3 单位成本**：每 run **¥2.89**（67 runs）、每会话 **¥4.34**（59）、累计 **¥255.88**；
  - **C4 近似**（整段作废候选）：**47** 次。
- **成本集中度**：会话 184 / 185 / 393 合计 ¥238（占 93%），其中会话 393 每轮 miss 120k / prompt 124k ≈ **每轮全量重算**。
- **潜在收益**：现状输入成本 ≈ ¥242；若达 99% 命中 ≈ ¥13（**↓约 95%**）。
- **工具**：`scripts/baseline-cost.mjs`（入库，服务器可复跑，改造前后对照同一把尺）。
- **证据**：`bb691f5`；服务器复跑输出与本记录一致。

## 步骤 2 · 环境事实单一来源（配置注入）

- **改了什么**：
  1. 新增 `server/env.js`：`RW_PLATFORM_DIR` / `RW_WORKSPACE` / `RW_SKILLS` / `RW_SERVICE` / `RW_SEARCH_ENGINE`，**默认值＝现行部署值**；
  2. `server/agent.js` 提示层参数化——`ENV_ENV` 的路径与搜索后端名、`ENV_IDENTITY(write)` 的工作区路径、`ENV_DISCIPLINE` 的行为准则路径，全部改为引用 env.js；
  3. **12 处重复默认值收敛**（`tools/index.js`、`tools/checkpoint.js`、`driver.js`、`index.js`、`runtrack.js`、`scheduler.js`、`channels/feishu-webhook.js`、`channels/wechat.js`）——各文件新增 `import ... from '../env.js'`，不再各自复制默认值。
- **假设与权衡**：假设"环境事实只在这 8 个文件出现、且不随会话变化"（已 grep 核实）；**只抽环境事实，纪律条款原文一字不动**，把行为回归面压到最小；**不引入配置框架/分层抽象**（客户差异用环境变量表达已足够）。
- **验证（四项全绿）**：
  1. `grep -c '/srv/' server/agent.js` = **0**；
  2. 重复默认值只剩 `server/env.js` 一处（其它文件 `process.env.RW_* || …` 零命中）；
  3. **两份配置跑同一份代码**：`RW_PLATFORM_DIR=/tmp/pf-A|B` + `RW_WORKSPACE=/tmp/ws-A|B` + `RW_SEARCH_ENGINE=SearchA|B` → 提示层环境块分别显示 A / B；
  4. 部署后：服务 active、`selfcheck` **12/12**、`final-e2e` **57/57**、日志 error **0**；服务器上 `server/env.js` 实证默认值＝现值（**行为不变**）。
- **证据**：`87c1871`。

---

## 步骤 3+4 · 前缀三杀手 + 审计原文留存（同批，2026-09-15）

- **改了什么**（3 处代码 + 1 处核证）：
  1. **删除 `archiveEarlyContext`**（`agent.js`）：原逻辑在"条数>90 或累计字符>65000"时就地改写早期 tool/assistant 内容、折叠 tool_call 参数——**一旦触发就让其后全部历史的前缀缓存失效**（长会话里几乎每轮触发）。按纪律 1（只追加）删除，原位留注释说明依据与替代方案。
  2. **工具面会话内冻结**（`agent.js`）：`defs` 与 `toolsHash` 从循环内提到 `runAgent` 开头，一次运行只计算一次（纪律 3）。
  3. **易变注入迁"尾巴区"**（`index.js`）：目标 / 技能 / 知识 / 断点现场 / 本轮只读意图 / 高成本自荐 **六类从 hist 之前移到 hist 之后**；历史之前只留固定段——早期摘要、用户自定义指令、AGENTS.md、壳语境（纪律 2）。
  4. **新增机检**（`agent.js`）：每轮比对"非 system 消息序列"与上轮是否逐条**同一对象**（＝只追加）；断链且非折叠轮次 → `console.warn('[prefix-invariant] …')`；折叠发生 → `console.warn('[collapse] …')` 记归因；`RW_PREFIX_DEBUG=1` 时打印 sys/tools 哈希与 core 条数。
- **步 4 核证结论**：全仓 grep 显示 `messages` 表**只有 INSERT、没有任何 UPDATE/DELETE** → **DB 本就保原文**，无需新建留存通道；折叠只 splice 内存副本，符合 §7.3 双投影（模型可见副本 vs durable 原文）。
- **验证（运行时取证）**：
  - 探针会话 **528**（5 轮串行工具，read 权限）逐轮 `命中/未命中`：
    `10112/2013 → 11776/1838 → 13184/533 → 13312/692 → 13568/686`；
    **命中逐轮单调增长**（前缀在累积并被复用），**每轮新增从基线中位 22,142 降到 533–692（≈30 倍）**，该会话 C1 = **91.5%**。
  - 日志取证（会话 527）：`[prefix-debug] round=1 sys=bc39a145ff6b tools=b870d41be1ae core=1` / `round=2 sys=bc39a145ff6b tools=b870d41be1ae core=5`
    → **系统层与工具面逐字节一致（哈希相同）、消息只追加（core 1→5）**；**零 `[prefix-invariant]` 告警**。
  - 回归：本地单测 **11/11**、selfcheck **12/12**、final-e2e **57/57**、服务 active。
  - 卫生：探针会话及其用量/观测/工具调用行**已删除**（残留 0，不污染基线）；临时开关 `RW_PREFIX_DEBUG` 已从服务器 `.env` 移除并重启确认。
- **证据**：`da1bb82`。
- **诚实说明**：本次取证是**短会话 + 缓存热**（轮间隔数秒）条件下的结果；**长会话与长空闲下的表现需用真实流量复核**——改造前后的 C1/C2 对照将在积累真实流量后用 `scripts/baseline-cost.mjs` 复测。

---

## 步骤 5 · C1–C5 仪表落地 + 折叠路径取证（2026-09-15）

- **改了什么**（全部落在现有载体上，**不新造表、不新造指标系统**）：
  1. **C5 豁免归因**（`agent.js`，运行起点只算一次）：查本会话最后一条 `usage_stats`，分类 `first-round` / `idle:Nmin`（间隔 ≥ `RW_IDLE_MIN`，默认 60）/ `model-switch:A→B`，命中则落 `audit_log(action='prefix:exempt')`。
  2. **C4 机检落账**：前缀不变量断链且**非折叠轮次** → `audit_log(action='prefix:invalidate', detail='first-diff-idx=…')`（此前只有日志行，现在可统计）。
  3. **折叠可数**：折叠发生 → `audit_log(action='prefix:collapse')`；C5 的折叠边界只报数、不设 0。
  4. **`scripts/baseline-cost.mjs` 扩展**：C1–C4 报告 + 失效账本（`prefix:*` 分组、C5 归因 top6、C4 明细 last5）。
- **两个真 bug（自检发现，当场修）**：
  1. **`agentLimits()` 静默回退**：`failPick` 定义在 `try` 之外却引用 `try` 块内的 `const rows` → **必然抛 `rows is not defined`** → 被裸 `catch` 吞掉、**所有 settings 护栏阈值一律回退默认值**（"折叠阈值设了不生效"的真凶），且无任何日志。修：用同语义的 `pick` 取代 `failPick`；`catch` 改为 `console.warn('[limits] 读取护栏失败，已回退默认值：…')`（不再静默降级）。
     → 修后实测 `[limits-debug] {"gap":1,"keep":1,"trig":1}` **＝设置值**，阈值真正生效。
  2. **折叠保留条数无下限 → 会话不可收敛**（`keep=1` 实测）：探针会话 542 在 300s 内跑 **58 轮 / 112 次工具调用**（`db_query×76 · list_dir×22 · kb_search×14`）**仍未收敛**——每轮折叠都把工作集折掉，模型反复失忆、重新探索。修：`COLLAPSE_KEEP_MIN = 10` 下限 + 低于下限**如实告警**（`[limits] collapse_keep_msgs=1 低于下限 10，已按下限生效`），`settingsSchema.js` 提示同步标注下限。
     → 修后同一探针：**4 轮 / 3 次工具调用 / 6s 收敛**（对照默认阈值 4 轮 / 3 次 / 7s）。
- **验证（可照抄的取证命令与实测输出）**：
  - 默认阈值探针（conv 543）：`LLM轮=4 工具事件=3 折叠=0 耗时=7s`；日志 `[prefix-debug] core=1→3→5→7` 纯追加、`sys=bc39a145ff6b`/`tools=58c929e998eb` 逐轮同哈希、零 `[prefix-invariant]`。
  - 折叠账本（conv 542，未加下限时的 keep=1）：`prefix:collapse round=1 … round=59`，**同时 C4 = 0**——折叠轮次的断链被正确归因为"预期失效"，未误报非预期改写。
  - **`prefix:invalidate` 全表 = 0 行**（机检上线至今，含全部探针与自检流量）。
  - 基线复跑（`node scripts/baseline-cost.mjs`，同一把尺）：C1 全量 **12.53%** / 近 7 天 **25.37%**；C2 近 7 天中位 **22,007** / P95 87,904；C3 **¥2.84/run**、¥4.27/会话、累计 ¥255.90；C4 近似（整段作废候选）**47**。
  - 回归：`selfcheck` **12/12**、`final-e2e` **57/57**、服务 `active`；临时开关（`RW_PREFIX_DEBUG`、`RW_IDLE_MIN`）已从服务器 `.env` 移除并重启复验。
- **诚实说明**：
  1. **C1/C2 与基线几乎一致属预期**：基线以改造前的历史数据为主（2,762 行里绝大多数是改造前产生），改造效果必须用**改造后的新流量**才能度量——同一脚本复测即可。
  2. **两种 C4 口径并存、不可混用**：`47` 是"整段作废候选"的**历史近似**（粗）；`prefix:invalidate` 是机检上线后的**实测精确计数**（当前 **0**）。引用时必须写明口径。
  3. **失效账本是按事件追加、不随会话删除清理**：探针 542 在客户端超时后服务端仍在继续跑，清理动作之后仍补写了一条 collapse 行；`selfcheck` 的临时会话也留下 2 条 exempt 行（三行对应会话均已不存在）。因此**引用计数时应结合"会话是否仍存在"或按时间窗口取数**。
- **证据**：`db3b151`（C1–C5 落地）、`7ee04ca6`（折叠诊断打点）、`c04be26`（`failPick` 修复）、`06353c9`（折叠下限）。

---

## 步骤 6 · 工具结果溢出 spill（2026-09-15）

- **改了什么**（1 个新模块 + 4 处接线）：
  1. 新增 `server/tools/spill.js`：`spillToolResult(text, cap, meta)` / `readSpill(path, offset, length)` / `SPILL_BYTES = 32768` / `SPILL_DIR = <工作区>/spill`；三条路径（读取类 / 已落盘 / 降级）各打一行 `[spill]` 日志便于取证。
  2. `agent.js`：**删除**旧的就地截断 `contextResultPrune`——它把结果中段直接丢弃，而库内 `tool_calls.result_summary` 只留前 2000 字符，**大结果实际不可回查**（信息真丢）；改为 `spillToolResult(...)`（带 tool / args / conversationId / callId / 脱敏函数）。
  3. `tools/index.js`：新增 `fetch_spill` 工具（按 `offset`/`length` 取回溢出文件；路径围栏限定在溢出目录内，越界直接拒绝）。
  4. `tools/meta.js`：`fetch_spill` 登记 **core** 档 + 进 `PLATFORM_EXEMPT`（**恒可用**——否则模型拿到定位符却没有取回手段）+ 中文名。
  5. `src/Chat.jsx`：`PRESET_TIP` 里"暴露全部 **61** 工具 / core+pro **52** 个 / 仅 core **21** 个"是**过时假信息**（实际 core 24 + pro 33 + expert 7 = **64**），改为不写死条数、只写包含哪一层；权威条数在后台「设置→工具」（`/api/toolset` 实时给出）。前端产物入库，故同步 `npm run build` 重建 `web/dist`。
- **定稿规则**（已同步进架构 §5.3/§5.4）：进上下文的**内联上限 = 4000 字符（子代理族 12000）且 32768 字节**，超任一即溢出；预览 = **头 60% + 尾 30%**；省略量**字节与字符都给**；定位符三态 = 源文件（读取类）/ 溢出文件（已落盘）/ 降级提示（存盘失败）。
- **验证（可照抄）**：
  - 单测 `node --test test/spill.test.mjs` → **6/6**。**故意破坏验证**：把 `SPILL_BYTES` 改成 `999999999` 后，**恰好只有"字节天花板生效"那条报红**（5 过 1 挂），还原后 6/6 —— 证明测试真的会失败。
  - **端到端（落盘 + 取回）**：read 会话让模型 `db_query` 取 50 行大结果 → `[spill] tool=db_query conv=549 bytes=10491 outcome=stored /srv/rw-workspace/spill/549/db_query-call_…txt`；溢出文件 10491 字节 / 9880 字符；**模型自行**按上下文里的定位符调用 `fetch_spill` → `status=done`；`readSpill` 取回前 40 字符与全文头部**一致**（total=9880）。
  - **读取类分支**：`read_file` 读 216KB 文件 → `[spill] tool=read_file conv=550 bytes=51865 outcome=reader /srv/rw-workspace/spilltest-big.txt`，**未产生溢出文件**；模型自述看到"已省略 48265 字节（48265 字符）"与"全文即源文件 …（共 51865 字节）"。
  - **降级分支**：把溢出根目录换成同名文件（→ `ENOTDIR`）→ `[spill] … outcome=degraded ENOTDIR`，**`db_query` 仍 `status=done`**，模型自述看到"已省略 6588 字节…全文未能存盘（ENOTDIR）"；随后目录已还原、测试文件已删。
  - 回归：`selfcheck` **12/12**、`final-e2e` **57/57**。
- **诚实说明**：
  1. **32768 字节尚未按 C2/C4 标定**（`RA-05b` 仍开放）：当前实际是**字符上限先触发**（中文 4000 字符 ≈ 12KB），字节天花板只在子代理族（12000 字符 ≈ 36KB）这类长文本上生效；标定需要真实流量的结果体积分布，样本不足。
  2. **溢出文件没有清理策略**（按"会话/工具-调用 id"落盘，只增不减）→ 登记为开放项 `OP-17`；**不自行发明保留期**（属运维策略）。
  3. 溢出写盘做了**与落库同口径的脱敏**（复用 `redactSecrets`）——密钥不入盘。
  4. 探针选路时踩到的**权限模型**（非缺陷）：read 会话读 `/srv/harness-workbench` 被拒（"路径超出工作区"），故本轮改用工作区内文件与库查询取证。
- **证据**：`5274d46`。

---

## 步骤 7 · 工具清单装载器（声明式装配，2026-09-15）

- **改了什么**：
  1. 新增 `server/tools/manifest.js`：**声明式能力清单**（65 条）——每条含 `tier / cn / when / not / ex` 与 `defaultOn / exempt / light` 标记；**上下线语义**＝删掉该行，或写 `enabled: false`（留痕式下线）。
  2. 新增 `server/tools/registry.js`：**装载器**——清单 × 实现 → 运行时工具表；元数据与四个集合（默认启用集/平台豁免集/轻量集/中文名）**全部由清单派生**，不再各写一份（此前一个工具要在 3 个结构里各加一次，步6 就是这么改的）。
  3. `server/tools/index.js`：`TOOLS` 字面量与 4 处 `TOOLS.push` 改名 `RAW_TOOLS`（实现侧），文件末尾 `export const TOOLS = assembleTools(RAW_TOOLS)`，元数据/集合改为从 registry 转发。
  4. **删除 `server/tools/meta.js`**（数据整体迁入 manifest），4 处引用改指 registry：`server/index.js`、`server/agent.js`、`server/canary.js`、`server/tools/hooks.js`。
  5. 新增 `test/manifest.test.mjs` + `test/fixtures/tools-snapshot.json`（**改造前快照**，作为"零漂移"对照基准；由服务器实跑导出）。
- **设计裁量（对架构 §4.3 字段表的一处偏离，依"技术架构我做权威"裁定并登记）**：§4.3 曾把"描述/参数 schema"也列进 manifest。落地时**不把契约面搬进清单**——描述与参数是实现的紧邻契约（改实现几乎必然改描述），拆到两个文件只会漂移。因此：**清单拥有策略与生命周期**（档位/中文名/选择提示/三集合/上下线），**契约面与实现同处** `tools/index.js`。装载器用校验把两边钉死：
  - 清单声明了却无实现 → **抛错**（清单不许说谎）；
  - 有实现却未进清单 → **默认拒绝 + 告警**（下线＝删一行）；
  - 重名 / 缺 `run` / 缺 `description` / 缺 `permission` → **抛错**（装配期暴露，不留到运行期）。
- **验证（可照抄）**：
  - 单测 `node --test test/manifest.test.mjs` → **6/6**：清单合法性、**与改造前快照逐字段零漂移**（description / 参数 schema / permission / params / meta / 中文名 / 三个集合 / 档位中文名）、默认拒绝、四类装配期拦错。
  - 端到端（服务器实跑 `/tmp/rw-step7-load.mjs`）：正常态 `工具面=65 实现面=65 清单启用=65 清单条目=65`，`repo_map` 三面皆在且**可执行**（`execTool` 返回 `ok:true`）。
  - **下线验收**：把清单里 `repo_map` 一行注释掉 → 重启后 `工具面=64 实现面=64`，`repo_map` 三面皆 **false**，`execTool('repo_map')` → **`未知工具: repo_map`**，启动日志出现 `[registry] 未进清单，已按默认拒绝不装载：repo_map`。
  - **还原验收**：取消注释 → 恢复 `65/65/65` 且可执行；服务器工作树 `git status --porcelain` **为空**、HEAD 与本地一致（扰动未落盘）。
  - 回归：`selfcheck` **12/12**、`final-e2e` **57/57**。
  - 顺带查实（**未修，留给步8**）：`node --test` 全量 62 条 → **61 过 / 1 挂**：`test/knowledge.test.mjs` 断言的是**旧 SQL**（实现已在 A6 追加 `AND status="active"`），属**测试陈旧而非代码缺陷**；因 `npm test` 只跑 2 个文件，这条陈旧断言长期没被跑出来。
- **诚实说明**：
  1. 架构 `RA-03` 的"加/删工具**不整体重启**"**未达成**：本轮是**启动期装载**，改清单需重启服务（≈3 秒）。热装载（扫描/监听清单变更）不在步7 范围 → 登记为开放项 `OP-18`。
  2. **MCP 外部工具不经本装载器**（仍走 `syncMcpExtras` + `execTool` 的 `mcp_*` 分支）：架构 §4.2 的"五类来源进同一注册表"目前只完成了**自研工具**这一类；MCP 合流属 M2 /《计划》§7.1 ⑭。
  3. 快照对照只覆盖**本地自研工具**（不含动态 MCP 工具）；工具**顺序**沿用实现声明顺序，本轮未改。
- **证据**：`574dda8`。

---

## 步骤 8 · 门禁 + 段边界（2026-09-15）

- **改了什么**：
  1. **`npm test` 修成跑全量**（`node --test`，此前只跑 2/10 个测试文件 → **61 过 1 挂**：`test/knowledge.test.mjs` 断言的是 A6 之前的旧 SQL，实现已追加 `AND status="active"` → **测试陈旧而非代码缺陷**，已按实现更正）。
  2. **新增 `test/invariants.test.mjs`（三条不变式机检，每条都带"故意破坏"负例）** + **抽出 `server/prefix.js`**（`diffCore`/`isUnexpectedBreak` 纯函数，运行期仍由 `agent.js` 每轮调用）：
     - ① 只追加/前缀冻结/工具面冻结：改写早期消息、截断历史、调换顺序**必须检出首个不同下标**；折叠轮断链＝预期失效（不计 C4），其它轮次必须计；工具面同输入同字节、**档位不同必须不同哈希**（证明检查非恒真）。
     - ② 第 2 层 fail-closed：安全网钩子（`danger_command_guard`/`system_write_guard`）**必须 fail-closed**，纪律钩子按设计 fail-open；`listHooks()` 增列 `failClosed`（"出事时是拦还是放"可审计）；注入抛错钩子 → fail-closed **必须拦**、fail-open 只告警。
     - ③ 系统层+工具层逐字节一致：环境/身份/纪律三层同输入同字节、权限层随权限变化（同样带"非恒真"对照）。
  3. **段边界（§5.3 纪律3 的另一半）**：进程内按会话记上次的工具面哈希，变更即判定"**新段**"——落 `prefix:exempt … tool-face-changed`、打 `[segment]` 日志、并在尾巴区注入一条如实说明（不假装没发生、也不计入 C4）。
  4. **CI 工作流** `.github/workflows/ci.yml`：push/PR 即 `npm install` + `npm test`（仓库暂无 lockfile，故用 install；有 lockfile 后应改 `npm ci`，已写在 workflow 注释里）。
- **验证（可照抄）**：
  - `npm test` → **70 测试 / 70 通过 / 0 失败**（本批前：62 跑 / 61 过 / 1 挂，且 `npm test` 只跑 2 个文件）。
  - **门禁自证（故意破坏三条不变式 → 必须报红）**：脚本逐条注入故障后跑 `test/invariants.test.mjs`：
    `① 只追加: ✅ 报红（fail=2，命中预期断言）`、`② fail-closed: ✅ 报红（fail=1）`、`③ 工具面冻结: ✅ 报红（fail=1）`；**还原后 8/8 全绿**，注入未残留（git diff 仅含本次正式改动）。
  - **段边界端到端**：同一会话两轮任务消息，中间把启用集去掉 `repo_map` → 第二轮账本出现 `prefix:exempt | tool-face-changed`，日志 `[segment] 工具面变更（新段）：conv=560 tools=c6a7759474a9 首轮缓存预期重建（不计 C4）`；第一轮只有 `first-round`（**无误报**）。
  - 回归：`selfcheck` **12/12**、`final-e2e` **57/57**。
- **诚实说明**：
  1. **CI 未真正跑过**：本机与服务器都无法访问 GitHub Actions（本地连不上 github.com；服务器只用于 push）。workflow 已按标准写法提交，`npm install + npm test` 两条命令**本地实跑通过**，但"GitHub 上真的绿了"这件事**没有证据**，等首次 push 后由平台侧确认。
  2. **轻量通道的工具面不随启用集变化**（前两次探针未触发新段的原因）：快速问答轮次走 `LIGHT_TOOLSET` 固定面（`toolDefs('all', null)` 后按白名单裁），因此改"账号启用集"在轻量轮次**不构成新段**——这是设计使然，不是漏检；任务模式才会体现。
  3. 三条不变式的运行期机检（`[prefix-invariant]` / `[prefix-debug]` / `[collapse]`）仍**只在 `RW_PREFIX_DEBUG=1` 时打印部分细节**；C4/C5 账本（`audit_log`）恒开。
  4. `isUnexpectedBreak`/`diffCore` 的抽取是**纯函数搬迁**（比对逻辑一字未改），运行期语义不变。
- **证据**：`f59edbf`。

---

## 批次收尾（8 步最短路径完成态）

| 步 | 内容 | 末提交 | 状态 |
|---|---|---|---|
| 0 | 文档同步与仓库收敛 | `832a33e` | ✅ |
| 1 | C1–C3 成本基线 | `bb691f5` | ✅ |
| 2 | 环境事实单一来源 | `87c1871` | ✅ |
| 3+4 | 前缀三杀手 + 审计原文留存 | `da1bb82` | ✅ |
| 5 | C1–C5 仪表 + 折叠取证 | `06353c9` | ✅（顺带修 2 个真 bug） |
| 6 | 工具结果溢出 spill | `5274d46` | ✅ |
| 7 | 工具清单装载器 | `574dda8` | ✅（启动期；热装载未做→`OP-18`） |
| 8 | 门禁 + 段边界 | `f59edbf` | ✅（CI 未实跑） |

**M1 尚未结束的部分（诚实登记）**：`RA-03` 热装载、`RA-05b` spill 阈值标定、`OP-01`–`OP-18` 开放项、真实流量下的 C1/C2 复测（`scripts/baseline-cost.mjs` 同一把尺）——**"引擎改完"不等于"M1 目标达成"**，M1 的最终判据是《架构》§14 全量验收 + 干净机器一份配置跑通。

---

## 诚实登记（本轮踩到的坑与遗留）

1. **本地连不上 GitHub**（代理 127.0.0.1:443 不通）：推送一律由服务器侧 `guard-deploy` 完成；本地 `origin/main` 引用是旧的，联网后需 `git fetch` 刷新。
2. **步骤 2 首轮提交失败未被及时发现**：漏写 `git add -A` → `git commit` 空提交 → bundle 仍是旧提交 → 部署的是旧代码（当时 selfcheck/e2e 是旧代码跑出的绿），**由服务端提示层实证脚本报 `MODULE_NOT_FOUND` 暴露**。已修正并重做。
   → **教训（已纳入流程）**：部署前必须核对 `git log --oneline -1` 与 bundle 内 HEAD 一致，部署后必须做**针对本次改动的行为验证**（而非只看 selfcheck/e2e 绿）。
3. ~~根因假设仍未验证（待第 3 步用哈希取证）：C1 偏低的直接嫌疑是 `index.js` 把 6+ 段易变内容注入在历史之前 + `agent.js` 的 `refreshSys()`/`archiveEarlyContext`/`maybeCollapseEarly` 会改写前缀。~~
   → **已在步骤 3 取证并处置**（原始记录保留以留痕）：嫌疑成立，`archiveEarlyContext` 是"每轮改写前缀"的元凶，已删除；易变注入已迁尾巴区。步骤 5 又查出**两个真 bug**：`agentLimits()` 因 `failPick` 作用域错误**静默回退全部阈值**（这让设置里的折叠阈值形同虚设）、折叠保留条数**无下限**可致会话不可收敛（已加下限 10）。
