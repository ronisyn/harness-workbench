# v0.3 剩余项收口表（2026-09-16 · 复核后）

> 口径：本表是**活表**——按"文档要求 vs 现状"逐条核，只列**还没收口**的项（已收口的在各自提交与冲突登记里，不在此重复）。
> 判定三值：**在做**（已派、有明确验收判据）／**待做**（无阻塞、排队中）／**待条件**（缺环境、缺时间、或需用户许可）。
> 依据：`RW-Agent引擎架构优化方案-v0.3.md`（唯一权威）；事件与偏离登记见 `架构文档冲突登记-20260915.md` 的 C-53…C-62。
> 基线读数（本轮开头实测）：全量 `776 / 774 pass / 0 fail / 2 skipped`；真机 15 段验证全绿；三方一致 `cd618a9`。

## 一、里程碑出口（§0.4）

| 出口判据 | 现状 | 状态 | 验收判据 |
|---|---|---|---|
| **G1**：干净机器 + 一份配置 → 跑通一次对话 + 一次工具调用 | 存储接口与两个实现已就位（账号/会话/消息/设置 13 个方法 + 52 条两实现契约用例），**调用点仍直连 MySQL** ⇒ 无 MySQL 的机器跑不起来 | **在做** | 一次性工作区 + `RW_STORAGE=jsonfile`、**不连 MySQL**：登录 → 建会话 → 一轮 → 落消息 → 读回历史 → 调一次工具（全程原始输出） |
| **G2**：装工具/MCP 不改代码；卸载后立即消失 | 声明式装载 + 热重载 + MCP 同一注册表已落地；"主动卸载 ⇒ 工具面立即少一条"的端到端读数仍缺 | 待做 | 一条端到端读数 |
| **G4**：模拟升级不丢数据；过账清单闭环 | 演练真机 `ok:true`；过账清单已闭环（C-39） | ✅ 已收口 | — |
| **M2**：被别的 agent 当 MCP 工具调用成功一次 | 只到官方 Inspector | **在做** | DSH（＝DeepSeek Harness，本机已装、MIT 开源）与 Codex CLI 各真调一次只读工具；PI 尽力；Claude Code 登记原因 |
| **M2**：客户形态机器（Windows Server）装上、自检通过、账本可导出 | 脚本与文档在位，**真机未装过** | **待条件** | 一台 Windows Server + 管理员权限 |
| **M3**：连续两个周期产出并落地提案（≥1 外部对标、≥1 自我体检，每条附前后对比，全程无自我放行） | 机制齐；**周期数/落地数/归档位置三样缺** | **在做** | 判据载体（三态判定，数据不足 ⇒ `undecided`）＋ 归档进仓库 `proposals/` |
| **M3 准入前置**：金标回归 + C4 失效监控在位 | ✅（金标门禁进 release 与 CI；C4 账本机检 0） | ✅ 已收口 | — |

## 二、§7.1 二十四项里仍未收口的 8 项

| 项 | 缺口（具体到一点） | 状态 |
|---|---|---|
| ① 运行面 | 版本号与变更说明**已收口**（`RW_VERSION` → MCP/JSON-RPC/启动日志/`/api/health` + `CHANGELOG.md`） | ✅ |
| ② 扩展面 | "三层分离"实为**有意的两层**（契约面与实现同处，代码注释写明理由）⇒ 需把收窄写回文档（避免每轮重判"部分"） | 待做（文档注） |
| ⑤ 声明化 | `permission` 已进清单（65 条 + 装配期双核对）；`parallelSafe` **只声明、无消费者**（引擎仍按 `maxParallelT` 无差别并发） | 待做（或登记为"声明先于消费"） |
| ⑦ 存储抽象 | 见 G1：接口在、调用点未迁 | **在做** |
| ⑯ 双平台 | 抽象与平台判据归属已落地；**Windows 臂只有本机/纯函数证据** | **待条件**（Windows Server） |
| ⑳ JSON-RPC | stdio 形态与夹具在位；**无 HTTP 挂载**（判定：文档只要"形态"，stdio 足够；HTTP 属可选） | 待做（可选，登记口径） |
| ㉑ 成批编排 | 四档触发器接口已定义、**手动/定时/外部三档无生产消费者** | **在做** |
| ㉒ 自进化三源 | 自我体检全自动；外部对标＝解析两份文档（半自动）、业务反馈＝人工录入（如实标注） | 待条件（外部对标要真抓取器＝另立需求） |

## 三、逐面设计里仍未收口的项（§4–§6）

| 条文 | 缺口 | 状态 |
|---|---|---|
| §4.2 | **连接器＝带凭证的执行后端**（只有占位字段 `connectors: []`） | **在做** |
| §4.3 | 模型能力声明（窗口/工具/视觉/思考）**无人消费**；**客户网关/内网推理无注册入口** | 待做（`server/index.js` 空出来即做） |
| §4.3 | "受限自动沉淀" | **在做**（决策：**提案式**，人审后才写库） |
| §4.4.1 规则4 | "给每轮新增设阈值并监控" | **在做**（决策：**机制在位、缺省不设线、由配置给数**） |
| §4.7 | 客户端可见性：**溢出提示**、**进度帧**、**断线重连的"缺口"帧**（`stream_gap`） | 待做（`server/index.js`） |
| §4.8 | "对账在平台"——对账能力在位但只有 CLI/夹具触发，**平台侧无承接面** | 待做（或登记为部署形态问题） |
| §4.9 | JSON 存储格式迁移链**已补**；**回滚**（无 downgrade，明文"只向前"）⇒ 判定为"代码层 + 有据可查"，需写进文档 | 待做（文档注） |
| §5 | **提醒通道**：邮件/短信全仓 0 命中 | **在做**（决策：引擎提供接口、产品决定通道） |
| §5 | **企微**零命中（只有飞书 + 微信 iLink） | 待做（决策：不补内置通道、改文档措辞、登记"待产品决策"） |
| §6.2 | 铁律机检只覆盖"路径+命令"一维 | **在做**（扩到库名/账号/页面/对客定价，白名单逐条写理由） |
| §4.4.1 规则5 | 跨轮"只追加"机检**已三路覆盖**（web/headless/渠道），headless 与 driver 的窗口/截断**已按规则1删掉**；**scheduler 路径无跨轮账**（形态不同：每轮只发一条 user 消息） | ✅ 主体收口；scheduler 那条登记为边界 |
| §4.9 / §7.1 ⑪ | **C-48 索引漂移**（链只加列不补索引；`schema-sync` 只管列） | **在做** |

## 四、已登记、明确"不做或后置"的（不再列为缺口）

- **C-45 沙箱迁移期偏离**：默认显式降级 + `RW_SANDBOX_REQUIRED=1` 严格语义；装 bwrap 或配 runner 后即完全落到文档口径。
- **C-47 指标门禁**：不设"变差线"、CI 不纳入（CI 无库必然"未在位"）；属有库的发布路径。
- **C-48/C-57**：索引漂移与"演练倒推对索引瞎"——前者本轮在做，后者已修。
- **C-50**：锚点子串匹配的假通过面（RA-36 已收紧；其余锚点待过一遍）。
- **C-53/C-54**：C1/C2 未达标的主因是**我们自己的部署频率与自造流量**（已修口径：自造流量自己声明身份）。
- **§0.6 明确不做**：岗位包、组织协作、原生 App、像素级对标、推倒重写成熟机制。
- **§8.5 本方案不解决**：岗位包与组织协作、计费结算与多租户、端侧原生 App、提示注入防线的工程化、具体技术选型。

## 五、需要用户给的条件（三件）

1. **一台 Windows Server**（+ 管理员）⇒ 闭合 M2 的"客户形态机器装上、自检通过、账本可导出"。
2. **M3 的两个周期**：需要时间；并且需要你允许**真跑一次提案落库**（`selfeval-propose --write`，只写 `extension_demands`/`evo_goals` 两张待审表）作为起点——现在真库那两张表 0 行，判据只能 `undecided`。
3. **外部对标的自动抓取器**（可选）：现在只解析两份本地文档（如实标注"半自动"）；要做成"自动读 DS/CD 变化"需要一个明确的数据源与许可。

---

## 六、第 23 轮状态（收口进度快照，2026-09-16）

**已交付且自成绿（等集成提交）**
| 面 | 交付物 | 读数 |
|---|---|---|
| §4.2 连接器 | `server/connectors.js` + `test/connectors.test.mjs`；另在 `server/index.js`（启动块 +8 行）、`server/tools/hooks.js`（2 处前缀守卫）、`server/tools/index.js`（`isExternalSource`）各只增改动 | **12/12 绿**；反向核对 4 处破坏全红；**真样例跑通**：飞书 HTTP 连接器两个只读 GET 返回 `code:0/msg:success`（无任何 POST） |
| §6.2 四维机检 | `test/boundary-dimensions.test.mjs`（8 条，7 条具名规则） | **19/19 绿**（与该文件 + portability 合跑）；反向核对五组含"白名单漂移也要能红" |
| §4.4.1 规则1/规则5 三路覆盖 | `server/prefix-assemble.js` + `test/prefix-paths.test.mjs`(17) + headless/driver 的窗口与截断删除 | 已入 `5c9dcc2` / `b83a5fa` / `cd618a9` |
| C-48 索引漂移 | `0008_indexes_of_baseline_tables` + `schema-sync` 也管索引 + `schema-sync.test.mjs` 索引判据 | 真机演练 `{"ok":true,"head":"0008…","tolerated":0,"checksFailed":[],"observationIndexDrift":[]}`；相关夹具 34/34 |
| M3 判据载体 | `server/selfeval/{m3-gate,alerts,archive}.js` | 交付说明已到；集成时随 selfeval 一起提交 |
| 五处决策落地 | `server/settingsSchema.js`（阈值配置面，缺省不设线）+ `test/metric-alerts.test.mjs`(7) + `src/Dashboard.jsx` | **7/7 绿**；文档五处措辞改动随该批提交 |

**尚未收口（在飞）**
1. **模型能力声明消费（§4.3）**：关键回归已按我的裁定修到**语义正确**（`capabilitiesOf('deepseek','deepseek-v4-flash')` 现在 `tool=null / vision=null` —— 不再因"营销标签里没写 tool"就砍掉工具面），但它那 6 条夹具正在按新规则重写，**未绿之前这一批不许提交**（它跨 `server/llm/gateway.js`、`server/capabilities.js`、`server/agent.js` 三个共享文件）。
2. **触发器三档接线（§4.5）**：还有 1 条红（"下一轮扫描不被上一轮那次 handler 抛错冻住"），夹具与实现在自调。
3. **G1 第二阶段（§4.1"存储走接口"）**：调用点迁移进行中；**它打破了 `test/llm-retry.test.mjs` 的 SQL 形状锚点**（已分派：锚点改接口形状、判据不许放宽）。
4. **集成阻塞**：`server/index.js`、`server/agent.js`、`server/tools/index.js`、`server/tools/hooks.js` 四个文件上同时压着 G1 / 连接器 / 模型能力三批改动 ⇒ 必须等这三批都停下来，才能按文件点名提交（否则会把别人的中间态卷进提交，本会话已因此吃过一次教训）。

**集成顺序（下一轮照此执行）**
1. 等上面三条全部转绿 + 全量 `# fail 0`；
2. 按文件点名 `git add`（**不用 `git add -A`**）分三笔提交：连接器／四维机检＋决策落地／G1＋模型能力；
3. `npm run build` → push → 服务器 `git pull` + `systemctl restart rw-test` → 跑 `tmp/verify-v03.sh` 15 段（判据：自检 13/13、金标 9/9、升级演练 `ok:true` 且 `tolerated:0`、指标门禁通过、汇总"✅ 全部项目已跑完"）；
4. 回到本表更新第五节与"待条件"三件。

---

## 七、第 24 轮状态（M2 出口闭合 · 第一批集成已部署）

**已入库并部署（`20bb72e`，服务器同版本，重启后自检 13/13）**
- **五处决策落地**：阈值机制（`server/selfeval/alerts.js`，代码 0 个默认阈值数字；`settingsSchema.metric_alert_lines` 缺省空＝不设线；R8 越线只产**待审提案**不阻断）、**提醒通道接口**（`server/reminders/{index,feishu}.js`；email/sms 只留位置；`RW_REMINDER_CHANNEL` 默认空＝没配通道；飞书实现复用既有渠道代码）、**提案式沉淀**（`knowledge-sink.js`：产出走既有 asks 卡片，**唯一写点＝有人答了这张卡**，源码不许出现 INSERT）。
- **M3 判据载体**：`server/selfeval/m3-gate.js`（周期数/落地提案数分来源/前后对比/无自我放行，三态，数据不足即 `undecided`）。
- **§6.2 四维机检**：`test/boundary-dimensions.test.mjs`（库名/账号/页面/对客定价；白名单用代码片段锚点、逐条带理由与 `until`；含"白名单漂移也要能红"）。
- **v0.3 文档五处加注**（C1 只报数不算硬目标 / 企微改"飞书+微信"并标待产品决策 / 提醒由产品层通道投递 / 连接器定义 / 规则4 阈值口径）——5 行替换、0 删除。

**M2 出口判据「被别的 agent 当 MCP 工具调用成功一次」——闭合（`docs/MCP-被第三方agent调用-实测.md`）**
| agent | 结果 | 证据 |
|---|---|---|
| **DSH（＝DeepSeek Harness）0.1.5-rc.2** | ✅ **成功** | 用户级 `~/.dsh/cordis.patch.yml` 加 `dsh-mcp-client` 一条；抓帧 `initialize`→`tools/list`→**`tools/call rw_status`** 返回 22 条真实消息；模型侧看到 `mcp__rw__rw_status` |
| **Codex CLI 0.153.4** | ✅ **成功** | `codex mcp add rw -- node …/rw-mcp-server.mjs`；`codex exec` 输出 `mcp: rw/rw_status (completed)` + 完整帧 |
| Pi（`earendil-works/pi`，MIT） | ❌ 未做 | **本体明确"不做 MCP"**（官网 What we didn't build 第一条），接上要写 Pi 扩展；装它＝改系统、跑一轮要 key（花钱） |
| Claude Code | ❌ 不接 | 本机未装且**闭源**（npm 只有分发包） |

- 两侧都用**本地假 LLM** 驱动（没花真模型的钱）：决定"调哪个工具"的是脚本，agent loop 是真的。
- **未验证**：平台真进程没起（用只读桩顶替，数据真从库 SELECT）⇒ 证明的是"MCP 协议面 + 我们 server 代码路径全通、返回真数据"，**不**证明真平台进程；`rw_chat`/`rw_export` 只验到出现在 `tools/list`（真调会烧模型）。
- 旁支：我们的 MCP server **需要账号凭证**（`RW_MCP_USER/PASS` 或 `~/.rw-keys.env`）；我们**没有离线只读工具**（三个都要求平台在线）；`~/.dsh/cordis.patch.yml:28` 有**明文飞书密钥**（原有条目，未动，分享配置时注意）。

**仍未集成（在飞，下一轮继续）**
1. **模型能力声明消费**：语义已正确（`deepseek` 的 `tool/vision` 现为 `null`，不再砍工具面），6 条夹具在按新规则重写。
2. **触发器三档**：1 条红（定时档"不被上一轮 handler 抛错冻住"）。
3. **C-48**：`schema-sync.test.mjs` 正在重写（中途有 `CHAIN_CREATED` 重复声明的语法错），未绿。
4. **G1 第二阶段**：调用点迁移进行中，并已分派修 `test/llm-retry.test.mjs` 的 SQL 形状锚点。
5. **连接器**：模块与夹具 12/12 绿、飞书真样例跑通；提交要等 `server/index.js` / `server/tools/index.js` / `server/tools/hooks.js` 三个共享文件的上游改动停手。

---

## 八、第 26 轮（补缺批次三已入库并部署 `87e26a0`）

**本轮落地（全量 870/868/0/2 绿；服务器同版本、重启后 selfcheck 13/13）**
- **§0.2 G1 出口**：`RW_STORAGE=jsonfile` + 不连 MySQL 的一次性跑通（登录 → 建会话 → 一轮 → 消息落库 → 读回历史 → 调一次只读工具 → **重启后再读回**）。
- **§4.7 客户端可见性**：溢出对客户端可见（只加帧字段）、进度帧、断线续订如实回**缺口标记**；并接上两处"模块已写好、没人挂"的：`metricAlerts` 进 `/api/cache-hit/summary`、`knowledge-sink` 的**提案式沉淀**挂到会话收尾/归档。
- **§0.2 G2 / §7.1 ④ 真缺陷修复**：`server/tools/registry.js` 的交叉核对**漏传当前生效的权限表** ⇒ 退回模块导入期常量快照 ⇒ **热重载只能减、不能加**（"装一个新工具"这条出口判据从未被真的验过）。改为传 `activePermissions`，补"热重载能加"的回归锁 + 对称复测"能减"。
- **§4.2 连接器热加载**（改声明不再需重启）＋ **§4.3 客户网关/内网推理注册入口**（providers 不再只能来自代码清单）。

**收口表台账**：第一～四节的"在做/待做"里，§4.1 G1、§4.2 连接器、§4.3 模型能力声明与注册入口、§4.5 触发器三档、§4.7 客户端可见性、§4.9 C-48、§6.2 四维、G2、M2 的 MCP 出口、五处决策 —— **均已落地**。

**仍未闭合（只剩"待条件"与两条新登记的观察项）**
1. **待条件三件**（不变）：Windows Server 真机装机自检；M3 的"两个周期"＋允许真跑一次提案落库；可选的外部对标自动抓取器。
2. **G1 的第二半**：账号/会话/消息/设置这条链已能走 JSON 存储；**审计/用量/工具调用账/事件账本等仍直连 MySQL**（详见本轮交付报告的"还剩哪些"一节）——"整台机器无 MySQL"还需把这些也搬完。
3. **§4.8"对账在平台"**：对账能力在位但只有 CLI/夹具触发，平台侧无承接面（属产品层拆分问题，未做）。
4. **§6.2 页面维的在案反例**：引擎树里仍有模型市场页的接口/逻辑/数据层（`/api/market/*`、`llm/market.js`、`market_snapshot`）与死表 `price_table`；因**本仓没有独立平台侧代码面**搬不动，已在门禁白名单里逐条登记并标 `until:`，门禁对"新增的市场面"照旧报红。

---

## 九、第 27 轮（`1202336`：并行声明化的消费者 · 事件契约补齐 · 工具账与检索第二实现已入库并部署）

**本轮入库两笔（本地＝GitHub＝服务器 `1202336f6`；重启后 15 段真机复核全绿）**

| 项 | 内容 | 判据与读数 |
|---|---|---|
| **§7.1 ⑤ 并行声明化（真缺陷）** | 清单逐条声明 `parallelSafe`，但引擎里**没有任何消费者** ⇒ 工具轮"每 maxPar 个切一块、无差别并发"，写入类工具会与兄弟调用同跑。新增 `server/toolbatch.js`（纯函数分批）＋ `registry.isParallelSafe`（判据单一出处）＋ `agent.runToolBatches`（生产循环唯一调用点）。语义照 DSH：`dsh-tools` 的 executionMode（**只有显式 true 才算可并行**，未声明/不认识=独占）＋ `dsh-agent-loop` 的 runGroup（独占＝屏障，提交恒按模型顺序） | `test/tool-parallel.test.mjs` 11 条：真代码路径记录"同时在跑几个"的峰值（read_file+write_file+grep_search ⇒ **峰值 1**；三条只读 ⇒ 峰值 3；上限仍是上界；让后面的先跑完、提交仍是 0,1,2）＋源码锚。**反向核对**：判据改成 `() => true` ⇒ 该用例当场红。真机：`rw-run '用 list_dir …'` 真跑一轮带工具调用（conv 812，`toolCalls:[{name:'list_dir',status:'done'}]`）⇒ 新调度路径在生产上跑通 |
| **G1 第二半 · 工具调用账** | 写口从直连 `INSERT INTO tool_calls` 迁到 `storage.toolCalls.append`；口径＝**先定既有对外形状**（真库 8583 行 `JSON_TYPE(args)` 全是 OBJECT ⇒ `/toolcalls` 的 args 是对象），故调用点保留截断/脱敏、把截断后的串还原成对象再交接口（`decodeArgsForLedger`）；顺序改为**先账本后审计**（原来 audit 先抛会把同一 try 里的 tool_calls 一起吞掉） | 干净机器（`RW_STORAGE=jsonfile`＋不连 MySQL）`/toolcalls` 不再是 `[]`、介质真有行、"账本缺行"日志消失；**真机**：真库 `tool_calls` 第 16006 行＝conv 812 / list_dir / message_id 2011（迁移没弄丢真库这条链）。`test/toolcalls-ledger-shape.test.mjs` 4 条含"二次编码真会把 args 变字符串"的反证 |
| **§4.3 记忆 · 检索第二个实现** | 新增 `server/kbsearch/like.js`（纯 JS 子串、零 SQL）并登记进实现表（`['fts','like']`，未知名字仍抛）；可见范围当**受限谓词**求值，看不懂就抛 `KB_LIKE_UNSUPPORTED_CONDITION`，**绝不"当没条件"放行**；契约新增**只读**实体 `knowledge:['all']` | `RW_KB_SEARCH=like`＋不连 MySQL：真返回结果、`mode/backend:'like'` 如实；**反向**：没显式选 like 时 fts 缺 MySQL **如实 fail**（不静默回落）。夹具 12+2 条 |
| **§4.7 事件契约成文（补齐＋两处过期更正）** | `docs/事件契约.md` 补 `agent_thinking`/`progress`（含 `roundCap=null`＝没有生效上限、`plan=null`＝没有计划）/`tool_done.tool.spill` 全字段/续订帧 `stream_hello`（`gap:true`⇒回落 /messages）与 `stream_end`；更正"`Last-Event-ID` 未实现"与"发 `stream_gap`"（**没有这个事件类型**）。`src/eventstream.js` 补认这三帧（此前 `agent_thinking` 会被记进 unknownTypes ⇒ 真流量过 verifyRebuild 报**假**的版本漂移） | `test/eventstream.test.mjs` 11/11（含"契约里列的每个 type 重建器都要认得"）；真机 11/11。注：重建器目前只有夹具/脚本在用，`src/` 无组件引用（GUI 走 /activity），`npm run build` 产物无变化 |
| **文档加注（只增不改）** | §4.2＋§7.1 ②：三层分离落地为**有意的两层**（不再按"部分"重判）；§4.9：回滚＝代码层＋有据可查（迁移只向前、无 downgrade）；§7.1 ⑤：四类声明**都已落地且都有消费者**；§7.1 ⑳：stdio 即达成，HTTP 挂载登记"可选不做" | 逐条可核（每处加注都点名落地位与夹具） |

**顺带修掉的两处工具链自身错误（不改产品语义）**
1. `tmp/verify-v03.sh` ⑩ 段此前调 `runProbe()`——那是**单候选**探针（`runProbe(candidate)`），不传候选就抛
   `reading 'configError' of undefined`，于是"沙箱探针"这一段只打出一行 ERR、**真正的读数一条都没拿到**（等于没验）。
   改走诊断入口 `diagnose()` 后拿到完整读数（见下）。
2. 真机复核新增 `tmp/verify-round27.sh`（六段，逐项原始读数）。

**真机读数（`1202336f6`，15 段全绿 + 本轮专项六段）**
- 自检/golden/rehearsal/指标门禁：`selfcheck` 通过 · 金标 9/9 · 演练 `{"ok":true,"head":"0008_indexes_of_baseline_tables","tolerated":0,"checksFailed":[]}` · 指标门禁"✅ 通过（在位性判定＋只报数不设线）"。
- 沙箱诊断（修正后）：`platform=linux`，`runner=null`，两个候选各自的原因（bwrap 解析不到 / unshare 退出码 3 `mount point is busy`），`available=false`、`sandboxRequired=false`；降级账 `audit_log` 有 `sandbox:degrade` 行（18338）。
- 严格语义：`RW_SANDBOX_REQUIRED=1` ⇒ 退出码 1 ＋ `{"ok":false,"strict":true,"error":"SANDBOX_UNAVAILABLE"}`。
- 真流量一轮（带工具）：conv 812，`totals.cost ¥0.0048`，`toolCalls` 1 条 done。
- `/api/health` → `{"ok":true,"service":"rw","version":"0.1.0"}`；`rw-test` active。
- 指标面如实报出 `failRate 5.54% → 7.04%`（**门禁只在位性判定**，不设线）。

**仍未闭合（本轮之后）**
1. **待条件三件**（不变）。
2. **G1 第二半的剩余实体**：`audit_log`（~66 处调用点，仍直连）／`usage_stats`（写读各若干）／`knowledge` 的**写**路径（`kb_add`/`kb_del`/导入与管理面）／`contract_events` 与 `events` 归档。现状：缺 MySQL 时审计与用量落不了账（工具调用账已不受影响），知识条目**攒不起来**（检索能跑）。
3. **§4.8"对账在平台"**、**§6.2 页面维反例**（同第八节，未动）。
4. **两处新登记的小项**：`GET /api/knowledge?q=` 只传 `db` 不传 `storage`（部署把 `RW_KB_SEARCH` 指到 `like` 时会如实抛"需要 opts.storage"，需 `server/index.js` 一行）；`src/` 无组件消费事件流重建器与 `/stream` 续订（GUI 走轮询）。

---

## 十、第 28 轮（`27af40f`：记忆写口走存储接口 · 夹具泄漏 C-63 已处置）

**本轮入库一笔（本地＝GitHub＝服务器 `27af40f32`；重启后 15 段真机复核全绿）**

| 项 | 内容 | 判据与读数 |
|---|---|---|
| **§4.3 记忆写口（§4.1「存储走接口」）** | `knowledge` 从只读扩到 `['all','append','update','remove','removeVisible','findByTitle']`；`kb_add`（同名查 / 覆盖 + `touch` / 新增）、`kb_del`（可见范围删除）、`selfeval` 的提案式沉淀写点（`dbc`→`store`）三条路全部改走接口。上一批只补了**读**（`like` 后端能从介质读），干净机器上"检索跑得起来、条目攒不下来" | **本地**：存储契约 56/56（两个实现同跑同一组用例：同名去重六个条件各自的反例、覆盖只留一条且 touch 刷新时间戳、身份字段不许修订、body 可空、可见范围删除逐条正反例）；**整服务端到端 4/4**（`RW_STORAGE=jsonfile` + `DB_PORT=1`、不连 MySQL：kb_add → 介质文件里真有那一行 → 换进程在**它自己的会话**里搜得到 → 同一关键词在别的会话搜不到 → kb_del 删掉且介质里也没了；反向：别人的账号 / 别的会话私有的条目都删不掉、介质里那两条都还在）。全量 **927 / 925 pass / 0 fail / 2 skipped** |
| **真机（服务器 `27af40f32`）** | ① **真 MySQL 上的写口语义**（保留账号 999999，跑完按 id 清干净）：append 得 id 142/143；`findByTitle` 命中（`<=>` 的 NULL 安全等在真库成立）、别的会话与"给 global 塞 shellId"两种落空；`update`+`touch` 后**只有一条**且时间戳真的前移；改 `accountId` 被拒（`STORAGE_INVALID_FIELD`）；`removeVisible` ＝ 别的会话 false / 本会话 true / 全局 true / 再删 false；`remove` 账号边界正确；② **复核**：该账号剩余 **0 行**（`knowledge` 总行数 11 未变）；③ 服务器上跑本轮两份夹具 56/56 与 4/4；④ 15 段全绿（自检、金标 9/9、演练 `tolerated:0`、指标门禁"✅ 通过"、严格沙箱退出码 1、headless 单轮 ok）；⑤ `/api/health` ok、`rw-test` active | 见上 |
| **夹具泄漏（C-63，已处置）** | `test/injection-trust.test.mjs` 只替换 `db.query`，而存储接口的写路径是 `exec` → **`db.run`** ⇒ 迁移后同一句 INSERT 换了出口，两次全量测试真的往共享库 `rw_test.knowledge` 写了 4 行 | **已按 id 点名删除并复核 0 行**（id 138–141；更早的 id 137 与本轮无关、未动、留痕）；夹具改为同时替换 query+run（断言口径一字未改）；教训（迁移要 grep"哪些夹具只挡了一半"）写进 `架构文档冲突登记` **C-63** |

**仍未闭合（本轮之后）**
1. **待条件三件**（不变）：Windows Server 真机装机自检；M3 的"两个周期"＋允许真跑一次提案落库；可选的外部对标自动抓取器。
2. **G1 第二半的剩余实体**（比上一轮又少两块：工具调用账、知识写口已迁）：
   - `audit_log`：**约 66 处调用点**（`server/index.js` 为主）＋ 归档路径，仍直连 MySQL ⇒ 缺 MySQL 时审计落不了账
     （现状：工具调用账与知识写口已不受影响，审计仍如实降级、只出声一次）。
   - `usage_stats`：写读各若干（`agent.js` 的逐轮/折叠记账、`index.js` 的仪表与遥测读法），仍直连 ⇒ 干净机器上成本计量是空的。
   - 知识**管理面**：`GET /api/knowledge`（展示列视图 + 带 `q` 时检索后的二次取列）、`PATCH` 状态治理、导入 —— 仍走 SQL；
     连带 `GET /api/knowledge?q=` 只把 `db` 传给检索层。**这两件事要一起做**：只补一行传参、展示列仍要 MySQL，等于没做。
   - `contract_events` 与 `events` 的归档路径（`events → events_archive`）：JSON 介质上"归档"语义待定
     （同一份文件里没有归档表、也没有体积压力）——要按"介质不支持就如实抛"还是"JSON 侧本就不需要归档、作业要能跳过"来定，**不猜**。
3. **§4.8"对账在平台"**、**§6.2 页面维反例**（同第八/九节，未动）。
4. **`src/` 无组件消费事件流重建器与 `/stream` 续订**（GUI 走轮询）：属前端接线，未做。

**下一批的推荐顺序（按"要求点名 → 影响面 → 可验证性"排）**
1. `audit_log` 走接口（§4.6「预算与审计：本地兜底」点名；写口形状统一，机械但量大；判据＝契约用例 + 干净机器落到介质 + 真库形状不变）。
2. `usage_stats` 走接口（同一条款；读法多，先迁写口再迁仪表读法，分两笔）。
3. 知识管理面（含 `?q=` 传 `storage`）与 `events` 归档口径 —— **后者要拍板**：JSON 介质上"归档"到底算什么（不是技术难点，是口径）。

---

## 十一、第 29 轮（`13f211f`：用量记账写口走存储接口 · 修掉「折叠花费从未入账」C-64）

**本轮入库一笔（本地＝GitHub＝服务器 `13f211fef`；重启后 15 段真机复核全绿）**

| 项 | 内容 | 判据与读数 |
|---|---|---|
| **§4.6 预算与审计 · 写口（§4.1「存储走接口」）** | 契约新增 `usage: ['append']`（**只加写口**；读法＝C1–C5 报表/仪表/遥测/导出仍走 SQL，如实登记）。17 个中性字段与 `usage_stats` 建表逐字对应；`createdAt` 是**介质时间戳**（mysql `DEFAULT NOW()` / JSON `nowIso()`），不由调用方给。`REQUIRED.usage = ['kind']`——`kind` 有默认值 `'request'`，可它是这条账**唯一的分类维度**（round/collapse/title/summary/warmup），C1–C5 全按它过滤。六处直连 INSERT 全部改走接口（`agent.js` 逐轮×2 + 折叠、`autotitle.js`、`epoch.js`、`index.js` 摘要、`tools/index.js` 归档），全仓 `INSERT INTO usage_stats` **0 命中** | **本地全量 929 / 927 pass / 0 fail / 2 skipped**（跑真库那几条夹具时临时开过 SSH 隧道，跑完即关）。存储契约 **58/58**（新增"五处记账形状各记一条"：逐轮字段最全 / **折叠那条特意不给 `durationMs`**（正是 C-64 的形状）/ 预热账号会话全空；`kind` 缺了当场拒写、拼错字段名不静默吞）；干净机器整链 **7/7**（新增正面判据：这一轮真花钱 ⇒ 介质里必须有 `kind='round'` 且 tokens>0 的用量行、必须经过 `usage.append`、每行都有介质时间戳） |
| **C-64（真缺陷，随迁移一起修）** | `maybeCollapseEarly` 那条折叠记账 **列了 `duration_ms` 却没给它值**（12 个占位符 / 11 个实参）⇒ mysql2 当场抛、被 `catch { 折叠计量失败不影响折叠 }` 吞掉 ⇒ **每次折叠的真实花费静默没入账**（与该处注释"计入账本（kind=collapse）"和 §4.4.1 规则5「失效可数」相反） | 取证＝只读数占位符/实参（`tmp/count-usage-args.mjs`）：折叠那处 **12/11 ❌**，另外五处逐条一致 ✅。改具名字段后**结构上不可能再错位**。登记进 `架构文档冲突登记` **C-64** |
| **真机（服务器 `13f211fef`）** | ① 真 MySQL 上的写口语义：三次记账（逐轮 / **折叠不给 durationMs** / 预热账号会话全空）→ 按 id 回读，列映射逐项正确、`created_at` 由库盖、折叠那行 `duration_ms=0`（走默认值而非错位）、缺 `kind` 在真库上同样被拒；跑完按 id 删除、保留号剩余 **0 行**；② 服务器上跑本轮两份夹具 **58/58 + 7/7**；③ 真机跑一轮带工具调用的 headless（conv 816）：`usage_stats` 新增 6102/6103 两行（kind=round）、`tool_calls` 新增 16129（message_id 2019）——迁移后的写口在真库上真的在写；④ 15 段全绿（金标 9/9、演练 `tolerated:0`、指标门禁通过、严格沙箱退出码 1）；⑤ `/api/health` ok、`rw-test` active | `tmp/verify-round29.sh` |

**仍未闭合（本轮之后）**
1. **待条件三件**（不变）：Windows Server 真机装机自检；M3 的两个周期＋允许真跑一次提案落库；可选的外部对标自动抓取器。
2. **G1 第二半剩下的实体**：`audit_log`（约 66 处调用点 + 归档路径，**最大的一块**）；`usage_stats` 的**读法**（C1–C5 报表/仪表/遥测/导出）；知识**管理面**（展示列 + 状态治理 + 导入，含 `GET /api/knowledge?q=` 传 `storage`）；`events → events_archive` 的 JSON 口径（**待拍板**）。
3. **§4.8"对账在平台"**、**§6.2 页面维反例**：属平台侧拆分，本仓无代码面（已登记，不动）。
4. **前端接线**：`src/` 仍无组件消费事件流重建器与 `/stream` 续订（GUI 走轮询）。
5. **MCP 看门狗**只盯 `settings.mcp_servers`：`kind=mcp` 的连接器意外退出不会被自动重连（已登记的小项）。

---

## 十二、第 30 轮（`dd339f6`：审计账走存储接口 · 62 处写口一次迁完 · 两起夹具泄漏已处置）

**本轮入库两笔（本地＝GitHub＝服务器 `dd339f66d`）**

| 项 | 内容 | 判据与读数 |
|---|---|---|
| **§4.6 审计账走接口（§4.1）** | 契约新增 `audit: ['append','lastDetail']`。**62 处**直连 `INSERT INTO audit_log` 全部改走接口（写口形状全仓只有两种：三列/五列）——`agent.js` 4、`credentials.js` 1、`epoch.js` 3、`index.js` 44、`prefix-assemble.js` 2、`sandbox/degrade.js` 1、`subagent.js` 1、`tools/hooks.js` 2、`tools/index.js` 3。迁移用脚本做（每条都核对列清单与占位符个数，形状不认识的**不动并报出来**），改完全仓只剩 1 处 `audit_log` 字样＝归档作业那条 `INSERT INTO audit_log_archive … SELECT … FROM audit_log`（读法，未迁）。跨轮前缀账的**对照读**（`lastDetail`）也迁了——它在每轮都在跑的路径上 | **本地全量 935 / 933 pass / 0 fail / 2 skipped**（跑真库夹具时临时开隧道，跑完即关）。存储契约 **60/60**（＋1 条审计用例：三列/五列两种形状、`accountId=null` 的系统级动作、缺 `action` 当场拒、拼错字段名不静默吞）；干净机器整链 **7/7**（新增正面判据：介质里必须有组装侧前缀账那一行、必须经过 `audit.append`、每行都有介质时间戳）。**真机**：一轮 headless（conv 820）后 `audit_log` 新增 `prefix:exempt`/`prefix:assemble`/`tool:list_dir` 三行、形状与字段一字未变；`prefix:assemble` 全表 110 行；五份相关夹具在服务器上 60/7/17/14/11 全绿；15 段真机验证全绿；`/api/health` ok |
| **夹具泄漏两起（C-63/C-65，已处置）** | ① C-63（上一批）：`injection-trust` 只挡了 `db.query`，而存储写路径是 `db.run` ⇒ 真库多了 4 行知识；② **C-65（本批新增）**：`credentials-rotate` 只注入假 `db`，写口却是模块级 `storage` ⇒ 真库多了 **12 行** `cred:rotate` | 两起都**按 id 点名删除并复核 0 行**（`tmp/clean-kb-artifacts.mjs`、`tmp/clean-cred-audit.mjs`）。修法：给 `rotateSecret`、CLI `main`、`auditDegrade` 补 `store` 注入缝；夹具改成假存储（`FakeStore`）；同批补上 `headless`/`cross-end`/`channel-turn` 三处同类注入缝。教训合并成一条纪律：**迁移写口时必须给所有"注入假库以隔离真库"的地方补 store 缝** |
| **真机抓到的漏网（已补 + 已补夹具）** | 迁写口时 `server/sandbox/degrade.js` 与 `server/subagent.js` **忘了 import storage** ⇒ 服务器启动时日志报 `[sandbox] 降级账本缺行（降级已发生）：storage is not defined`。**本地全量门禁是绿的**——这条路径此前一份夹具都没有 | ① 补 import；② 新增 `test/sandbox-degrade-ledger.test.mjs`（4 条：留痕形状与"为什么降级"、无 reason 时退回第 2 层说明、留痕失败必须出声且不改判主流程、源码级"写口走接口且必须 import"反向锁）；③ 另写 `tmp/check-storage-imports.mjs` 全仓扫"用了 storage.* 却没 import/注入缝"；④ 修后重启：**近 1 分钟"降级账本缺行"计数 = 0**，`sandbox:degrade` 新行照常落（id 18756），`/api/health` ok |

**仍未闭合（本轮之后）**
1. **待条件三件**（不变）：Windows Server 真机、M3 两个周期＋允许真跑一次提案落库、可选的外部对标抓取器。
2. **存储接口剩下的读法/面**：审计**读法**（`/api/audit` 列表、90 天归档作业、前缀账统计、`/trace`）；`usage_stats` 读法（C1–C5 报表/仪表/遥测/导出）；知识**管理面**（展示列＋状态治理＋导入，含 `?q=` 传 `storage`）；`events → events_archive` 的 JSON 口径（**待拍板**）。
3. **§4.8"对账在平台"**、**§6.2 页面维反例**（平台侧拆分，本仓无代码面）。
4. **前端接线**：`src/` 仍无组件消费事件流重建器与 `/stream` 续订。
5. **MCP 看门狗**未覆盖 `kind=mcp` 连接器。

---

## 十三、第 31 轮（`a6867bb`：MCP 看门狗覆盖连接器 · 修掉重连竞态 C-66）

**本轮入库一笔（本地＝GitHub＝服务器 `a6867bb02`）**

| 项 | 内容 | 判据与读数 |
|---|---|---|
| **§0.2 G2 的另一半：挂了要能自己回来** | 子进程意外退出时 `server/mcp.js` 的 `proc.on('exit')` 会把客户端从池里删掉（对的），但**此前没有任何东西把它拉回来** ⇒ 会话静默缺 `mcp_*` 工具直到手动 reload。旧看门狗只读 `settings.mcp_servers` ⇒ **`kind=mcp` 的连接器挂了不会被拉回来**。本轮把这套判据搬进 `server/connectors.js`：新增纯函数 `declaredMcpIds`（两份声明的并集：`mcp_servers` ∪ `connectors(kind=mcp)`，声明非法时只认前者）＋ `reconnectMissingDeclared(dbc)`（"声明了但不在池里"→ 走既有唯一实现重连 + 同步工具面；**不在声明里的源一个都不撤**，撤源是 reload 的职责）。`server/index.js` 的定时看门狗变成薄壳（只做定时与日志） | 夹具 `test/connectors-watchdog.test.mjs`（4 条）：① 纯函数穷举（并集/去重/http 不算/非法声明/脏输入）；② **真 spawn 假 MCP server**：连接器声明的源从池里消失 ⇒ 一拍内被拉回来、工具面恢复；③ 负例：没有缺失时**零副作用**（已连接的客户端不许被断开重连、工具面一模一样）；④ 源码级锁（看门狗那段必须调它、旧判据不得复活、两份声明都要读）。本地全量 **939 / 937 pass / 0 fail / 2 skipped** |
| **C-66（夹具抓到的真缺陷，已修）** | `proc.on('exit')` **无条件** `clients.delete(id)`：断开旧进程后立刻重连时，旧 exit 事件（异步）可能在新客户端装进池子**之后**才到 ⇒ 把刚连上的摘掉（症状：看门狗报"重连成功、工具 2 个"，而池子是空的、`registeredTools: 0`）——正是这条要消灭的"静默缺工具" | 修法：判据改成"当前这一格还是不是我"（`clients.get(id) === cl` 才删）。登记进 `架构文档冲突登记` **C-66** |
| **真机（服务器 `a6867bb02`）** | 用仓库自带假 MCP server 声明成一个**连接器**（`kind=mcp`）→ 热加载后进池（2 个工具，`registeredTools 28`）→ **杀掉它的子进程**（15:37:39 `[mcp:wd_fake] 退出 code=null`）→ 60 秒后日志：**`[mcp] 看门狗重连 wd_fake → 实际回来 ["wd_fake"] 注册工具 28`**，且新子进程已在跑 → 恢复原声明并热加载，无残留进程 | `tmp/verify-round31-watchdog.sh`（只动 `settings.connectors` 一行，跑完恢复原值；不调模型、不碰业务数据） |

**仍未闭合（本轮之后）**
1. **待条件三件**（不变）：Windows Server 真机、M3 两个周期＋允许真跑一次提案落库、可选的外部对标抓取器。
2. **存储接口剩下的读法/面**：审计**读法**（`/api/audit` 列表、90 天归档作业、前缀账统计、`/trace`）；`usage_stats` **读法**（C1–C5 报表/仪表/遥测/导出）；**知识管理面已收口（见第十四节）**；`events → events_archive` 的 JSON 口径（**待拍板**）。
3. **§4.8"对账在平台"**、**§6.2 页面维反例**（平台侧拆分，本仓无代码面）。
4. **前端接线**：`src/` 仍无组件消费事件流重建器与 `/stream` 续订。

---

## 十四、第 32 轮（`c760189`：知识管理面 + 每轮注入走存储接口）

**本轮入库一笔（本地＝GitHub＝服务器 `c76018933`）**

| 项 | 内容 | 判据与读数 |
|---|---|---|
| **§4.3 记忆面收口（§4.1）** | `knowledge` 再补三个动词：`adminList`（管理视图展示列）、`updateOwned`（账号边界内修订）、`visibleList`（每轮注入的可见范围读法）。`FIELDS.knowledge` 补 `relatedComponent`；`PATCHABLE` 新增 `knowledgeOwned`。四条路由（列表/`PATCH`/`DELETE`/导入）＋ `/api/chat` 的每轮知识注入全部改走接口——**上一轮登记的"`?q=` 只传 db 不传 storage"那条小项一并收口**（干净机器上现在"检索、写入、删除、注入、管理页"五条路都通） | **本地全量 941 / 939 pass / 0 fail / 2 skipped**；存储契约 **62/62**（＋1 条管理面用例：id DESC 且含 superseded、账号边界、`shell_key` 如实 null、`body_preview` 截 200、过滤同名同义、按 id 取、空 id 列表≠无条件、可见范围四列形状、`updateOwned` 边界与白名单）。假 pool 补一段**按整段形状认**的管理面 SELECT（LEFT JOIN + `LEFT(...) AS body_preview` + `k.id IN`）——列名是**前端在读的对外形状**，不许拆成通用条件去糊 |
| **一处口径澄清（写进注释与夹具）** | 管理视图是**账号内的治理视图**，与"会话可见范围"（`kbVisibleWhere`）**不是一回事**：`q` 那条路刻意**不套**可见范围判据（套上会当场少掉本账号的 conv 私有条目）——过滤条件与改造前逐字相同 | `test/kbsearch.test.mjs` 的管理面源码守卫跟着搬：路由层判"两条路分开 / 顺序 id DESC / 过滤条件 / 走检索层"，"展示列形状"三条改打在实现上（SQL 现在在那儿） |
| **真机（服务器 `c76018933`）** | ① `GET /api/knowledge` 返回**同形**展示列（真数据：id 74 那条 Excel 记忆，`shell_key`/`body_preview` 都在）；② 过滤分支（`scope=global&kind=fact`、`q=部署`）正常；③ `PATCH` 走 `updateOwned`：置 `superseded`+`relatedComponent` → 真库读到、改回 `active`/`null` → 真库读到；**越权 PATCH 别人的 id=1 → HTTP 404**；④ 真机跑一轮"知识库里有没有部署的记录"：模型真调 `kb_search` + `db_query`，两边结果一致（记忆面在真机上活着）；⑤ 服务器夹具 62/62、21/21、8/8、7/7；⑥ `/api/health` ok、服务 active、无 error 行 | `tmp/verify-round32-kbadmin.sh` |

**仍未闭合（本轮之后）**
1. **待条件三件**（不变）。
2. **存储接口剩下的读法**：审计**读法**的其余三处（`/api/audit` 列表 + 90 天归档作业、`/trace` 的按会话回溯）；`usage_stats` **读法**（C1–C5 的逐轮/按天读数、报表、会话导出）；`events → events_archive` 的 JSON 口径（**待拍板**）。
3. **§4.8"对账在平台"**、**§6.2 页面维反例**（平台侧拆分，本仓无代码面）。
4. **前端接线**：`src/` 仍无组件消费事件流重建器与 `/stream` 续订。

---

## 十五、第 33 轮（`aaaaa6c`：审计读法（C4/C5 仪表）走存储接口）

**本轮入库一笔（本地＝GitHub＝服务器 `aaaaa6cff`）**

| 项 | 内容 | 判据与读数 |
|---|---|---|
| **§4.6 审计读法（§4.8 的 C4/C5 仪表）** | `audit` 再补三个读法：`countByAction`（各动作条数）、`lastByAction`（某动作最后一行＝"最近一次失效"）、`countByFirstToken`（豁免原因分布；MySQL 侧 `SUBSTRING_INDEX(detail,' ',1)`，JSON 侧用同一判据的 JS 实现）。`/api/agent/capabilities` 的 C4/C5 段改走接口——**此前这三条读法是直连 SQL，干净机器上 C4/C5 只能出 0**（而仪表正是"失效可数／省钱"那条主线的门面） | **本地全量 943 / 941 pass / 0 fail / 2 skipped**；存储契约 **64/64**（＋1 条审计读法用例，两个实现同跑；假 pool 补一条**按整段形状认**的 `SUBSTRING_INDEX … GROUP BY r` 特例）。`test/metric-alerts-wiring.test.mjs` 的**口径升级**：它跑在 `RW_STORAGE=jsonfile`（干净机器形态）上，原来 C4/C5 的数是假 db 给的罐头行，现在改成**把 3 行 invalidate + 12 行 exempt 铺进 JSON 介质再读回来** —— 即"仪表从**本地账本**出数"，这正是 §4.6「本地兜底」要的性质 |
| **真机（服务器 `aaaaa6cff`）** | `GET /api/cache-hit/summary` 的 C4/C5 与**同一时刻真库直查逐项一致**：C4 `count=26`、`lastAt=2026-09-16T07:30:06`、`lastDetail="fp=c140e75d261c … src=channel"`（真库 `ORDER BY id DESC LIMIT 1` 那条逐字相同）；C5 `exempt=220`、`byReason={"first-round":209,"tool-face-changed":10,"idle:2737min":1}`（真库 `GROUP BY reason` 逐项相同）；服务器夹具 64/64、4/4、9/9；`/api/health` ok、服务 active | `tmp/verify-round33-auditread.sh` |

**仍未闭合（本轮之后）**
1. **待条件三件**（不变）：Windows Server 真机、M3 两个周期＋允许真跑一次提案落库、可选的外部对标抓取器。
2. **存储接口剩下的读法**：审计读法的其余三处（`/api/audit` 列表 + 90 天归档作业、`/trace` 的按会话回溯）；`usage_stats` 读法（C1–C5 的逐轮/按天读数、报表、会话导出）；`events → events_archive` 的 JSON 口径（**待拍板**）。
3. **§4.8"对账在平台"**、**§6.2 页面维反例**（平台侧拆分，本仓无代码面）。
4. **前端接线**：`src/` 仍无组件消费事件流重建器与 `/stream` 续订。

---

## 十六、第 34 轮：本会话累计确认 ＋ 剩余项交接（`af7dfc6`）

**整体确认（真机整套过了一遍，`tmp/verify-round34-cumulative.sh`）**：15 段全绿（`✅ 全部项目已跑完`）——
自检 13/13（含 write 档会话真跑完一轮 + SSE 真流式）、金标 9/9、升级演练 `tolerated:0`、账本离线导出
**19689 行**、指标门禁在位、严格沙箱退出码 1、日志无 error；本地＝GitHub＝服务器 `af7dfc6`。

**存储接口现状**（`node -e` 读契约，可复跑）：**12 个实体 / 60 个方法**
```
conversations create,get,update,findOwned,listByAccount,updateOwned,touch,exists,remove
messages      append,list,recent,count,history,guardAppend,countByTool,removeByConversation
toolCalls     append,list,recent,attachToMessage,removeByConversation
settings      get,set,all,getMany          agentRuns create,getLatest,update
events        append,read                  usage     append
audit         append,lastDetail,lastByAction,countByAction,countByFirstToken
accounts      findByUsername,create        sessions  create,findValid,remove
knowledge     all,append,update,updateOwned,remove,removeVisible,findByTitle,adminList,visibleList
deliveries    insert,findByKey,claimRetry,finish,list
```

**本会话（第 25–34 轮）的净增量**：并行声明化的消费者（§7.1 ⑤，真缺陷）· 工具调用账写口 · 检索第二个实现 ·
事件契约补齐与两处过期更正 · 用量记账写口（并修掉 C-64 折叠花费漏账）· 审计账 62 处写口 + 跨轮前缀账对照读 ·
MCP 看门狗覆盖连接器（并修掉 C-66 重连竞态）· 知识写口/管理面/每轮注入 · 审计读法（C4/C5 仪表）。
两起夹具泄漏（C-63 / C-65）按 id 清理并登记；真机日志抓到的 `storage is not defined` 漏网已补 import + 补夹具。

**剩余项与建议的接口形状（下一会话照此接着做，不必重新推导）**
1. **审计读法的其余三处**：
   - `/api/audit` 列表 + 归档作业 → `audit.adminList({accountId?, action?, since?, until?, conversationId?, archived, limit, offset})`
     与 `audit.archiveBatch({before, limit})`（**后者与第 4 项同一个待拍板口径**）。
   - `/trace` 按会话回溯 → `audit.traceByConversation({conversationId, limit})`（保留既有那条 `conversation_id=? OR detail LIKE '%conv=<id>%'` 口径）
     ＋ `toolCalls.traceByConversation({conversationId, limit})`（展示列形状）＋ `usage.summaryByConversation(conversationId)`（`{calls,cost,tokensIn,tokensOut}`）。
   - 判据：契约用例（两个实现同跑）＋ 在真机上与真库直查**逐项对账**（照第 33 轮那套做法）。
2. **`usage_stats` 读法**：C1–C5 的逐轮/按天读数（`usage.rowsByConversation` / `usage.dailyByAccount`）、
   报表与遥测（`scripts/metrics-report.mjs`、`selfeval/collect.js` 那 8 条查询逐条对到方法上）、会话导出。
   建议**先迁 selfeval 采集**那条链（M3 判据靠它），再迁仪表。
3. **前端接线**：`src/` 消费 `src/eventstream.js` 的重建视图（与 `/activity` 轮询结果对照）与 `/stream` 续订
   （断线时读 `stream_hello.gap` 决定是否回落 `/messages`）；判据＝`npm run build` ＋ 一个能证明"真的用到重建器"的
   纯函数夹具（GUI 没有 DOM 测试面，这一点要在交付说明里写清）。
4. **待拍板（唯一一件等用户）**：`events → events_archive` 在 JSON 介质上的口径 —— (a) 不支持就如实抛，
   (b) 识别到"无归档能力的介质"就跳过并留痕。**建议 (b)**（同一份文件里没有归档表、也没有体积压力）；
   定了之后 `archiveBatch` 与归档作业一起做。
5. **待条件三件**（Windows Server 真机；M3 两个周期＋允许真跑一次提案落库；可选外部对标抓取器）。

---

## 十七、第 35 轮（`a6cc383`：`/trace` 按会话回溯走存储接口）

**本轮入库一笔（本地＝GitHub＝服务器 `a6cc38313`）**

| 项 | 内容 | 判据与读数 |
|---|---|---|
| **§4.8 可观测 · `/trace` 三条读法** | `audit.traceByConversation`（保留既有那条"挂在本会话上的 **＋** detail 里带 `conv=<id>` 的"口径）、`toolCalls.traceByConversation`（展示列形状 `tool_name`/`duration_ms`/`created_at`）、`usage.summaryByConversation`（`{calls,cost,tokensIn,tokensOut}`）。`GET /api/conversations/:id/trace` 三条读法全部改走接口——**迁移前它们直连 SQL ⇒ 干净机器上这一页整页失败**；返回的列名与改造前逐字一致（前端在读） | **本地全量 943 / 941 pass / 0 fail / 2 skipped**；存储契约 **64/64**（同一条用例里补了三条读法的判据：OR-LIKE 回溯口径与列名、工具账展示列、用量合计把"没挂会话的预热行"排除在外；假 pool 补两段**按整段形状认**的特例） |
| **真机（服务器 `a6cc38313`）** | `GET /api/conversations/824/trace` → `ok:true`，返回真实审计行（`tool:db_query` 的 args/result 原文）、会话元信息与用量段；**同一时刻真库直查**该会话：审计 4 行 / 工具账 2 行 / 用量 `n=2, cost=0.0128, tin=15494, tout=903` —— 与页面读数一致。服务器夹具 64/64 通过，`/api/health` ok、服务 active | `tmp/verify-round35-trace.sh`、`tmp/verify-round35b-trace.sh` |

**仍未闭合（本轮之后）**
1. **待条件三件**（不变）。
2. **存储接口剩下的读法**：审计 `/api/audit` 列表 + 90 天归档作业（`audit.adminList` / `audit.archiveBatch`，**后者与第 4 项同一个待拍板口径**）；`usage_stats` 读法（C1–C5 的逐轮/按天读数、报表、会话导出）。
3. **§4.8"对账在平台"**、**§6.2 页面维反例**（平台侧拆分，本仓无代码面）。
4. **前端接线**：`src/` 仍无组件消费事件流重建器与 `/stream` 续订。
5. **待拍板**：`events → events_archive` 在 JSON 介质上的口径（建议 (b) 跳过并留痕）。

---

## 十八、第 36 轮（`4d29561`：C3 仪表走存储接口）

**本轮入库一笔（本地＝GitHub＝服务器 `4d295616c`）**

| 项 | 内容 | 判据与读数 |
|---|---|---|
| **§4.8 C3 仪表（账号成本三件套）** | 新增 `usage.summaryByAccount(accountId)`（`{total, runs, convs}`，去重按 `agent_run_id`/`conversation_id`），`/api/cache-hit/summary` 的 C3 段改走接口——**至此这一页的 C1–C5 全部从本地账本出数**（C1/C2 走逐轮读数、C3 走账号合计、C4/C5 走审计读法） | **本地全量 943 / 941 pass / 0 fail / 2 skipped**；存储契约 **64/64**（用量用例补 C3 判据：两条记账同一个 `agentRunId` ⇒ 只算 1 次执行；空账号＝全 0）。`test/metric-alerts-wiring.test.mjs` 的介质播种也跟着补上 usage 行（40 次执行 / 10 个会话 / 总花费 100）——那一页的三类读数**全部**来自 JSON 介质了 |
| **真机（服务器 `4d295616c`）** | 端点 C3 = `{"perRun":1.7944,"perConv":2.1563,"total":256.6,"runs":143,"convs":119}` vs **真库直查** `total=256.5968 / runs=143 / convs=119` —— 三个数逐项一致（`total` 的两位小数是改造前就有的展示口径）；C4 `count=26`、`lastAt=2026-09-16T07:30:06`、C5 `byReason={"first-round":212,"tool-face-changed":10,"idle:2737min":1}` 同样与真库一致；服务器夹具 64/64、4/4；`/api/health` ok、服务 active | `tmp/verify-round36e-c3.sh` |

---

## 十九、第 37 轮：当前提交的整体复验（`ef69827`，无代码变更）

**本轮只做一件事：把"现在这个提交"整体验一遍**（连续几笔迁移之后，先确认没有累积性回归，再谈下一批）。

| 面 | 读数 |
|---|---|
| 本地全量 | **943 / 941 pass / 0 fail / 2 skipped** |
| 真机 15 段 | `✅ 全部项目已跑完`：自检 13/13、金标 **9/9**、升级演练 `tolerated:0`、账本离线导出 **19867 行**、指标门禁在位、严格沙箱退出码 1、日志无 error |
| 存储接口方法面 | **12 个实体 / 64 个方法**（较第 34 轮的 60 个又 +4：`toolCalls.traceByConversation`、`usage.summaryByConversation`、`usage.summaryByAccount`、`audit.traceByConversation`） |
| 三方一致 | 本地＝GitHub＝服务器 `ef69827` |

**下一批（仍是那三件，按建议顺序）**
1. 审计 `/api/audit` 列表（`audit.adminList`：q/分类/天数/会话/壳/limit 六种过滤 + 两表合并的 `archived=0|1|all`）——
   **归档那半与第 5 项同一个待拍板口径**；可以先落 `archived=0` 的活表分支，拍板后再补归档分支。
2. `usage_stats` 读法：C1/C2 的逐轮读数（含 `GROUP BY DATE(created_at)` 的按天线）、报表（`scripts/metrics-report.mjs`）、会话导出。
3. `src/` 前端接线：事件流重建器 + `/stream` 续订（断线读 `stream_hello.gap` 决定是否回落 `/messages`）。

---

## 二十、第 38 轮（`9e2f5d7`：审计管理视图 `/api/audit` 活表分支走存储接口）

**本轮入库一笔（本地＝GitHub＝服务器 `9e2f5d7fc`）**

| 项 | 内容 | 判据与读数 |
|---|---|---|
| **§4.8 审计管理视图（活表分支）** | 新增 `audit.adminList({conds, params, limit})`：条件串由调用方按**既有口径**拼好（`1=1` 起头 + q 的 `(action LIKE ? OR detail LIKE ?)` / `created_at > NOW() - INTERVAL ? DAY` / 分类的 `action IN (…)` / `conversation_id=?` / `shell_id=?`），列名与改造前那条 SQL 逐字一致；脱敏仍留在路由出口（展示层口径不属介质）。`GET /api/audit` 的 `archived=0` 分支改走接口——**干净机器上审计页从此能打开**（默认视图就是它） | **本地全量 943 / 941 pass / 0 fail / 2 skipped**；存储契约 **64/64**（补活表分支判据：按会话过滤、LIKE 条件、列名逐字、limit 生效；JSON 侧**看不懂的条件如实抛** `STORAGE_UNSUPPORTED`，绝不"当没条件"把整表放出去；假 pool 补一段按整段形状认的特例）。**真机**：`/api/audit?limit=3` 与真库直查同 3 行逐项一致（`sandbox:degrade`，id 19113/19112/19111、时间与会话/壳列相同），列名 `id,account_id,action,detail,conversation_id,shell_id,created_at,archived` 不变，`categories` 仍 8 个；服务器夹具 64/64；健康检查 ok、服务 active |
| **仍未迁（如实登记）** | `archived=1|all` 两条**归档分支仍是 SQL**（`audit_log_archive` 只在 MySQL 介质里有；JSON 侧"归档"口径**待拍板**，见上一节第 5 项）。代码里就地写明"不假装支持"，等口径定了与 90 天归档作业一起做（`audit.archiveBatch`） | 同上 |

**仍未闭合（本轮之后）**
1. **待条件三件**（不变）。
2. **存储接口剩下的读法**：审计归档作业（`archived=1|all` + `archiveBatch`，**待拍板**）；`usage_stats` 读法里 C1/C2 的逐轮/按天读数与报表、会话导出（**已按第二十一节的裁定 A 定好切法**）。
3. **§4.8"对账在平台"**、**§6.2 页面维反例**（平台侧拆分，本仓无代码面）。
4. **前端接线**：`src/` 仍无组件消费事件流重建器与 `/stream` 续订（**已按第二十一节的裁定 B 定好落点**）。
5. **待拍板**：`events → events_archive` 在 JSON 介质上的口径（建议 (b) 跳过并留痕）。

---

## 二十一、第 39 轮：剩余两项的**决策记录**（架构裁定，代码未动；`f8c95d1`）

**为什么这轮只落决策不写码**：剩下两件各卡在一个前置上——① 归档口径要你拍板（不猜）；
② `usage_stats` 的 C1/C2 逐轮读数**不是"换个入口"那么直**：它带的 cohort 归属判据（`cohort.js` 的 `REAL_WHERE`）
是**跨表子查询**（`conversation_id IN (SELECT id FROM conversations WHERE title REGEXP …)` ＋ 对 `audit_log` 再查一次），
JSON 介质里的受限条件解析器**表达不了**它。先把这个裁定了，下一批才不会写成"两套 cohort 口径"。

**裁定 A（cohort 归属跨介质）**：把"归属"从**谓词**改成**两次读法** ——
1. 先用**已有的**接口读法解析出"本窗口内属于真实流量"的会话 id 集合（`storage.conversations` + `storage.audit` 各读一次；
   判据仍是 `cohort.js` 那一份，**不下沉到介质里**——它是领域规则，落进介质就会变成"两个实现各写一遍"）；
2. 再调新方法 `usage.roundRowsByAccount({ accountId, days, conversationIds })` 按 **id 列表**取逐轮读数
   （两实现都只是 `WHERE account_id=? AND kind='round' AND created_at >= … AND conversation_id IN (…)`）。
   为什么这样切：`IN (…)` 两个介质都能表达（MySQL 走索引、JSON 走 Set），而子查询只有 MySQL 能表达。
3. 判据：契约用例（两实现同跑）＋ 与改造前**同一窗口的真实读数逐项对账**（C1 中位/P90、C2 中位/P90；
   `scripts/metrics-report.mjs` 已有同一套数，可直接比）。

**裁定 B（前端接线落点）**：`src/` 侧只接两件事，都不新造状态 ——
1. **重建器**：把 `/activity` 轮询回来的事件喂给已有的 `src/eventstream.js`（纯函数），用它的视图渲染"工具/计划/进度/成本"，
   而不是在组件里各写一遍归并逻辑（那正是事件契约要防的"两套解析"）；
2. **续订**：断线时改调 `GET /api/conversations/:id/stream`（带 `Last-Event-ID`），读到 `stream_hello.gap === true`
   才回落 `/messages` 拉全量，并把"这段实时轨迹已丢"如实显示在界面上（不假装接上了）。
判据：`npm run build` 通过 ＋ 一个纯函数夹具（把一段合成事件流喂给重建器，断言视图里出现工具/计划/进度）；
GUI 没有 DOM 测试面这一点会在交付说明里写明。

**仍未闭合**：① 归档口径（**等你一句话**）；② 按裁定 A 迁 C1/C2 逐轮读数与报表、会话导出；③ 按裁定 B 接前端；
④ 待条件三件；⑤ §4.8"对账在平台"／§6.2 页面维反例（平台侧拆分）。

---

## 二十二、第 40 轮：执行裁定 A 的**前置盘点**（代码未动；`736e470`）

**这轮去动手时发现裁定 A 还差两块前置**（如实记下来，免得下一批又把它们当成"顺手"）：
`cohort.js` 的三条判据（`PROBE_WHERE` / `ORPHAN_WHERE` / `REAL_WHERE`）里有**两类子查询**，要在 JSON 介质里
用 JS 复算"真实流量的会话 id 集合"，光靠现有接口读法不够：
1. `conversation_id IN (SELECT id FROM conversations WHERE title REGEXP '<PROBE_TITLE_RE>')`
   ⇒ 需要一次**按账号列出会话（含 title）**的读法 —— `conversations.listByAccount` 有，但**不含 title**（它只回展示列子集），
   要么扩它的字段集、要么新增 `conversations.listIdsTitles({accountId})`；
2. `conversation_id IN (SELECT conversation_id FROM audit_log WHERE action LIKE 'prefix:%' AND conversation_id NOT IN (SELECT id FROM conversations))`
   ⇒ 需要一次**按动作前缀取会话 id**的审计读法（现有 `audit.*` 都没有这个形状）。

⇒ **裁定 A 的完整批次 = 2 个新读法 + `cohort.js` 的 JS 复算（判据仍只有一份，SQL 与 JS 由同一组常量生成）
+ `usage.roundRowsByAccount({accountId, days, conversationIds})` + 路由改两次读法 + 契约用例 + 真机对账**。
这已不是"顺手改一处"，是标准的一批；本轮把清单钉在这里，避免下一批边做边发现。

**本轮另外做的一件小事**：去掉第 38 轮末尾**重复的"仍未闭合"块**（同一节里两份清单会互相矛盾），
并在两处标注"已按第二十一节的裁定定了切法"。

**仍未闭合**：① 归档口径（**等你一句话**）；② 按上面这份清单执行裁定 A（C1/C2 逐轮读数与报表、会话导出）；
③ 按裁定 B 接前端；④ 待条件三件；⑤ §4.8"对账在平台"／§6.2 页面维反例。

---

## 二十三、第 41 轮：交付态复验（`dc52e58`，代码未动）

**部署态健康**：服务器 `dc52e58`，`rw-test` active，`/api/health` ok；15 段复跑 `✅ 全部项目已跑完`
（金标 9/9、账本离线导出 **19927 行**、严格沙箱退出码 1、指标门禁"✅ 通过"）。

**本轮为什么仍不写码**：裁定 A 的完整批次（第二十二节那份清单：2 个新读法 + `cohort.js` JS 复算 +
`usage.roundRowsByAccount` + 路由两次读法 + 契约用例 + 真机对账）是一个完整批次，
而本会话的上下文预算只够做其中一段——做一半会留下**未经真机验证的中间态**，那违反"没验证过＝没做完"，
也违反"半迁移比不迁更糟"（前几轮已经吃过一次半迁移的亏，C-63/C-65 就是它的产物）。
所以这一轮把力气花在"确认交付态没有漂移"上，而不是再压一批半成品。

**交接状态**：本地＝GitHub＝服务器 `dc52e58`；剩余两批的**接口形状与判据已逐条写在第二十一/二十二节**，
一个新会话可以照单直接开工，不需要重新推导；唯一的外部依赖是那一个待你拍板的归档口径。



