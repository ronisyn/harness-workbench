# 架构文档冲突与过期登记（2026-09-15 晚，架构调优批）

> **为什么要有这份文件**：三份权威文档（`RW-Agent架构-v1.1.md` / `RW-Agent引擎架构优化方案-v0.3.md` / `RW-Agent平台化改造总方案-v2.34.md`）
> 的状态列与结论是**分批写下的**，而代码一直在动。今天按"哪份最新读哪份"重读一遍后，发现 **10 处对不上**：
> 有的是状态列过期（其实已经做了），有的是结论与今天的实测冲突（照它做会做错），还有一处是文档内部两处自相矛盾。
> **本文件只登记、不改那三份的正文结论**——正文怎么改由你拍板；已经改了代码的地方在"处置"列写明依据。

| # | 位置 | 文档怎么说 | 实际/实测 | 处置 |
|---|---|---|---|---|
| **C-1** | v0.3 §7.1 ④ 声明式装载 | 🟡「**热装载（不重启）未做** → OP-18」 | **已落地**：`tools/registry.js` 的 `reloadManifest()`/`startManifestWatch()`；`RA-03` 实测改 `manifest.js` 一行 → 工具面 65→64，**全程未重启**（M1 矩阵 §二 RA-03 ✅） | **状态列过期**，建议改为 ✅（OP-18 可收口） |
| **C-2** | v0.3 §7.1 ⑫ 逐项过账收敛 | 🟡「**"旧账清理"未做**」 | **已定稿并有取证**：M1 矩阵 §四过账清单（已闭环 5 · 转 M2/M3 8 · 仍开放 6，`scripts/audit-ledger.mjs` 可复跑）；本轮又把"仍开放 6"里的 **4 项**做完（OP-03/12/16/17） | **状态列过期**，建议改为 🟡→✅（余 2 项见 §"仍未闭环"） |
| **C-3** | v0.3 §7.1 ⑨ 缓存影响声明检查 | 🟡「**"缓存影响"声明检查未做**」 | **本轮已做**：`server/prefix-participants.js`（12 个进请求的组件逐个声明 `where` + `cacheImpact` + **源码锚点**）+ `test/prefix-participants.test.mjs` 4 条（含"锚点必须还在"的反脱节检查） | 已实现，建议 ⑨ 拆成两条分别标 ✅/🟡 |
| **C-4** | v0.3 §7.1 ⑥ 输出溢出 + 保留策略 | 🟡「**保留（清理）策略未做** → OP-17」 | **本轮已做**：`cleanupSpill()`（按龄 7 天 + 按量 64MB，两步都保守；越界路径一律不碰）+ 启动跑一次 + 每 6h 一次 + `spill:cleanup` 账本；夹具 5 条 | 已实现，⑥ 可收口 |
| **C-5** | v0.3 §0.3.1 第 3 条 | 「**单会话 C1 有结构上界 `1 − 1/轮数`**」 | **实测证伪**：固定前缀（系统提示+工具面 ≈10,496 tokens）**跨会话共享** ⇒ 首轮就命中，不是 0。正确递推是 `hit_t = in_{t-1}`、`in_t = in_{t-1} + m_t`（`scripts/c1-ceiling.mjs` 已按此实现并删掉旧式） | **结论需更正**（M1 矩阵 §三-1 已更正过同一处，v0.3 未同步） |
| **C-6** | v0.3 §2.3 长空闲 | 「长空闲：492 分间隔 → 未命中 574,865；**小时级空闲必然重算**」 | **不能搬到我们这条路由**：会话 269 空闲 **167.9 小时**后未命中仅 2.9%；会话 185 连续三天 **24 小时**空闲未命中恒为 142；受控实验 30 分钟空闲未命中 387。而**同一会话在前缀变了 +46 tokens 的那天**，24 小时空闲未命中 92.8% ⇒ **真因是前缀面变更，不是时间** | **结论需限定适用范围**（DS 的 443k 大前缀 vs 我们的 14k）；见 `缓存追平DSH-方案-v1-20260915.md` §2 |
| **C-7** | v0.3 §4.4.1 规则3「**工具面会话内冻结**」 | 硬约束（Q2 建议采纳） | 实现**违反**它：`index.js` 的 `const light = !needsTools(content)` 让工具面**按每条消息内容**在轻量面（5,130 tokens/28 工具）与全量面（14,731 tokens/91 工具）之间翻 | **已按规则3修正**：单向粘滞（`conversations.face_full`，一旦用过全量面即固定）；这是文档与实现的**真冲突**，按文档改了代码 |
| **C-8** | v0.3 §4.7 G5「可控制（停/挂/续、**断线重连不丢现场**）」 vs M1 矩阵 §三-9 | 矩阵把「SSE `Last-Event-ID` 续订」归 **M2** | v0.3 把它算进 **M1 的 G5** | **里程碑归属冲突**；本轮按 v0.3 口径实现：新增**只读**续订通道 `GET /api/conversations/:id/stream`（补发 + 跟播，**不触发执行**），矩阵那条 M2 归属应撤 |
| **C-9** | v0.3 §0.3 C1 目标「长会话 **≥99%**」 vs §9.1 D9「"命中率 99%"**不作为目标值**」 | 同一文档两处口径 | 今天又实测出一条：**C1（会话累计）≈ 1 − 2/N**，与 P、m 无关 ⇒ 它主要在度量"会话多长" | **建议不删 C1，但在 0.3 表里加一行注**：C1 是**结果指标/与 DSH 对齐的口径**，工程目标是 C4 + C2 |
| **C-10** | v0.3 §0.3 C2 目标「中位 ≤1k / P95 ≤5k」 | 目标 | 当前实测 **m=1,766 tokens/轮**（改造后窗口），每轮输入均值 19,666 ⇒ 差距明确且可量化 | 无冲突，**列入下一步**（`缓存追平DSH-方案` §4-M4；工具结果体积是主项） |
| **C-11** | DSH `dsh-llm-retry` 的退避参数（`initialDelayMs`/`maxDelayMs`/`jitterRatio`，指数退避+抖动） | DSH 用退避重试 | **我们有意不做**：DSH 仓库里拿不到这些参数的**实际取值**——它的 `Config` 是空 schema，且明确报错 `retryPolicy belongs under each provider configuration`（策略在部署配置里，不在包内）。编一个 `initialDelayMs` 就是莫须有的值 | 本轮只做「**服务端 `Retry-After` 优先 + 没给就立刻重试**」（`gateway.js::retryAfterMsOf`/`cancellableDelay`）。**触发条件**：出现"限流下立刻重试仍连续失败"的真实记录，再引入退避；届时取值也照抄厂商给的 `Retry-After` 分布，不自己拍 |
| **C-12** | DSH `dsh-llm-retry` 的 `mode: 'always'`（**不限次数**重试，`retryPolicyKey` 里连 `maxRetries` 都不参与） | 两种模式：`normal`（受 `maxRetries` 约束）/ `always`（不受） | 我们只实现 `normal`：每轮预算来自设置 `llm_max_retries`（默认 1，0=关），跨轮自动重置 | 有意：`always` 是"人在看、随时能 Ctrl-C"的形态。我们交互式已有"停止"（且本轮起它能打断重试等待），无人值守有 `progress_stall_n`。**真实需要时再加**（加它要同时想清楚"没人看着时会不会无限打厂商"） |
| **C-13** | 「DB 要不要设业务超时」 | 三份文档都没写；我一开始按"工具必须有超时"想给 DB 编一个 | 实测挂死的真因**不是"查询慢"**，而是**对端已经没了而我们还以为它在跑**（隧道断/网络抖动时本地 socket 仍可写、write() 还返回成功）。另：mysql2 的 `connectTimeout` 默认已有 10s、`enableKeepAlive` 默认已开，但 `keepAliveInitialDelay` 不设＝交给 OS 默认（Linux `tcp_keepalive_time=7200s`）⇒ 等于发现不了 | **不设业务超时**（慢查询归慢查询日志，砍它只会掩盖问题）：连接层 `keepAliveInitialDelay: 120000`（复用本项目既有"120s 无动静即判异常"口径）+ 查询可被"停止"中断（signal → 销毁连接）。**触发条件**：真出现"查询既没死也没返回、就是慢"并拖住整轮时，改用 MySQL 服务端 `max_execution_time` 决定，不由我们编数字 |
| **C-14** | `server/agent.js` 的 `collapse_window_ratio` | `settingsSchema.js` 登记了它、`agentLimits()` 也 `pick()` 了它 | **读取键清单里没有它**：`SELECT skey, svalue FROM settings WHERE skey IN (…15 个…)` 不含 `collapse_window_ratio` ⇒ `pick()` 永远找不到行、永远返回默认 15 ⇒ **这个设置项设了不生效**（核实后还发现另两层：取值处『或 15』把显式 0 吞掉、`effectiveCollapseChars` 又把 0 兜成 0.15） | **已修**（`0c2aace`）：三层都通 + 两条机检（`test/settings-wiring.test.mjs`、`test/modelwindow.test.mjs`）+ RA-08 锚点补齐 |
| **C-15** | OP-18 统一装载器 | v0.3 §7.1 ④ 与架构 §15 记为「五类来源同注册表未做」 | 实测 MCP 工具此前是**第二张表**：`MCP_EXTRA`（给模型看的 defs）+ `execTool` 里按名字正则**现造**伪工具 ⇒ 装配期校验/工具界限表都看不见它们；`tools/list` **不跟随 nextCursor** ⇒ 工具多于一页时静默截断；重名只能等厂商 400 后由网关静默去重 | **已落地**（见 OP-18 ✅）：照 DSH `dsh-mcp-client` 注册进同一注册表 + 全有或全无 + 重名即拒 + 分页跟随 |
| **C-16** | MCP server 挂掉后的工具面 | 三份文档都没写 | 现状：`proc.on('exit')` 只删连接、**不动工具面** ⇒ 工具仍在面上（模型仍能看见），调用时如实报"未连接"。DSH 是断连即 dispose（工具面立刻消失） | **有意保留我们的做法**：工具面变化＝前缀作废（`faceChanged` 会开新段），而"看得见但调不通"是可恢复的（看门狗 60s 重连）。**代价如实记**：若 server 长期不再上线，会一直占着工具面 |
| **C-17** | MCP 分页无页数上限 | DSH `dsh-mcp-client` 只查"游标重复"，不设页数上限 | 我们照抄（同样只查重复游标）。理论上一个"每次都返回不同游标"的坏 server 会让 `tools/list` 长时间循环 | **照 DSH 不发明上限**；触发条件：真遇到此类 server，再按"总页数/总用时"设界（或直接把它当无效工具列表拒绝） |
| **C-18** | MCP 凭据存放 | DSH 有 credentials 服务（单一读写路径） | 我们的 `settings.mcp_servers` 里**明文**存 `GITHUB_PERSONAL_ACCESS_TOKEN`；出站已有两层脱敏（`/api/settings` 走 `redactMcpServers`、审计走 `redactSecrets`），但 `db_query`（global 权限工具）能直接读到明文 | **已修**（RA-48）：照 DSH `dsh-credentials`（配置只放引用、单一访问路径、现取不缓存、describe 永不含值）把值搬进专用凭据文档（0600），settings 只留 `__CRED__:NAME`。**真机验证**：服务器迁移成功、重启后真实 GitHub MCP 仍连上（26 工具）、日志/返回值无明文。**待人工**：轮换曾明文入库的那把 PAT |
| **C-26** | 两处观测仪表被夹具污染 | 三份文档都没写 | ① `agent_runs` 的断点现场（rounds/tool_counts）**107 行全是 0/{}**——根因是落盘写在工具执行**之前**（与注释相反），且最后一轮直接 return 永不落最终值；② 失败报告的 7 天 186 次失败里 **139 次来自夹具/哨兵会话**（42/42/41 的对称数字全是测试产生的），真实会话 0 次 | **都已修**：① 落盘挪到工具执行之后，并给 `.catch` 加出声（同类静默 catch 本仓库第二次栽）；② `failure-report.mjs` 只统计真实会话（`conversation_id>0`），**另一侧口径照常打印，不藏数据**；③ 同类第三处：`subagent:spawn` 的深度证据也只在有会话归属时落账，否则夹具会把 "2 层够不够" 的样本灌歪（实测未加护栏前已灌 3 行，已清理） |
| **C-27** | `mcp.js` 的 `spawn` 没有 `'error'` 监听 | 三份文档都没写 | **会带走整个进程**：`spawn npx ENOENT` 是 unhandled `'error'` 事件，`connectConfiguredMcps` 的 try/catch 抓不到（那时 promise 还没 reject）。触发条件很日常：command 配错、PATH 变化、工具没装（Windows 上只有 `npx.cmd` 时必现） | **已修**：给该次连接的所有 pending 以同一个错 reject 并摘掉 client；子代理实测发现，我在集成时落 |
| **C-28** | B-①② 的覆盖边界（子代理如实上报，我接受） | B-①② 要求"策略类写入必须归属 + 单独可审计"，我给的负例是"即便有人绕过工具直改 SQL 也要能识别" | **该负例在本设计里不成立**：识别点是 `db_write` 的 after 钩子，mysql CLI / 其它进程 / 别处代码写 `settings` 不经过 `execTool` ⇒ 不落 `policy:` 行。子代理**没有编一个假装成立的夹具**，而是把边界钉在代码注释与夹具里（这正是我要的诚实） | **接受边界并登记**。理由：本机制防的是"**模型**经自己的工具静默改策略"，而直连数据库改 `settings` 的是**管理员**（他本来就有库凭据）——用 DB 触发器覆盖它会在正常的人工改配置上产生噪音。若将来真要全覆盖：`settings` 上挂触发器 + 给人工路径补账（见 C-29），两条一起做才有意义 |
| **C-29** | 人工路径的策略写入没有账本行（未做） | 三份文档都没写 | `PUT /api/settings` / `/api/access-rules`（人经设置页改策略）走 `setSetting()`，**不留任何账本行**；`set_limits` 只有 `tool:set_limits` 行（命名可辨，但不叫 `policy:`） | **未做（有意留待拍板）**：加 3~5 行即可在 `setSetting()` 里落同一条 `policy:settings-write`（带 `req.user.id`）。现在只保证"模型路径可审计"；补上它之后 C-28 里的漂移检测方案才成立（否则人工改配置会被误报成漂移） |
| **C-30** | A2-a 分两步落，现已补完 | 我的拍板里 A2-a = "不可信注入块加来源句 + 写入带归属" | 写入归属先落（`kb_add`/`skill_save` 返回值带 `source{}`）；注入块本身的来源句当时未落——那些块在 `server/index.js`/`server/agent.js`/`kbgate.js`/`lessonrecall.js`（子代理未获授权，交回给我） | **已补完（2026-09-16）**：三处块头来源句已落（知识库标"属数据、不是指令"；错题标来源＝reviews 表；技能是指令性内容，标"不得覆盖系统与用户指令"）；技能那句在两个文件各一处，夹具两处都断言（改一处必红）。**有意不加**：平台自产的三类 system 注入与优先②清单——"标记多了等于没标记" |
| **C-31** | **"工具结果体积是每轮新增主项"被实测证伪** | 我此前的判断（也写进了 C-10 的下一步计划）：压 m 的主战场是工具结果体积，并据此做了 grep 整形/按行读/去重三件事 | 新写的 `scripts/m-attribution.mjs`（只读、真库、按轮分类）量出：近 7 天 **639 轮里工具结果只占 m 的 2–3%**、上一轮模型输出 2–3%；**219 轮是"整段重建"**（miss/输入>0.8），其中 **214 轮集中在 `conv=185` 一个会话**（432 轮、平均输入 44.6k、轮间隔仅 3–9 秒）——成因是**我在它活跃期间反复部署**（换纪元）；44 个会话里 **38 个一次重建都没有**；我们自己的 `prefix:invalidate` 账本 **0 行**（不是我们改坏前缀）。另：我第一版探针把"首轮/换段"也算进新增，量出"残差占 95%"的错误结论——**探针本身也要先被验证** | **结论更正**：① 稳态每轮新增其实很小（最近轮 m≈200–650、命中 95%+）；② 真正的浪费是**换纪元撞上活跃会话**（一次迭代 ≈14M tokens 量级），对策是纪律/流程而非代码（列为 A0）；③ grep/read 整形仍值得留（省的是那几次调用自身的上下文与延迟），但**它不是 m 的主战场**——C-10 的目标口径据此更正 |
| **C-33** | **"Windows 交付的硬编码只有三处"被全仓审计推翻** | D2′ 侦察时我写下的结论：真障碍只有三处（`/tmp/rw-jobs`、`systemctl` 自重启、`/srv` 默认值），"其余是文案（提示词，非功能）" | 独立审计（子代理全仓扫描 + 本机 Windows 探针实测）给出 **8 条阻断 + 4 条降级**，且根因只有两个：① 全仓拿 `'/'` 兼作"无边界"哨兵——Windows 上 `path.resolve('/')` 是**当前盘根**、`spawn(cwd='/')` 无效、拼分隔符判包含关系**恒为假**（`ws.startsWith(platformDir + '/')` 于是把平台仓库当业务仓库自动提交，`inside()` 判包含关系也错，5 条阻断同一根因）；② 命令执行层"按空格拆 argv 直接 `execFile`、**不过 shell**"——Windows 上没有管道/重定向/内建命令，`npm`/`npx` 只有 `.cmd` 形式（直呼 ENOENT，显式 `.cmd` 在 Node 22 上 EINVAL），连带 `run_test`、任务契约验收钩子、MCP 子进程 | **已修（结构，不是打补丁）**：① 新增 `RW_FS_ROOT`（平台所在盘的盘根）替换五处 `'/'` 哨兵，`inside()` 改用 `path.relative`（顺带修掉"根下任何路径都判在外"的既有错）；② 新增 `server/shell.js` 作为"命令串怎么执行"的唯一出处（Linux `bash -c` / Windows `powershell.exe -Command` + UTF-8 前言，**选型有实测依据**：`cmd.exe /c` 会吞内层引号、返回空输出且 code=0；PowerShell 的引号/中文/emoji/退出码都对），`run_command`/`run_test`/`run_long_task`/验收钩子/MCP 全部走它；③ 环境事实改由代码位置推导（平台目录＝`server/` 的上级、工作区＝其兄弟目录，**与现行 `/srv` 部署逐字节相同**），临时目录用 `os.tmpdir()`（留 `RW_JOBS_DIR` 覆盖：Windows 的 SilentCleanup/KB4506040 会清 `%TEMP%`）；④ 安全网在 Windows 形态下补上判据（`system_write_guard` 只认 Unix 路径 ⇒ 在客户机是空网；`danger_command_guard` 同理）；⑤ 提示词不再写死某台机器的路径与命令（`/srv/...` 示例改部署中性、`systemctl` 改"服务重启"、系统提示按平台生成）。**验证**：`test/portability.test.mjs` 8 条 + 工具面快照白名单补丁（差异清单见提交说明）+ 本机 Windows 真机跑通 shell/env/restart 三层。教训：**"按关键字扫"只能找到长得像目标的东西**——我扫了 `/srv`、`/tmp`、`systemctl`，于是只看见三处；而真正让客户机跑不起来的，是**哨兵值**（`'/'`）、**执行方式**（argv vs shell）、**边界判据**（字符串拼接 vs `path.relative`）、**编码**（UTF-8 前言）、**外部命令解析**（`.cmd`/PATHEXT）这些"不长得像路径"的类别。下次判"能不能在另一台机器上跑"，按语义类别逐条过，不要按关键字过 |
| **C-39** | **⑫ 逐项过账的清单此前只存在于已归档文档里，且没覆盖"两套"** | v0.3 §0.5 要求"旧账要么并入新架构，要么删掉，**不留两套**"，并要"逐项过账"。而实际的过账清单在 **已归档** 的 `proposals/_archive-实施记录-20260910/M1验收矩阵-20260915.md:180-231`（机器状态表不覆盖它，唯一可复跑工具 `scripts/audit-ledger.mjs` 还直连真库、CI 跑不了），**且 4 处"两套"一处都没记** | 逐条核对出 4 处：①导出 JSONL（`index.js:412`）vs `rw-session`（`index.js:435`）；②事件四处——内存环（`agent.js:51-55`）／`events` 表（`eventlog.js:37-51`）／`contract_events`（`db.js:413`，写于 `driver.js:24`）／`audit_log`；③同一次工具调用双写 `tool_calls`（`tools/index.js:1605`）＋`audit_log`（`:1591`）；④两条建表路径（`db.js` SCHEMA vs `migrations.js` 链，后者靠"人肉约定"注释，已漂移 11 列＝C-34） | **已按 §0.5 三分法逐条判定并落表**（详见 `proposals/v0.3-符合性核对-20260916.md` §6）：①**保留两者**（`rw-session`＝权威迁移格式，JSONL＝审计视图；待补一句契约说明）；②环＝保留（跟播缓存、非事实源）、`events`＝保留（唯一账本）、`audit_log`＝保留（审计动作账）、**`contract_events`＝改造/迁移**（与 `events` 同源，否则"同一事实两种投影"永远对不上账）——**唯一未闭环项**，排在 `server/index.js` 空出来之后；③**保留**（`tool_calls`＝调用明细/遥测，`audit_log`＝审计动作，分工写清）；④**保留两条路径 + 机器保证一致性**（`test/schema-sync.test.mjs` ＋ 启动自检从链推导已落地）。**在这之前不许声称"已过账"**<br>**⚠️ 2026-09-16 收口：本行"唯一未闭环项"的措辞作废** —— ②的 `contract_events` 已按本行裁决落成"与 `events` 同源"：`server/eventlog.js` 新增 `persistContractEvent()`（**一次调用里先写账本、后写投影**；投影行 `event_id` 硬链接回指账本行 id，既是追溯判据也是重放去重键；账本 `type` 与投影 `kind` 同值，追溯判据是**等式**、可机检），`server/driver.js` 的 `addEvent()` 不再自己 INSERT（9 个调用点补会话归属；`findOrCreateConv` 挪到第一条事实之前，否则首次执行的 `start` 会因 `conv_id` 为 NULL 而丢），两条建库路径一起加 `event_id`+唯一键（`db.js` SCHEMA 与 `migrations.js` 的 `0006_contract_events_event_link`，只增不删、老行留 NULL）。**`server/index.js` 一字未改**（读接口 SQL 与删会话清理 SQL 由夹具逐字节 pin；表仍是实表、`id` 仍是自增主键 ⇒ 排序与响应字段不变）。机检：`test/contract-events-source.test.mjs` 6 条 + 反向核对两组（把 `driver.js` 的直接 INSERT 加回去 ⇒ "全仓唯一写入者"报红；拿掉 `event_id` ⇒ 3 条报红）。**遗留（见 C-46）**：投影器会把契约事实误折成"当前 run 出错"，以及两处"读数会说谎"的地方 |
| **C-40** | **`server/tools/manifest.js` 被并行写入损坏（196 个 U+FFFD、换行被吞）** | 仓库约定：含中文的仓库文件必须用 write/edit 工具写盘；本仓此前没把这条写成可机检的纪律 | 另一代理做"反向核对"时用 pwsh `Set-Content -NoNewline` 把含中文的 `manifest.js` 按 ANSI 重编码（UTF-8 尾字节 0x0A 被当成双字节字符的次字节吞掉）：`node --check` 报 `SyntaxError: Unexpected identifier '覆盖'`（第 7 行字符串收尾引号被吞）、全文 196 个替换符、第 1~4 行注释被合并；**影响面＝任何 import 工具面的东西当场炸**（`npm test`、金标门禁、`tool-bounds`、`tools-face-diff`、`tools/index.js`）。同批还有一处：`scripts/windows/install-service.ps1` 的 **BOM 被编辑工具吃掉**（已用 `WriteAllText(..., UTF8Encoding($true))` 补回 239,187,191） | **已复原且未 `git checkout`**（checkout 会连本次清单改造一起抹掉）：用结构化 UTF-8 整文件重写，损坏现场留档 `tmp/damaged/manifest.corrupt-0424.js`；自证＝`node --check` exit 0、`U+FFFD=0`、`import` 装载 65 条、`APPROVAL_REQUIRED` 7 项、装配期校验照常抛错。**纪律（对全体代理广播过）**：仓库文件一律走 write/edit 工具；pwsh 只做只读与执行；改 `.js` 必跑 `node --check`，改 `.ps1` 必复核前 3 字节 `EF BB BF`；**半写状态会给并发读者造成假红**（本轮门禁读数在 04:24–04:27 因此漂移过 4~8 条，属观测噪声，不是仓库有坏文件） |
| **C-41** | **`fetch_spill` 该不该进轻量面（清单与快照不一致，门禁 250/251 双红）** | v0.3 §6.1 通则：大结果必须"落溢出文件 + 给定位符 + **可取回**"，不许静默丢弃 | `server/tools/manifest.js` 里 `fetch_spill` 已声明 `light: true`（轻量集 live 29 条），而 `test/fixtures/tools-snapshot.json` 的 `lightToolset` 仍是 28 条；`test/manifest-fields.test.mjs:100` 的夹具标题已把"轻量面闭环：否则轻量会话溢出后取不回"写成**有意契约**。另：`light` 面里本就有会溢出的工具（`read_file`/`grep_search`/`db_query`…） | **裁决＝进轻量面**（否则轻量会话拿到"截断却取不回"的结果＝§6.1 禁止的静默丢弃）；动作：快照 `lightToolset` 28→29（字典序）＋ 5 条 description 重写（`extract_pdf/docx/xlsx/pptx`、`repo_map`）＋ `scripts/tools-face-diff.mjs` 白名单**新增** 5 条理由行；裁决理由同时写进清单头部注释（审计可查）。读数：`test/manifest.test.mjs` 6/6、`tools-face-diff` 漂移 0、全量 `# tests 601 / pass 600 / fail 0 / skipped 1`。**换纪元记账**：本批改工具面（5 条描述 + 轻量面多一工具）⇒ 所有会话下次请求重建一次公共前缀，**同批一次发布** |
| **C-42** | **`scripts/migration-rehearsal.mjs` 本机跑不通（⑪ 的验收工具自身未闭环）** | v0.3 ⑪ 要求"会话与存储版本化 + 迁移链"，且验收必须是**真库上升级演练真跑过** | 本机实测（Windows + SSH 隧道到 rw_test）：`node scripts/migration-rehearsal.mjs` → 打印 `== 8. 一次性库已删除（rw_mig_rehearsal）==` **之后**抛 `Unknown database 'rw_mig_rehearsal'`（`scripts/migration-rehearsal.mjs:51` / `:101`）⇒ 收尾/复核查询连的还是那个刚被删掉的一次性库；另 `scripts/session-import-rehearsal.mjs` 需要导出物作参数（无参数＝用法错） | **未闭环**：已派 ⑪ 代理修（建库/删库/复核三步的连接目标必须显式一致；`session-import-rehearsal` 必须能吃 `session-export.mjs export <convId> --out <file>` 的产物并给出一条端到端命令）。⑪ 的验收＝**在服务器真库上跑通两条演练**并把原始输出贴回来 |
| **C-43** | **本机跑夹具/脚本时运行态 `spill/` 落在仓库根（10 个 `run_command-*.txt`）** | 仓库"工作区里只应出现源码与文档"的既有取向（`.gitignore` 已忽略 `tmp/`、`data/`、`.runtime/`、`.rw-checkpoints/`） | `server/tools/spill.js` 的 `SPILL_DIR = path.join(RW_WORKSPACE, 'spill')`，而 `RW_WORKSPACE` 缺省＝`RW_PLATFORM_DIR/../rw-workspace`；本机跑夹具/脚本时它落到了仓库根 ⇒ 溢出明细出现在工作区，且 `git add -A` 会把它带进仓库 | **已处置（只增）**：`.gitignore` 新增 `spill/`（并写明"这是磁盘运行态、可清理可重建"）。**服务器不受影响**（`RW_WORKSPACE=/srv/rw-workspace` 在平台目录之外）。另登记：`web/dist/assets/index-UxNaQaEl.js` 是本机 `vite build` 的**孤儿产物**（`index.html` 仍指向 HEAD 的 `index-CrJ5umCr.js`），按"禁止删除既有文件"**保留未删**，发布时以 `npm run build` 重建 dist 为准，且**不把它纳入本次提交** |
| **C-44** | **服务器上的 git remote 把 GitHub PAT 明文内嵌在 URL 里** | C-18（`dsh-credentials` 口径：配置只放引用、单一读写路径、describe 不含值）；v0.3 §4.5 凭证部件 | 部署前核查服务器状态时看到：`/srv/harness-workbench` 的 `origin` 形如 `https://x-access-token:ghp_…@github.com/ronisyn/harness-workbench.git`（**值不在此登记，也不写进任何文档**）。该 token 有 push 权限；`.git/config` 属 0644 可读文件，任何能读平台目录的进程/备份/日志抓取都能拿到它 | **已登记，待用户拍板**（改它要动部署链路，不属"按文档执行"的范畴）：建议 ①**轮换该 PAT**（旧值已出现在服务器配置文件里）；②把 remote 换成**不带凭据**的 URL，凭据走 `server/credentials.js` 的凭据文档（0600）或 git credential helper，而不是固化在 `.git/config`；③核对 GitHub 侧该 token 的 scope 是否可收窄为单一仓库。**本轮不改**：服务器仍在跑旧版，任何 remote 改动都必须与一次部署同批做并当场验证 `git ls-remote` 仍通 |
| **C-45** | **⑰ 沙箱：v0.3 §4.6"第 2 层拿不到模式 ⇒ 拒绝启动"落成"默认显式降级 + 开关走严格语义"** | v0.3 §4.6 原文：「第 2 层（引擎自带沙箱）拿不到模式 → **拒绝启动**；第 1/3/4 层能力缺失 → **显式降级**（留痕 + 提高审批 + 客户可见，禁止静默降级）；降级程度逐次上报 `enforcement: full/partial/none`」 | 本平台（以及客户机）现在**没有任何可用沙箱后端**：本机 Windows 实测 `runner:null / enforcement:none / layers 1:none 2:none 3:full 4:none`；服务器 Ubuntu 24.04 无 bwrap/nsjail/firejail、有 `/usr/bin/unshare`（能否 `mount -o remount,ro /` 只能真机验）。若按字面默认拒绝启动，**线上服务直接起不来**——那是拿"安全"换"服务不可用" | **落成两档（登记偏离，代码注释与交付说明都写明）**：默认＝**显式降级**（`enforcement` 如实上报 + 落一条 `sandbox:degrade` 账 + 提高审批 + `capabilities` 里客户可见，**禁止静默**）；`RW_SANDBOX_REQUIRED=1`＝v0.3 字面语义（`startupGuard()` 返回 `{ok:false, error:SandboxUnavailableError('SANDBOX_UNAVAILABLE')}` ⇒ 拒绝启动；`unconfined()` 同一判据 ⇒ 拒绝执行）。开关真值表的**唯一实现**在 `server/env.js` 的 `sandboxRequiredEnv()`（函数而非常量：判据要调用时读环境，夹具才能在同进程里断言两种语义），`server/sandbox/degrade.js` 转调它。装上 bwrap 或配好 `RW_SANDBOX_RUNNER` 后把开关打开即完全落到文档口径，**无需改代码**。**接线状态**：`server/env.js` 的开关已落（本轮）；`server/index.js` 的启动守卫（2 行，位置在 `app.listen` 之前）与 `execTool` 那一处"提高审批"的最终弹卡**尚未接线**（前者那份文件当时有其他代理在写）——**未闭环项，下一批第一件** |
| **C-46** | **⑫ 收口后暴露的三项副作用 + 一处行为面扩张（都是"同一事实进同一账本"的连带结果）** | C-39② 的裁决（`contract_events` 与 `events` 同源）＋ v0.3 §6「账本只有一个追加点」＋ 仓库"读数不许骗人"的既有取向 | ①**投影器会说谎**：契约事实现在也进账本，而 `server/projection.js` 的 `case 'error'` 会把 `type='error'` 的契约事实折成"当前 run 出错"（`run:-`）——对任务会话跑 `scripts/projection-replay.mjs` 会多一条假条目（发生面窄：任务会话账本里基本只有契约事实）；②**删会话级联不含 `events`**：`server/index.js:398` 清的是投影行，账本行留着 ⇒ 投影被删、事实还在（改造前就有的账本行为，所有会话事件同理，但现在契约事实也走了进来）；③**夹具静默不注册**：`test/eventlog-archive.test.mjs` 的顶层 `await pool.query('SELECT 1')` 在连不上库时**永不 settle** ⇒ 只报 4 条却 `# skipped 0`——不是 skip 而是"没登记"，**会让 CI 读数骗人**；④**行为面扩张**：契约事实现在也走 `notifyAppended` ⇒ 用户配了事件档触发器时，契约事实会像会话事件一样触发（这是"同源"的必然，已写进代码注释；若要回退是一行） | **①②③ 已派修（在飞）**：① 在 `project()` 里按账本行已有的结构字段（payload 带 `contractId`）跳过契约域事实——**不改账本 `type`**（那会破坏"追溯判据是等式"），并加夹具钉住"假条目不出现 + 正常会话帧投影逐字节不变"；③ 把"库不可达"改成**显式可见**的 skip（有库真跑、无库如实 skipped 两种情形都要读数），并加反向核对；② 归入下一轮（要动 `server/index.js`，且要先定"删会话是否连账本一起删"——那是保留策略，不是顺手改）。④ **接受并登记**：触发器是用户显式配置的行为，账本只有一个追加点本就意味着"进账本的事实都要广播"；若有异议一行可回退。<br>**⚠️ 2026-09-16 订正（③的根因与严重度，实测推翻了我原来的措辞）**：不是"连不上库时探测永不 settle"。真相是——基线命令 `node --test --test-force-exit` 下，**已登记的用例一跑完运行器就结束进程，模块顶层的 `await` 续体一律没有机会恢复**，所以那条真库用例**有库时也一样从没被登记过**（读数 4 条 / `# skipped 0`；连跑 3 次确认）。修法：探测移进测试体内 + **机检（读源码形状，"顶层不许 await"）登记在探测之前**（第一版把机检放在探测之后，反向核对时它自己也没被登记 ⇒ 挪到前面才成立）。反向核对：改回顶层 await → 有库/无库**都红**（不是"少一条还报 skipped 0"）。**修复后**：有库 6/6 全绿、无库 6 条 / pass 5 / **skipped 1**（如实）。**这条比原登记更严重**：一条真库用例在基线命令下从未执行，而读数看起来是绿的 |
| **C-47** | **㉔ 的六处待定口径（阈值/留档/CI/回填位）——已由我按 v0.3 与既有规则逐条判定** | v0.3 §0.4 M3（"连续两个周期产出并落地提案…每条附前后指标对比"）＋ C-31「规则4：只报数不设线，不发明阈值」＋ §0.6（不做的事写清楚） | ㉔ 交付时列出六条要拍板的：①"变差多少算回归"的线；②既有脚本里的 C1 99% / C2 1k·5k 判定线要不要搬进门禁；③提案"落地后回填位"（谁执行/改了哪些文件）；④提案清单路径约定；⑤指标报告只落 `tmp/`（M3 要跨周期可追溯）；⑥CI 要不要纳入指标门禁 | **判定**：①**不设线**（v0.3 未给，且 C-31 已定"只报数不设线、需人判"）；**要给线是用户的政策决定**，登记为待拍板项。②**不搬**：那些是 KPI 脚本的**绝对目标**（由 v0.3 §0.4 的验收口径管），本门禁管的是**前后回归**，两件事混在一起会让"没变差但没达标"被误报成回归。③**暂不加**（要动 `extension_demands` 列或加回填表＝建表变更，本批不做，登记为下一批）。④路径维持 `tmp/metrics/proposals.json`（与报告同目录）。⑤**快照本体留 `tmp/`（可重建、不进库仓）**，但每次发布把该次快照的 `batchId/codeId/golden identity/逐项读数` 写进 **release 提交信息**（本批即如此：见 `17c74e9` 之后那次发布的提交信息）⇒"两个周期"经 git 历史可追溯；**"把它也接进 `proposals/验收状态-生成.md`"排在下一批**（要动 `scripts/acceptance-status.mjs` 的生成逻辑，本批不动）；**"持久化指标归档放哪"仍是待拍板项**（仓库内 `proposals/_metrics/` vs 部署机 `data/`）。⑥**CI 不纳入指标门禁**：CI 无 MySQL，而 ㉔ 的硬条件恰是"读不到库⇒未在位⇒红"——加进去只会让 CI 永远红、门禁被无视（放宽判据不允许）；它属于**有库的发布/部署路径**（`release.mjs` 第 2.7 步已接）。 |
| **C-48** | **迁移链只加列、不补索引 ⇒ 升级来的库一直在悄悄丢索引（目前无人看管）** | v0.3 §7.1 ⑪（会话与存储版本化 + 迁移链）＋ C-39④ 的"两条建表路径必须一致"（`test/schema-sync.test.mjs` ＋ 启动自检从链推导）＋ 全局规范 06（数据库迁移：只增不删） | ⑪ 的升级演练（`scripts/migration-rehearsal.mjs`：旧形状＝**0001 之前的真实客户机**，由链**倒推**得到）实测出**索引漂移**——`audit_log_archive.idx_arch_conv` 在"照 `server/db.js` SCHEMA 新建"的库里存在，而"沿迁移链升级上来"的库里**没有**（链里的语句只 ADD COLUMN / CREATE TABLE，从不建索引）；只读探针另证实真库 `rw_test.audit_log` **已经缺** `idx_audit_time` / `idx_audit_conv`。**`test/schema-sync.test.mjs` 只管列不管索引**，所以这条漂移没有任何机检在看它 | **已登记，本批有意不修**：它不是"顺手改"——① 要新增一条迁移步把缺的索引补齐，而**在真实大表上建索引有生产影响**（锁时长/磁盘），必须专门排期；② 要把 `test/schema-sync.test.mjs` 从"只比列"扩到"也比索引"，否则补完还会再漂；③ 要定下"索引也属于 schema 契约"这条口径。**风险等级：性能向，不是正确性**（无索引不改结果，只影响查询代价）。**列为下一批第一件**，触发条件：任何一次"在客户机上跑升级"之前必须先补。<br>**原始读数（只读探针，MySQL 8.0.46，库 `rw_test`，SQL＝`information_schema.STATISTICS`/`COLUMNS`）**：`audit_log` 索引去重后只有 `["PRIMARY"]`，而 `server/db.js` 的建表语句声明了 `KEY idx_audit_time (created_at)` 与 `KEY idx_audit_conv (conversation_id)` ⇒ **两条都缺**；`audit_log_archive` 源库有 `["idx_arch_conv","idx_arch_time","PRIMARY"]`。**机理**：`0001_baseline` 只 `ADD COLUMN conversation_id` 不补索引，而"照 SCHEMA 新建的库"有 `idx_arch_conv`；倒推旧形状时 `DROP COLUMN` 会连带删掉单列索引，链再跑回来只有列没索引 ⇒ 升级库与新建库就此分成两套（演练每次都会打印这条观察项，可复跑） |
| **C-49** | **`GET /api/deliveries` 没有账号过滤 ⇒ 任何登录账号都能读到所有账号的投递记录** | D3 / OP-01「注入层与读接口按账号过滤」；`test/data-egress.test.mjs` 的存在本身说明这条边界是被当作契约的 | 存储代理定位投递夹具竞态时顺带发现：`server/index.js:465` 的该路由**只有 `requireAuth`**，而 `listDeliveries`（`server/deliveries.js`）的查询**没有任何账号维度** ⇒ 返回里有别人的 `idemKey` / `conversationId` / `lastError` / `messageId` / `runId`。**代码里没有任何管理员角色/中间件**（全仓 grep 无 `requireAdmin`/`is_admin`/`role ===`），所以这既不是"运营台跨账号视图"，也不是"管理员专属"，就是一条漏了收口的读路径 | **已派修（在飞），口径＝按调用者账号收口**：先核实该接口的预期读者（前端谁在用、`req.user` 上账号字段、既有契约文档有没有把它定义成跨账号视图）——**若证据表明它本来就是跨账号运营视图，先回报、不许自己发明管理员角色**；否则把列表限定到调用者账号（`listDeliveries` 只增一个可选账号参数，默认行为不变），并加夹具钉住"A 拿不到 B 的行、A 自己的行仍在"，再做一组反向核对。**同时登记**：投递夹具竞态的真因也在这一轮查清（写死的哨兵账号 `999999` + 依赖"全表列表窗口"的两条断言 ⇒ 并发跑必然互删/互挤），已修成"每次运行唯一账号 + 按 id 读回自己的行"，并发两跑修前 4/4 红 → 修后 10/10 绿 |
| **C-50** | **验收机检的锚点是"子串匹配" ⇒ 前缀式改名会假通过（机检本身的可信度问题）** | 仓库既有取向"读数不许骗人"；`scripts/acceptance-status.mjs` 的锚点语义 | ⑪ 为新能力加锚点时**自己踩到并报上来**：锚点判定是 `src.includes(pattern)`，所以把 `export function registerSessionMigration` 改名成 `registerSessionMigrationTMP_REVERSE_CHECK` 时，旧名仍是新名的**前缀** ⇒ 锚点照样 ✅（一次**假通过**）。这类假通过比"缺锚点"更危险：机检看起来在守，其实什么都没守 | **已修（等强度加固）**：把该锚点的 pattern 收紧为**带左括号**的 `export function registerSessionMigration(`（"前缀式改名"因此会报缺失）；反向核对原始输出：收紧前改名仍 ✅，收紧后改名 → `机检通过 49 · 缺锚点 1` + `❌ 缺：server/session-export.js :: export function registerSessionMigration(` + exit 1，改回复绿（`50 · 0 · 3 · 7`、exit 0）。**同类风险面登记**：其它锚点若以"函数名/导出名"结尾且不带定界符，同样可被前缀式改名骗过——**下一批过一遍全部锚点**，给"名字型 pattern"统一加定界符（`(`、`:`、`=` 或行尾锚）|
| **C-52** | **v0.3 批次的部署与真机验证记录（本地＝GitHub＝服务器三者一致）** | 用户口径："每项带夹具与真机验证""本地=GitHub=服务器一致" | 部署：`17c74e9`（主体）+ `856ff90`（收尾）推上 GitHub → 服务器 `/srv/harness-workbench` `git pull --ff-only` → `systemctl restart rw-test`；三方 HEAD 同为 **`856ff90`**，`GET /api/health` → `{"ok":true,"service":"rw"}`，服务 `active`。真机 15 段脚本（`tmp/verify-v03.sh`，原始输出留在服务器 `tmp/verify-out.txt`）读数：**selfcheck 13/13**；**金标 passed=9/9**（`code@7a7cc14e7251`）；**账本导出 audit=13594 + events=869 = 14463 行**；自进化采集与提案 dry-run 正常（不写库）；**headless 真跑** `conv=773 / run=367 / ¥0.0126 / status=saved`；**JSON-RPC** capabilities 正常（方法表含 `session.chat/stop/status/export`）；**升级演练** `{"ok":true,"head":"0006_contract_events_event_link","skipped":0,"tolerated":0,"checksFailed":[],"ddlMs":1041}` + 一次性库已删并复核 + **索引漂移观察项在真机复现**（见 C-48）；会话导出/导入演练通过；**沙箱探针（真机 Linux）**：`bwrap` 未安装、`unshare` 退出码 3（`mount: /: mount point is busy`）⇒ `enforcement:none` + 显式降级；**`sandbox:degrade` 真落库**（`audit_log` id 14220–14222，含 `missingLayers` 与 `reason`）；**严格语义** `RW_SANDBOX_REQUIRED=1` → `{"ok":false,"strict":true,"error":"SANDBOX_UNAVAILABLE"}` 且**退出码 1**；`0006` 已在真库应用、`contract_events` 0 行、**悬空引用 0**、`events` 869 行 | **结论：本批 ⓪–㉔ 全部落地并在真机上验过**（未闭环项见下）。**同时登记两个"跑真机才暴露"的验证脚本缺陷（已修）**：① `schema_migrations` 的列是 `(id, note, 时间戳)` 而我第一版写成 `(id, name, applied_at)` ⇒ 该段误报失败；② 严格语义那条把 `$?` 接在管道后面取，拿到的是 `tail` 的退出码（读数 0），实际是 **1**。**教训**：验证脚本自身也要按"读数不许骗人"对待——它的红/绿都要能被解释，不能只看有没有打印出想要的字 |
| **C-51** | **⑯ 收口后留下的四处"现状如实登记"（都不改，登记备查）** | v0.3 §5（平台事实只落执行后端）＋ 仓库"只增不删／只报告不顺手改"的取向 | ① **`server/shell.js` 的 `runShellLine`/`spawnShellLine` 现在没有生产调用方**（全部调用点已切到 `server/exec` 的动词），只剩 `test/exec-backend.test.mjs` 在用；`SHELL_FILE`/`SHELL_CN`/`shellArgs` 仍被 driver/agent/index 使用。② **`run_command` 的 read/write 档仍按空格拆 argv**（`String(a.cmd).split(/\s+/)`）⇒ 带引号的命令在这一档本来就会碎（`node -e "…"` 的引号会进 argv）——**既有行为**，本批未动。③ `hooks.js` 的语法检查在严格模式拿不到沙箱时抛 `SANDBOX_UNAVAILABLE`，由 `emitHooks` 按其 `failure:'open'` 记一条 hook 失败日志（**写文件仍成功、只是没做语法检查**）⇒ "拒绝"的**客户可见性**只在日志里，能力上报侧的体现取决于 `capabilities` 怎么读。④ `restartPlan()` 在 Windows 上无 `RW_RESTART_CMD` 时仍返回 `argv:null`（设计如此：如实说"没有可用方式"）；`sandbox:'off'` 的例外清单现含 `index.js` 两处（自我重启＋模板库 git 同步） | **判定：四条都不改，只登记**。①按"只增不删"保留（它仍是 shell 事实的单源与夹具载体；要不要收成一处属结构性调整，排下一批）；②③④都是**既有行为**，本批的范围是"平台判据收进执行后端"，顺手改它们会扩大改动面且没有对应条款 |
| **C-38** | **按 v0.3 逐项核对时发现的 9 处缺口，登记里一条都没记**（此前只按老文档的 RA/OP 清单过账，v0.3 的 24 项从没被逐条核对过） | 我此前的"剩余待办"是以老文档 §14.3 的 RA/OP 清单为骨架，**v0.3 §7.1 的 ⓪–㉔ 从未逐项核过**；于是"机制齐了"的印象掩盖了下面前四条实质缺口 | 逐项核对（只读，证据在 `proposals/v0.3-符合性核对-20260916.md` §3.5）：① **渠道绕过会话 API**——飞书 `channels/feishu-webhook.js:110-128` 与微信 `channels/wechat.js:54-73` 自己拼历史、直调 `runAgent` **不传 `emit`** ⇒ 渠道会话没有 run_start/run_end/done/工具/审批/问询/成本事件、无幂等与投递记录、**不可停不可续**，还硬编码 `deepseek/deepseek-v4-flash`；G5"跨端一致"因此**被代码反证**。② **溢出文件跨会话可读**——`tools/spill.js:137-147` 只校验路径在 `SPILL_DIR` 内、**不校验会话归属**（§4.4 要求"溢出文件的权限"，只做了密钥脱敏）；实测暂无 spill 文件故未爆。③ **会话内历史滑窗改写请求前缀**——`index.js:882-890`（消息 >40 条只发最近 30 条 + 早期摘要）违反 §4.4.1 规则1"只追加"，而 **C4 机检在 run 内重置 `prevCore`（`agent.js:490`）看不到它**。④ **前缀声明表漏组件**——漏 4 条 hist 前注入（`index.js:892/901/910/918`）＋`COMPLETION_HINT`（`agent.js:898`）＋护栏/打回提示（`agent.js:697/721/770/774/884`），与规则5"任何进前缀的组件都必须声明"不符。⑤ **轻量面缺 `fetch_spill`**（`tools/manifest.js:17` 只有 `exempt` 无 `light`，而 `agent.js:264` 只保留 `LIGHT_TOOLSET`）⇒ 轻量会话溢出后取不回（`repo_map` 却在面上）。⑥ `extract_xlsx` 仍整表切片（`extract.js:21-30`＋`tools/index.js:578`），**v0.3 §6.1 点名的处置未做**。⑦ **C4/C5 无仪表无告警**（只有 C1/C2 上首页），审计台连 `prefix` 中文名都没有（`src/console/AuditBoard.jsx:6`）。⑧ 契约漏**溢出提示/可回滚/逐轮成本事件**（§4.7 要求）。⑨ 登记 C-3 写"4 条夹具"、实际 6 条（状态列过期，与 C-1/C-2 同病） | **只登记，未动手**（本轮是核对不是实现）。**处置顺序建议**：①③④ 属"看着对、实际漏"的机制缺口，与 ⑧⑨⑩ 同批（第二批）修；②是安全项（跨会话读溢出文件），归 D3/RA-59 那条线；⑤⑥ 是工具面小修；⑦ 随 ⑨ 的仪表一起做。**核对方法与全部证据**见 `proposals/v0.3-符合性核对-20260916.md` |
| **C-37** | **"每日自我进化"定时任务与 v0.3 的自我放行铁律正相反** | 我此前把自进化当成"M3 还没开工"（㉒㉓㉔ 空转 ⇒ 没在做），没有去看**已经在库里的定时任务正文** | v0.3 §0.4 原文：「② 自我放行（对策=产出的是**提案**，改自己代码仍需人工审批，**铁律不松**）」。而真库 `scheduled_tasks.id=4`「每日自我进化-05:00」的 `prompt` 字段**明文要求**"修复（低风险优先）…通过后 `git commit`"——即**绕开提案与人工审批直接改自己代码并提交**；`docs/archive/daily-evolve-log.md` 尾部记录了它 2026-09-14 的提交 `c272e40f4`。当前该任务与 #3/#5/#7 **都是 `enabled=0`**，所以是**潜在违规而非正在违规**：一旦有人把它启用，就等于把铁律关掉 | **已处置（2026-09-16，按用户指令"全部按新文档执行修改、冲突一律以新文档为准"）**：把该任务的 prompt **改写成 v0.3 §0.4 的合规形态**——只读侦察 → 产出提案（`intake_submit` 落 `extension_demands`／写入 `evo_goals`）→ **明令禁止改平台代码、`git commit/push`、`db_write`、`reload_platform`**，并要求每条提案带证据（文件:行）与验证方式、按 §0.4 的优先级判据排序；**原文完整留档**在 `proposals/C-37-每日自我进化任务原文-20260916.md`。回读核对：长度 897、"只能产出提案" ✓、"不得 git commit/push" ✓、原来的"通过后 git commit"已移除 ✓。**启用状态保持 `enabled=0`**（M3 准入前置未满足，启用与否留作下一步决定）。原处置意见（保留备查）：① 该任务的 prompt 需要改写为"**产出提案**（落 `extension_demands` 或 `evo_goals`）→ 等人审批"，或删除该任务；② 启用前必须先有 ㉔ 的"指标回归门禁"在位（否则自进化＝盲改）；③ 与 ㉓ 的"提案链断在产出环节"是同一件事的两面。**触发条件**：动 M3（㉒㉓㉔）之前先处置这条 |
| **C-36** | **权威文档切换：老架构文档作废，v0.3 成为唯一权威** | 我此前一直把 `RW-Agent架构-v1.1.md` 当"agent 侧唯一权威"，据它（§7.2 的"无外网"、§14.3 的 RA 清单）与 `引擎优化方案-v0.3` 的 M2 行推导范围与优先级；沙箱那组（RA-15–19/25/27）我按"§7.2 已拍板不为客户形态造沙箱 + D5 不接外部引擎"判成**搁置** | **用户 2026-09-16 当面更正：v1.1 是老文档、`RW-Agent引擎架构优化方案-v0.3.md` 才是新文档，且沙箱那些都是要做的**（v0.3 §0.2 的 G1 原话就含"存储、模型、**沙箱可替换**"；§4.6 保障面把"沙箱：服务 + 按平台后端 + 失败语义"列成目标；§7.1 的 ⑰「沙箱服务与分级」是 P3 正式条目，依赖 ⑯ 双平台执行后端）。⇒ 我据老文档得出的"沙箱搁置"结论**作废** | **已按更正处置**：① `RW-Agent架构-v1.1.md` 顶部加**废弃横幅**（说明唯一权威已切到 v0.3、本文退为"被 v0.3 引用的口径来源"——v0.3 的 §4.3 记忆口径与 §4.6 沙箱失败语义仍指向本文的 §5.6 与 RD-05/RD-15，故**保留正文不删**）；② 本登记「剩余待办总览」加降级说明，排期依据改为 v0.3 §7.1；③ 按 v0.3 逐项核对现状，产出 `proposals/v0.3-符合性核对-20260916.md`（差异 + 任务清单）。**顺带记一处 v0.3 自身的措辞未更正**：§0.4 的 M2 行仍写"客户内网可装可用 / 无外网 + Windows Server"，与用户"客户必然有互联网"的更正（见 C-32）冲突——**文档正文我没改**，只在废弃横幅与核对报告里标注，请你有空时定稿 |
| **C-35** | **"收尾只在 full 权限下验过"漏掉了一整类会话** | `selfcheck`/`agent-smoke` 12/12 常绿，我据此认为 `/api/chat` 的收尾（done/run_end）没问题 | MCP server 默认用 **read** 权限建会话，第一次调用就炸：`/api/chat` 的 `run_end` 里 `root: permission === 'full' ? RW_FS_ROOT : ws` 中的 `ws` 声明在 Agent 执行循环那个块里、被引用在块外；**三元表达式只在非 full 时才求值到它** ⇒ **每一轮 read/write 会话跑完都抛 `ws is not defined`**，客户端收到 `done` 却永远等不到 `run_end`（异常兜底那一处也在块外，于是再炸一次，落到 Express 错误中间件）。full 会话走 `RW_FS_ROOT` 分支，永远碰不到它——所以两个脚本全绿也一直没有暴露。**这不是 MCP 引入的**：代码在基线 `a3796e4` 上同形（只是当时写的是 `'/'`） | **已修**：`ws` 提到处理器最外层声明（它本来就是常量；块内那份删掉），四条引用回到同一层——不玩作用域技巧。**回归保护**：`selfcheck` 增加一步"**write 权限会话也能跑完一轮**（done + run_end saved）" ⇒ 自检从 12 项变 13 项；真机验过（13/13）。**教训**：自检只跑**默认权限档位**时，它证明的是"这一档能跑"，不是"都能跑"——权限/档位/渠道这些**正交的轴**要各有一条 |
| **C-34** | **"全新库装机能用"被端到端实测证伪**（装机阻断，与操作系统无关） | 我在 C-20 修过"全新库迁移语义"，之后一直认为新机器装完即可用；D2′ 的 Windows 端到端验证也差点只测"能起服务"就收工 | 实测（Windows 上真跑 + Linux 上同一路径复核）：全新空库上服务能起、`/api/health` 200，但 **`POST /api/conversations` 永久挂住**（客户端 240 秒超时），服务端只有一行 `unhandledRejection: Unknown column 'provider' in 'field list'`。根因两条：① `server/db.js` 的建表语句与 `server/migrations.js` 的迁移链**漂移了 11 列**（`conversations.provider/model/shell_id/face_full`、`usage_stats.shell_id`、`audit_log.shell_id`、`shells.intent_rules/task_profiles`、`knowledge.kind/status/related_component`）——migrations.js 头部只写了"两处都改才算完整"这句**人肉约定**，没有机器核对；② 全新库走"按最终形状建表 + 整条链标记为已应用"的特例 ⇒ 那些列的 ALTER 一条都不跑。**客户机（以及任何新机器）装机必然撞上** | **已修（结构）**：① 补齐 11 列（类型逐字抄自 0001_baseline）；② 新增**静态一致性夹具** `test/schema-sync.test.mjs`——链里加过的每一列/每张表都必须在 `db.js` 建表语句里，两条建库路径从此由机器对齐；③ 启动自检的列清单**改为从迁移链推导**（原来是手写一小段，实测漏了 5 列），运行时盖住"库里有、链里没有"的反方向；④ 顺带修掉放大器：**Express 4 不接 async 处理器的 rejection** ⇒ 任一 handler 里 await 抛错时请求挂住而不是 500（新增 `server/asyncwrap.js` 统一包 240 个处理器 + 夹具断言"抛错必须换来 500"）。**真机验证**：服务器上建一次性空库、起第二实例、缺列 0、建会话 28ms、**selfcheck 12/12（不改库）**、日志 0 次 Unknown column，验完停实例删库 |
| **C-32** | **客户形态的前提写错了**（"无外网"） | `RW-Agent架构-v1.1` §7.2 与 RA-34、`引擎优化方案-v0.3` M2 行、M1 矩阵 §三都写着"客户形态机＝**无外网** + Windows Server"，并要求"离线安装包 / 本地兜底 / 健康上报默认关闭 / 白名单代理" | **用户 2026-09-16 当面更正：客户必然有互联网**（永远在网络上使用）。照旧前提推导出的两条决策——"内网网关 vs 本地模型"（D1）与"离线安装包 vs 内网私服"（D2）——**都是不存在的问题**；我照文档推了它们，是我的判断失误（错在"把文档当事实，没向你核对"）。连带：§7.2 的"默认断网＋白名单代理"与 v0.3 的"无外网/无平台侧时自落账"也应一并重估 | **已按更正重写 M2 决策表**（D1 删除、D2→D2′ 客户机交付）；**那三份文档的正文我没动**（本登记只登记不改正文），请你在有空时更正 §7.2 与 RA-34 的措辞，否则下一批人还会照它推 |
| **C-19** | 失败的结构化程度 | DSH 全线 `error.info.code`（工具结果 `{isError, error:{info:{code}}}`，重试策略读 `failure.code`） | 我们此前是自由中文 `{error:'…'}`（同一类失败在十处各写一句话；账本只有 `status=fail`，回答不了"最常见的是哪种失败"） | **已做最小可用的一份**（RA-46）：码表 `server/failures.js` 一处 + `fail()` 表外码抛错 + 夹具交叉核对 + 账本 `tool_calls.error_code`（迁移 0002）+ `scripts/failure-report.mjs`。**未照搬**：没有 `info`/`name` 嵌套、没有把每个异常都包成结构化对象——够用即可 |
| **C-20** | 装机路径（全新库）的迁移语义 | 三份文档都没写；我在 migrations.js 里写的口径是"新库路径按最终形状建表 + 新迁移不写 tolerate" | **两条口径会打架**（加 0002 时发现）：`initSchema` 先按最终形状建表 ⇒ 0002 的 `ADD COLUMN error_code` 在全新库上就是重复列 ⇒ 不 tolerate 则**判失败并 break**，于是**链停在 0002、后续迁移永不应用**，且每次启动刷错误日志。客户装机正是这条路径 | **已修**：`runMigrations` 判定"核心表此前不存在"＝全新库 ⇒ 整条链标记为已应用、不执行任何结构语句（与 DSH 会话格式同思路：新文件直接写在最新版本上，不存在"迁移"）。存量库（有表无迁移表）照常走链，两条路径由夹具分开锁住 |
| **C-21** | `run_command` 命令纪律 | 架构把"命令纪律"列为一条纪律（读文件用 read_file 等） | **实测（服务器真库 14 天）**：`run_command` 1,116 次调用、71 次失败，其中 **65 次是纪律拦截**：`cd` **40**、`grep` 17、`ls` 4、`sed` 2、`find` 1、`echo` 1；同期专门工具用量其实很大（grep_search 555 / read_file 475 / list_dir 208） | **已回答并处置一半**（见下条 C-22）：`cd` 这一半改成**改写**（不再拦）——这是 DSH 的做法：它给 bash 工具一个 `workdir` 参数，模型根本不必写 cd |
| **C-22** | 为什么 DSH 很少"被拦" | 三份文档都没写 | 读 DSH 源码：① `dsh-tool-bash` 的参数里有 **`workdir`**，描述原文 "Each call runs in a fresh shell: no state (cwd, variables, functions) persists between calls — **pass `workdir` instead of using `cd`**"；② 全量扫 DSH **没有任何**"别用 shell，去用专门工具"的预拦钩子（唯一命中就是上面那句 workdir 提示）；③ 它的边界是**沙箱**（真实能力边界），且描述明说"Attempting a command the sandbox may deny is safe and expected: run it and read the marker **rather than assuming the denial**"——不预判、不靠 hook 猜模型想干什么 | **已改**：`run_command` 加 `cwd` 参数（+ 描述里一句正向提示"换目录用 cwd"）；命令纪律钩子把 `cd <简单目录> && 其余` **改写**成 `{cwd, cmd}`（留痕 `hook:rewrite`），其余读型别名（grep/ls/find/cat/sed/head/echo）照旧拦——它们拦得住**有实测理由**（grep_search 整形后 9,990→3,890 字节、去重再降到 290），而 `cd` 拦了毫无收益。**不给绕过口**：改写后用改写结果再判一次纪律（`cd . && grep x` 仍被拦）。代价：工具面前缀 +约 65 tokens（命中价 0.086/M，可忽略）；收益：14 天里最多 40 次白花的一轮（每轮 ≈ m 1.2k tokens + 一次模型调用） |
| **C-23** | 读型别名黑名单**整体删除** | 架构把"命令纪律"列为一条纪律 | 按上条逐条过原则后决定：拦读型别名是为了省 token，而**我们已经有同一套输出层机制**（`runCmd` 8000 字符截断 + `spillToolResult` 大结果落盘 + `result_bytes` 遥测）⇒ 用"拦掉一整轮"重复实现同一件事，是拿更差的手段；且它不是能力边界，是自己发明的风格规则 | **已删**（钩子改名 `shell_readonly_guard` → `shell_cd_normalizer`，只剩一层 cd 改写）；保留意图但不花轮次：命中读型别名时在**结果里**附一行提示。**观察窗口**：一周后看 `scripts/failure-report.mjs` 里 `TOOL_HOOK_BLOCKED` 是否归零、`run_command` 的 `result_bytes` 中位数升了多少（两个数都已在账本里） |
| **C-24** | `RA-37`「仅靠事件流可重建全过程」 | 该验收项标 ✅（重建器 + 真 SSE 三场景） | **摸底发现它建在两个不同的源上**：前端重建器读的是业务表（`messages`/`tool_calls`），而"事件流"本身**只活在内存环里**（最多 300 条、进程重启即忘），库里没有任何事件表 ⇒ 严格意义的"靠事件流重建"当时做不到 | **已补另一半**（RA-47）：`events` 表 append-only、写入点唯一挂在对外事件契约的出口 `send` 上、`scripts/replay-events.mjs` 可从账本回放。**归档器未实现**（保留沿用审计账本 90 天口径），登记为未闭环 |
| **C-25** | 本地开发库指向哪里 | 我此前多次说"本地 + 服务器各跑一次" | 实测：本地 `.env` 是 `DB_HOST=127.0.0.1:3306` + `DB_NAME=rw_test` —— 那正是 **SSH 隧道到服务器的同一个库** ⇒ 我说的"两次独立验证"其实是**同一个库跑了两遍**（C-14 那次也一样） | **口径更正**：以后只说"真库验证"，要说"独立"就换库或换机器；本轮起取证脚本输出里带库名，避免再混 |

## 剩余待办总览（2026-09-16 更新；逐项状态以 `验收状态-生成.md` 为准）

> ⚠️ **本节已降级**：**任务清单的唯一来源是 `RW-Agent引擎架构优化方案-v0.3.md` §7.1 的 ⓪–㉔ 与 §7.2 的五批次**
> （2026-09-16 用户当面指定 v0.3 为唯一权威，`RW-Agent架构-v1.1.md` 已加废弃横幅、退为被 v0.3 引用的口径来源）。
> 本节与 `### A.`–`### D.` 各表是**上一轮工作视角的快照**，其中的 A0/A2/A3、"形态"、"交付" 等行已被本轮做掉；
> 读它请对照 `proposals/v0.3-符合性核对-20260916.md`（按 v0.3 逐项核对的差异与任务清单）。
> 保留本节的理由：它是**历史判据与代价记录**（每条都写了"为什么当时不做"），对过账有用；但它**不再是排期依据**。

> 上一版的这一节写的是"下一步＝投影器/对账/归档器"——那三项**都已经做完并真机验证**（RA-47/RA-50/RA-51）。
> 这里换成一份总览，避免登记里留着"已经做完的下一步"。**架构 A 类清单已清空**（工具界限/LLM 重试/DB 连接层/
> 统一装载器/统一失败分类/事件账本+投影+归档/凭据单一访问路径/会话导出/注入防线），下面是其余全部。

### A. 可立即做（不需要任何外部条件）

| # | 事 | 为什么要做 / 触发条件 | 依据与代价 |
|---|---|---|---|
| A0 | **换纪元的代价要有政策**（如"有活跃会话时不做工具面改动"或"一天只改一次面"） | **实测发现（C-31）**：近 7 天 219 次"整段重建"里 **214 次集中在同一个会话**（`conv=185`，432 轮、平均输入 44.6k），原因是**我在它活跃期间反复部署**（轮间隔只有 3–9 秒，不是空闲过期；`prefix:*` 账本 0 行 ⇒ 不是我们改坏前缀）。按 214 × ≈65k miss 估算，**一次迭代约烧掉 14M tokens** | 修法不是代码而是**纪律/流程**（何时改工具面、改完要不要立刻 reload）；代价为零，收益是这 14M tokens 级别的一次性浪费不再重演 |
| A1 | **C-10 的 m 压缩**：先出一张"每轮新增从哪来"的实测分布表，再按占比整形工具结果 | 唯一还挂在终极目标（成本/延迟、超越 DSH 指标）上的架构项；A 类清空后它是最大的未动项 | 仪表已就位：`result_bytes` 遥测 + `failure-report`（口径已修正）+ `projection-replay`。做法延续既有结构性路线（导航优先/溢出到盘），**不发明阈值**、先量后改 |
| A2 | **C-29 人工路径的策略审计**：`setSetting()` 落一条同口径 `policy:settings-write`（带 `req.user.id`） | 补上之后"C-28 的漂移检测"才成立（否则人工改配置会被误报成漂移） | 3~5 行；已被点名两次，属"审计完整性"而非新机制 |
| A3 | **`tools-face-diff` 接进常规门禁**（CI 或 `scripts/verify.mjs`） | 它现在是脚本，不进 CI 就会重演"快照悄悄漂移" | 1 行接线；`package.json` 尚未动 |
| A4 | **确定性投影的"运行时读"**（增量三） | 现在只做到"投影 + 对账"；让运行时改读投影，要先有"长期一致"的证据 | 证据正在累积（每次会话都在对账）；这一项是**下一增量**的正式形态，不是补丁 |
| A5 | 归档器的**自动化周期复核** | 归档函数与 6h 接线已落，但真实的量与周期要等数据 | 不预设参数（真实增长数据出来再定） |

### B. 需要人判或真实样本（机器判不了）

| # | 事 | 缺什么 |
|---|---|---|
| B1 | `RA-35` 命中率与增量指标 | 判据已更正为"**4 条 0/1 不变量 + 只报数的观测量**"（原三条数值目标已撤回）；观测在跑，结论要人看 |
| B2 | `OP-08` 子代理深度实证 | 深度证据已可一条 SQL 查（本轮补的）；**"2 层够不够"要真实三层任务样本**——样本还没积累 |
| B3 | `RA-11` 历史 SKU 备货量走精确查询 | 需真实业务数据核对（平台侧） |
| B4 | `OP-02` 自动验证谁读业务数据 | 平台侧；`RA-31` 的"自述不可信"目前只能部分生效（能力清单已如实标注） |

### C. 转 M2/M3（**触发条件出现前不做**，理由见上一节表格）

| 类 | 项 | 触发条件 |
|---|---|---|
| 抽象 | 存储抽象 / fs-exec 后端抽象 | 第二个真实使用者（客户形态确定、或出现 Windows 原生/远端执行） |
| 安全 | **沙箱策略（v0.3 §7.1 ⑰，P3，依赖 ⑯）**；`RA-15..19` 数据视窗、`RA-20..25` 外部引擎围栏 | ⚠️ **本行原写"§7.2 已拍板不为客户形态造沙箱"，已作废**（C-36）：用户 2026-09-16 指定 v0.3 为唯一权威，**沙箱是要做的**——v0.3 §0.2 G1 含"沙箱可替换"、§4.6 保障面列"沙箱：服务 + 按平台后端 + 失败语义"、§7.1 ⑰ 是 P3 正式条目。现状：**零实现**，第 1/2/4 层是硬编码常量、无探针、无"第 2 层拿不到模式就拒绝启动"的挂点。外部引擎围栏仍随 D5 搁置（不接外部引擎） |
| 形态 | 访问形态 headless/CLI/JSON-RPC/**MCP server**（`RA-41` 被别的 agent 当工具调、`RA-42` 幂等键/回调 HMAC/死信） | M2 开工；开工前先看 DSH 对应形态再定契约 |
| 数据 | `RA-32` AgentAdapter 契约 / `RA-33` 三家引擎成本基线 | 需要多引擎真实流量 |
| 交付 | `RA-34` 客户形态机装 + 自检 + 账本可导出 | 客户形态确定（导出能力已具备：会话导出 + 迁移链 + 全新库路径本轮都验过） |

### D. 已决但**有意不做**（留档，避免被当成漏项）

| 项 | 为什么不做 |
|---|---|
| `A2-b` 知识/技能注入降为 `role:user` | 结构与 DSH 一致，但要先做真机角色对照实验（同内容两种角色），且贴在生成点前有被当"最新用户指令"的风险 |
| `B-③` full 会话硬拦策略写入 | 安全剧场：挡不住 `write_file` 路径，且与"模型可自改 + reload_platform"主线冲突 |
| `C` 按"本轮含外部内容"给高危工具加门禁 | 可绕（读一轮、写一轮）+ 误伤主线 + 无真实事故样本 ⇒ 转**观测项** |
| `C-17` MCP 分页设页数上限 | DSH 也没有（只查重复游标）；真遇到坏 server 再说 |
| 平台自产 system 注入加来源句 | "标记多了等于没标记"——它们无可信度疑问 |

## 仍未闭环（本批没做完、需要排期的）

| 项 | 出处 | 还差什么 |
|---|---|---|
| `OP-08` 子代理深度实证 | 架构 §15 | 本批只做到"上限可配置（`settings.subagent_max_depth`）+ 每次派生记录实际深度（journalctl `[subagent] depth=`）"；**"2 层到底够不够"仍要真实三层任务的数据**——现在有数据可采了，但样本还没积累 |
| `OP-11` 提示注入防线工程化 | 架构 §15 | **方案已成文并拍板**：`proposals/提示注入防线-方案-20260916.md`（含 DSH 对照：它只对 **web** 用 `EXTERNAL_WEB_CONTENT_NOTICE`、消息带 `source{kind}`、技能/AGENTS 以 **user 角色**注入并附"不覆盖系统/用户指令"；**没有** taint、没有按来源的工具门禁、"策略只能人改"没有强制点）。**决定**：做 A1（只对外部来源加不可信声明，结果头+描述，接受一次换纪元）、A2-a（不可信注入块加来源句 + `kb_add`/`skill_save` 写入带归属）、B-①②（策略类写入必须可识别+单独审计）、D（能力清单**新增一等字段**如实声明"提示注入未防住"，进用户可见面但不进模型上下文）；**不做** A2-b 角色降级（缓，先做真机对照实验）、B-③ full 会话硬拦（安全剧场：挡不住 write_file 路径）、C 按"含外部内容"加门禁（可绕 + 误伤 + 无真实样本）→ 转**观测项**。另记两条最锋利的发现：`kb_add` 是 `permission:'read'`（只读会话即可写 `scope=global`、对全会话以 system 角色注入的长期记忆）；`db_write` 是 `permission:'global'`（checkPerm 对 global 恒真）⇒ write/full 会话可无审批改 settings 任意策略键 |
| `OP-02` 自动验证谁读业务数据 | 架构 §15 | 平台侧，`RA-31` 的"自述不可信"目前只能部分生效（已在能力清单里如实标注） |
| `RA-27` 干净沙箱 + 重放摘要 | v0.3 §7.1 ⑰（P3，依赖 ⑯） | **零实现**（C-36 已更正：不是"不做"）。要做：沙箱服务 + 按平台后端（DSH 的做法可参照：Linux bwrap→Landlock、Windows ACL 受限令牌、macOS Seatbelt，且**每个 runner 都要功能性探针**）＋ §4.6 失败语义（第 2 层拿不到模式 → 拒绝启动；第 1/3/4 层缺失 → 显式降级 + 留痕 + 提高审批 + 客户可见 + 逐次上报）。现状可承担的部分：`capabilities.js` 的三值 + 四层结构 + DEGRADE_CATALOG（声明与出口已在）、`hooks.js` 的 fail-closed 与留痕、`approval.js`/`asks.js` 队列 |
| 重试身份与可重放状态 | DSH `dsh-llm-retry` | DSH 给整条重试链铸一个 `retryId`，并把 `llm/retry`、`llm/retry-started` 两个事件落进**会话投影**（重启后可重放、可核对）；我们只在事件流里发一条 `llm_retry`，没做投影与身份。属 M2（会话可重放）范畴，届时一并做 |
| 工具界限的"体检"数据 | DSH `dsh-tool-call-timeout-policy` | 本轮把界限变成**可声明**（8 个内置工具 + MCP 同一口径），但"哪个界限定得不对"仍需真实数据：越界事件现在会落成 `tool_calls.status='fail'` 与 `[tool-timeout]` 日志。攒一段真实流量后按这些行复盘，再决定是否调（**不预先拍数字**） |
| **D2′ 审计里已确认但本轮有意不修的四项**（留档，避免被当成漏项） | 本登记 C-33 的审计（子代理全仓扫描，逐条给了 `文件:行号`） | ① **CRLF 解析**：`skillsmgr.js:20`/`knowledge.js:68`/`repomap.js:56,87`/`tools/index.js:113` 等处只按 `\n` 切分。**实测判定：影响比报告说的小**——frontmatter 取值处有 `.trim()`（`\r` 被吃掉），这些正则也大多不以 `$` 结尾；真的会残 `\r` 的只有"知识分块标题"和少数 `$` 结尾的符号正则。属**降级**，改动会散在 5 处解析器里，等真出现"Windows 上编辑过的文件解析出错"再修。② **`e2e-*.mjs`（仓库根 4 个）与 `cache-*.mjs` 等一次性历史探针**：里面是 `import ... from '/srv/harness-workbench/server/db.js'` 这种绝对路径 import，在 Windows 上连模块都加载不了。它们是**已经交付过的历史证据**，不是平台功能，**不修不删**（按"只报告不顺手改"的纪律）。③ **`scripts/guard-deploy.sh` / `orphan-scan.sh` / `PROD-DEPLOY.md`**：bash + systemd + nginx 的 Linux 部署链路，Windows 侧另写（`scripts/windows/`），**不把 Linux 那份改成跨平台**（两边的运维现实本来就不同）。④ **`mreduce-probe.mjs` 等探针里剩下的 `root: '/'`**：探针自身的入参，不是平台代码；真要跑就用 `RW_FS_ROOT`。 |
| **D2′ 审计的"待确认"里仍然悬着的一条** | 同上（待确认 5） | Windows 上**junction / 8.3 短名**：`inside()` 现在用 `path.relative`（已修掉大小写与盘根两类误判），但若工作区是经 junction 或短名访问的同一目录，两侧字面路径不同仍会被判"在外"。**没有真实部署依赖 junction**（WinSW 安装方式不需要），故不预先加 `realpath` 归一（那会引入"每次边界判定都碰一次文件系统"的代价）。触发条件：客户机上真出现"同一目录两种写法"。另两条待确认已闭环：控制台代码页**已由 UTF-8 前言处理**（`server/shell.js`，实测中文+emoji 正常）；盘符/写权限**已由启动时建目录 + `RW_JOBS_DIR` 可覆盖处理**。 |

## 里程碑归属：这些**不在本轮做**（如实转 M2/M3，附不做理由）

> 判据：**在客户形态/接口形态定下来之前做，就是投机设计**——抽象要由第二个真实使用者逼出来，
> 而不是先造好等它来（这也是本轮反复用的那条原则：不发明、不打补丁、简单优先）。

| 项 | 归属 | 为什么现在不做 |
|---|---|---|
| 存储抽象（DSH 有 storage 层） | ~~M2~~ **改为：按 v0.3 §7.1 ⑦（P1，依赖 ①）做** | ⚠️ 本行原写"触发条件=客户形态确定，现在抽就是空转的间接层"，与 v0.3 冲突（v0.3 §4.1 要求"存储走接口"、G1 要求"不依赖我们的数据库"，⑦ 是 P1 批次）。现状量化：`server/` 下直接 `db.query`/`pool.query` **379 处**（`index.js` 一个文件 244 处），无 storage 接口层。**做法参照 DSH**：接口包 + 实现包分层（`dsh-storage`/`-domain` 接口 + `dsh-storage-json` 实现；会话侧 `dsh-session-persistence` + `-jsonl`、查询 `dsh-session-query` + `-sqlite`） |
| fs / exec 后端抽象 | ~~M2~~ **按 v0.3 §7.1 ⑯（P3，依赖 ②）+ §5** | ⚠️ 本行"只有命令执行是 OS 绑定的"这句与 v0.3 §5 冲突（v0.3 把"执行后端抽象 + 双平台支持"列为第四批）。现状：`server/shell.js` 只做"按平台选 shell"，**没有执行后端接口**；DSH 的做法是 `dsh-subprocess`（接口）+ `-local`（实现）、`dsh-shell` + 每平台一个实现（`dsh-bash-local`/`dsh-pwsh-local`） |
| 沙箱策略 | ~~M2~~ **按 v0.3 §7.1 ⑰（P3，依赖 ⑯）做** | ⚠️ 本行原据"§7.2 已拍板不造沙箱"——**该前提已作废（C-36）**。见上表「安全」行与「仍未闭环」里的 `RA-27` 条目 |
| 访问形态：headless / CLI / JSON-RPC / **MCP server** | M2 | `package.json` 的 `bin` 为空。**MCP server 已落地**（`server/mcp-server.js` + `scripts/rw-mcp-server.mjs`，被官方 Inspector 真调通）——但它**建在平台 HTTP API 之上**（要服务在跑），所以 v0.3 §7.1 的 **⑬ headless 执行形态仍是空的**（⑭ 在 v0.3 里依赖 ⑬，我们绕开了这条依赖，如实登记）。**JSON-RPC 形态（⑳）未做** |
| 三家引擎成本基线 / AgentAdapter 契约 | M2/M3 | `RA-32`/`RA-33`：当前只有单一引擎的真实流量，基线与契约都要真实数据支撑 |
| 确定性投影的**运行时读**（不只是对账） | 下一增量 | 本轮先把账本与投影/对账做出来（RA-47 + 投影器）；让运行时改读投影，要先有"投影与现值长期一致"的证据 |
| 事件账本归档器的**自动化周期** | 后续 | 本轮实现归档函数与接线；真正的周期与容量参数等真实增长数据（不预设） |

## 已决：技能注入不再作废前缀（原"一处需要你拍板的取舍"）

**决定：A（改实现）**——`skill_load` 的技能全文改注入**尾巴区**（追加到历史之后），系统提示保持逐字节冻结。
理由：规则 2「前缀冻结」比规则 3「工具面冻结」更硬（前者影响**所有**会话，后者只影响当前会话），而技能是会话内行为。

**已落地并有证据**（2026-09-16 核对，声明与实现一致）：
- `server/prefix-participants.js` 的 `skill-load` 已标为 `where: 'tail'` / `cacheImpact: 'tail-only'`（原为 `breaks-prefix`）；
- 两条路径都是追加：开跑前由 `server/index.js` 推进 messages（锚点 `【已载入技能: `），运行期由 `server/agent.js` 的 `appendNewSkills()` 推在**工具结果之后**（锚点 `const appendNewSkills = () => {`）；
- 角色仍是 `system`（权威性不变），它前面的历史照样命中；
- 机检：`test/prefix-participants.test.mjs` 核对锚点还在 + 节点 0 只有一个写入点。

代价（如实）：技能在历史之后，模型对它的"权重"略降；这条取舍已被接受。

## M2 决策点（2026-09-16 更正后；**客户必然有互联网**）

> **前提更正（用户 2026-09-16 当面更正）**：旧文档写的"客户形态机 = **无外网** + Windows Server"（见 `RW-Agent架构-v1.1`
> §7.2 与 RA-34、`引擎优化方案-v0.3` M2 行、M1 矩阵 §三）**不成立**：客户永远在网络上使用，必然有互联网。
> 因此"内网网关 / 本地模型"（原 D1）与"离线安装包 / 内网 npm 私服"（原 D2）**两条决策都作废**——
> 那是我照旧文档推出来的、根本不存在的问题。**模型通道照现在这样走公网厂商 API，安装照现在这样装。**

| # | 决策 | 选项与代价 | 我的建议 |
|---|---|---|---|
| **D2′** | **客户机交付（Windows Server）**：装得上、跑得起来、自检过、账本可导出 | 有互联网 ⇒ 依赖照常装，工作量集中在 **Windows 兼容 + 服务化**。**账本导出已具备**（会话导出 + 迁移链 + 全新库路径本轮已真机验过） | **已落地主体（2026-09-16）**，见 C-33：全仓审计把原以为"只有三处硬编码"扩到 **8 条阻断 + 4 条降级，根因两个**（`'/'` 兼作无边界哨兵、命令执行层不过 shell），已全部按结构修掉并用夹具锁死（`test/portability.test.mjs` 8 条）。服务化取 **WinSW**（见 `proposals/D2-Windows-Server服务化调研.md`：只有它自带**为进程内自我重启设计**的 `restart!`，而 `sc stop`/`nssm stop` 会把服务停死），安装/升级/卸载脚本与运行手册在 `scripts/windows/` |
| **D3** | **数据出口唯一化（OP-01）**，而不是"沙箱" | 原 RA-15–19/25/27 里的"沙箱"是给**外部引擎**准备的（数据视窗在出口生效、沙箱内看不到原始数据）。**D5 不接外部引擎 ⇒ 那组多数不成立**（gVisor 也只在 Linux 有意义）。真正仍然成立的那半是：业务数据**只经工具出口**、出口按会话权限过滤、能力清单如实标 `partial/none` | **已落地（2026-09-16，见 RA-59）**：先量后改，把"平台数据进模型上下文"的通道逐条过了一遍——**只发现一条没有边界**（错题召回的候选 SQL 无账号过滤，全平台错题标题会注进任意会话），照 HTTP 面 `GET /api/reviews` 的同一口径修掉（SQL 收进 `lessonrecall.js:recallLessons`，缺账号就不召回），并在能力清单加一等字段 `dataEgress` 如实写出"注入层按账号过滤 / 工具出口按会话权限放行 / `db_query` 全库只读不做行级隔离"。真库验证：跨账号召回不到、自己的召得回、闲聊不注入。**沙箱不在这条里**——它是 v0.3 §7.1 ⑰（P3），C-36 已更正"沙箱要做" |
| **D4** | **对外形态**（RA-41/42；§8.9 三项：网页嵌入 / 会话 API / 上下文注入钩子） | **A 会话 API**：`/api/chat` + 流式 + 会话导出**已都在**，固化契约 + 补幂等键/HMAC/死信（RA-42）。**B MCP server**：能被别的 agent 当工具调；MCP 我们**只有客户端**（`server/mcp.js`），服务端要新写。**C 网页嵌入**：前端工作量大 | **A 与 B 都已落地（2026-09-16）**：A = 契约文档 `docs/会话API契约-v1.md` + 幂等键 + 飞书回调 HMAC + 死信落点（见 RA-42/56/57）；B = `server/mcp-server.js` + `scripts/rw-mcp-server.mjs`（stdio MCP，只暴露 `rw_chat`/`rw_status`/`rw_export` 三个**平台能力**，不搬内部工具），被**官方 MCP Inspector**（第三方实现）与两个独立客户端真调通（见 RA-41）。**C 仍不做**（前端工作量大、且触发条件＝真出现网页嵌入需求） |
| **D5** | **外部引擎（Codex/Claude Code 等）接不接**（RA-32/33 + OP-05/06/07） | **接**：与 DSH 对齐的多引擎形态；代价＝AgentAdapter 契约 + 每家围栏/工具面适配 + 成本基线（M2/M3）。**不接**：省掉整列；代价＝没有多引擎对照数据 | **先不接**（我们的形态是"平台自用 + 客户机"，不是编排别人的引擎）；真要做时先读 DSH 的 Adapter 契约再定 |

**结论**：更正后 M2 只剩三件——**D2′ 客户机交付（Windows 服务化，含那三处硬编码）** → **D4 对外形态（先会话 API）** → **D3 出口唯一化**；
D1 不存在，D5 搁置。**不需要你再回答问题**，按这个顺序开工即可。

### D4 拍板（2026-09-16，依据 `proposals/D4-会话API契约-方案.md` 的调研与证据）

> 拍板原则照旧：**不发明阈值**；能引 DSH 语义就引、没有先例的就不假装有；**不破坏既有调用者**（前端 + `selfcheck`/`agent-smoke`
> 两个脚本是既有事实调用者）；只增不改。

| # | 问题 | 拍板 | 依据 |
|---|---|---|---|
| D4-1 | 幂等键窗口多久 | **不设时间窗口**：键随**会话寿命**存在——同一会话（`account_id` + key）内重复的 `POST /api/chat` 返回**原始接受结果**，不再落一条消息、不再跑一轮 | DSH 的 prompt `requestId` 就是这个语义（"retries whose requestId is already queued or logged **return the original acceptance** without inserting another message"）。本仓没有"外部调用重试间隔"的任何数据，**拍一个天数就是莫须有的值**。行清理策略等出现真实增长再定（与事件归档同一套"不预设参数"的处理） |
| D4-2 | 错误加不加机器可读码 | **(a) 只加 `code` 字段**，保留 `message` 与 `ok`；第一批只覆盖调用方真会分支的：`PARAM_MISSING` / `CONV_NOT_FOUND` / `CONCURRENCY_LIMIT` / `IDEMPOTENT_IN_PROGRESS` / `INTERNAL` | `规范/04-接口规范.md:82-88` 的意图是"出错能定位"；**不改壳**（改成 `error:{...}` 会破坏全部前端与两个脚本，收益是形式合规） |
| D4-3 | 续订锚点 | **(a) 维持现状**：只有带 `seq` 的帧可续订，且**如实写明 seq 是进程内存值、重启归零** | 现在没有任何调用方要求"跨重启续订"；改锚点要动执行链的事件环，属投机 |
| D4-4 | 入站 HMAC 按谁的规范 | **(a) 飞书官方规范** —— **已落地并真机验证**（签名 = `sha256(timestamp+nonce+encrypt_key+原始body)`，验签先于解析、失败 401、不记签名；challenge 官方口径不在签名范围，单独按 token 校验） | 不是猜的：逐条读了飞书开放平台文档；且服务器**本来就配了** `FEISHU_ENCRYPT_KEY`/`FEISHU_VERIFICATION_TOKEN`，真机三种请求（无凭证 401 / 正确签名 200 / 错一位 401）已验。见 RA-56 |
| D4-5 | 死信载体 | **(a) 新增 `deliveries` 表**，不复用 `events` | `events` 值钱的地方正是它的三条口径（唯一写入点 / 只追加 / 投影源，`server/eventlog.js:10-15`）；塞进状态机就把它变成状态表。它也没有"调用方身份/试了几次/怎么重放"三样 |
| D4-6 | 失败几次算死信 | **不设自动阈值**：只记 `state=failed` + `attempts`，由人看 `GET /api/deliveries?state=failed` 决定（重放＝**同一个幂等键**重发 `POST /api/chat`，不另造重放 API） | **我们根本没有投递重试引擎**（出站回调尚不存在）；给一个"几次算死"的数字就是编的。既有的 `llm_max_retries`(=1) 与契约 `attempts` 都不是投递次数口径 |
| D4-7 | 两条导出口径 | **(a)** `/api/conversations/:id/export`（JSONL）**冻结不动**；把带版本、可导入的 `session-export.js` 接到**新**端点 | `session-export.js` 原作者已把"两者并存"写成留给人的决策；删掉它等于扔掉一份现成的版本化实现，替换则破坏 JSONL 既有形状 |
| D4-8 | 锚点什么时候补 | **现在补**（D1–D6 已定） | 顺序反了会返工——这条按调研建议执行 |

**顺带拍一条与 `规范/04` 的偏离（如实登记，不偷偷绕）**：规范 §一 要求版本放路径（`/api/v1/...`），
我们**不加路径版本**——理由：平台有 98 条路由 + 一个已发布的前端在用 `/api/...`，改路径＝破坏既有调用者；
而"对外"目前**只有我们自己的两个脚本**，还没有第三方调用方。版本号落在**流协议**（`run_start`/`run_end`/`stream_hello` 的 `v`）
与契约文档上。**触发条件**：出现第一个第三方调用方时，再按需加 `/api/v1` 门面（"抽象要由第二个真实使用者逼出来"）。
