# B1 前置 Q0 · 参数-消费对照表（2026-09-08）

> 依据：v2.6 方案 §9"前置 Q0 · 参数清点"；核验基准=服务器运行版 b2910c2（=origin/main=本地）。
> 结论：无"文档宣称消费而代码未读"的参数（env 未消费组已如实标注）；真护栏均被代码读取。

## 一、settings 键（settings 表 + settingsSchema.js 默认）→ 消费点
| 键 | 运行时值/默认 | 消费点（代码证据） | 判定 |
|---|---|---|---|
| time_budget_min | 120 | agent.js 护栏读取（getSetting，5s 缓存） | 有消费 |
| round_cap | 2000 | 同上 | 有消费 |
| loop_guard | 6 | 同上 | 有消费 |
| max_parallel_tools | 10（默认） | agent.js 并行上限 | 有消费 |
| task_budget_yuan / task_budget_total | 0(关)/0(不限) | agent.js 成本护栏 | 有消费 |
| max_concurrent_chats | 5（默认，未入库） | server/index.js:402 getSetting('max_concurrent_chats',5) | 有消费 |
| temperature | 0.4 | gateway 请求参数 | 有消费 |
| systemPrompt | 空 | 组消息注入（空=不注入） | 有消费 |
| __policy_rev | 1 | bumpPolicyRev/运行时快照 | 有消费 |
| collapse_* / consecutive_fail_guard / selfchange_budget_yuan | 默认 | agent.js/settingsSchema | 有消费 |

## 二、env（.env / config.js env()）→ 消费点
| 变量 | 值 | 消费点 | 判定 |
|---|---|---|---|
| PORT / DB_* / DB_NAME | 880 / rw_test | db.js/config.js | 有消费 |
| SESSION_SECRET/DAYS | - / 5 | auth.js | 有消费 |
| RW_ADMIN_USER/PASS | - | auth.ensureAdmin | 有消费 |
| DEEPSEEK/GLM/ARK/MOONSHOT/DASHSCOPE/TOKENHUB/QIANFAN/MINIMAX/SILICONFLOW_API_KEY | 已配 | providers keys（启动载入） | 有消费 |
| FEISHU_* / RW_FEISHU_WEBHOOK | on | channels/feishu-webhook | 有消费 |
| AGENT_TIME_BUDGET_MS=0 / AGENT_ABSOLUTE_CAP=600 / AGENT_STALL_WINDOW=6 / AGENT_STALL_MAX_REPEAT=3 / AGENT_STALL_RECOVER=3 / BG_ABSOLUTE_CAP=1500 / BG_STALL_RECOVER=3 / RW_AUTO_RESUME=1 / LLM_MAX_OUTPUT_TOKENS=8192 | 见左 | **全代码库 0 读取点（grep 0 命中）** | **未消费→疑似遗留** |

## 三、处置
- 未消费组（AGENT_*/BG_*/RW_AUTO_RESUME/LLM_MAX_OUTPUT_TOKENS）：**B1 提交评审时二选一**：① 从 .env 删除；② 若确需其语义，在对应模块正式接入并回填对照表。**删除优先**（现状行为不依赖它们）。
- **执行结果（2026-09-08，⑤观测批次后补）**：经用户确认**已删除**——服务器 `.env` 移除全部 9 行（`RW_AUTO_RESUME / AGENT_TIME_BUDGET_MS / AGENT_ABSOLUTE_CAP / AGENT_STALL_WINDOW / AGENT_STALL_MAX_REPEAT / AGENT_STALL_RECOVER / BG_ABSOLUTE_CAP / BG_STALL_RECOVER / LLM_MAX_OUTPUT_TOKENS`）；删除前备份 `/srv/harness-workbench/.env.bak-20260908-q0`；重启后 selfcheck **12/12**（行为零变化）。不涉及代码/git。
- 补充核验：真实 token 上限=代码常量（gateway.js：非工具 8000 / 工具轮 12000）；`LLM_MAX_OUTPUT_TOKENS` 不生效，勿误用。
- 本表结论与 v2.6 附录 C"运行时参数（有消费/未消费）"两行一致。
