# 验收状态（生成物，勿手改）

> 本表由 `node scripts/acceptance-status.mjs --write` 生成 —— **不要手工编辑**。
> 锚点与判据在 `proposals/验收锚点.json`；"做没做"由源码锚点机检得出，人只维护目标与理由。
> 为什么这么做：状态列靠人手抄，代码动了没人回头改，同一件事就会在三份文档里出现三种说法。

- 生成时间：**2026-09-15 20:49:23**　·　代码版本：`17c74e9`
- 机检通过 **50** · 缺锚点 **0** · 需人判 **3** · 转后续里程碑 **7**

| 项 | 要求 | owner | 状态 | 说明 / 取证 |
|---|---|---|---|---|
| RA-01 | 换客户环境只改配置不改代码 | agent | ✅ | 环境事实单一来源；两份配置跑同一份代码<br>`server/env.js` |
| RA-02 | 编排核心脱离 HTTP 单跑 | agent | ✅ | runAgent 不依赖 Express；headless 实测 1 秒返回<br>`server/agent.js` |
| RA-03 | 加删工具不改核心代码、不整体重启 | agent | ✅ | 清单热重载；实测 65↔64 未重启（这条同时关掉 v0.3 §7.1 ④）<br>`server/tools/registry.js` |
| RA-04 | 前缀五纪律生效；C4=0 | agent | ✅ | 只追加机检 + C4 账本；机检口径实测 0 行<br>`server/prefix.js` `server/agent.js` |
| RA-05 | 大结果 → 预览+定位符+省略量，可取回 | agent | ✅ | spill + fetch_spill 取回<br>`server/tools/spill.js` `server/tools/index.js` |
| RA-05b | spill 阈值标定 | agent | ✅ | 原固定 32768 在普通路径几何上不可能触发 → 改为按 cap 派生（cap×2）<br>`server/tools/spill.js` |
| RA-06 | 存盘失败工具仍成功 | agent | ✅ | 落盘 best-effort，失败不改工具成败<br>`server/tools/spill.js` |
| RA-07 | 超限结果 → 预览+省略量+定位符，无额外 LLM 调用 | agent | ✅ | 三路径（stored/reader/degraded）<br>`server/tools/spill.js` |
| RA-08 | 换小窗口模型折叠阈值自动变小（比例制） | agent | ✅ | 有效阈值 = min(绝对阈值, 窗口×占比)；0=关闭比例制只用绝对阈值。**2026-09-15 修（C-14）：这条设置此前三层都没接线**——键不在 `agentLimits()` 的 SELECT 清单里（pick 永远取默认）、取值时又接了『或 15』的兜底把显式 0 变成 15%、`effectiveCollapseChars` 还把 0 兜成 0.15，于是"0=关闭"在界面上写着、实际永远按 15% 收紧（静默说谎）。现在三层都通，并加了两条机检：① `test/settings-wiring.test.mjs` 核对"凡被 pick 的键必须在读取清单里"与"schema 写 0=关闭 的键不得用 `pick(k, d) \|\| 默认` 把 0 吞掉"；② `test/modelwindow.test.mjs` 锁住 0=关、null/脏值=默认 的区分。真机实测：设 42→0.42、设 0→关闭、删行→0.15。<br>`server/modelwindow.js` `server/modelwindow.js` `server/agent.js` `test/modelwindow.test.mjs` `test/settings-wiring.test.mjs` |
| RA-09 | 不用知识的轮次上下文里无知识 | agent | ✅ | 三档按需注入；none 档一个字节都不进<br>`server/kbgate.js` `test/kbgate.test.mjs` |
| RA-10 | 不用技能时只有技能目录 | agent | ✅ | 技能全文只在载入后注入，且走 in-history 追加（不碰节点 0）<br>`server/agent.js` |
| RA-11 | 历史 SKU 备货量走精确查询 | agent | 需人判 | <br>RW 无该业务数据；机制在（db_query 精确查询），场景项需真实业务库才能实测 |
| RA-12 | 子代理工具清单可收窄到单个工具 | agent | ✅ | schema 层与执行层同口径<br>`server/subtools.js` `server/subtools.js` |
| RA-13 | 子代理失败 → 交付物照出并标注未取得 | agent | ✅ | 真模型两种失败形态端到端实测<br>`server/subagent.js` |
| RA-14 | 子代理只烧自己额度；未花完显式回收 | agent | ✅ | 轮首+轮尾双判定；与段阈值取 min<br>`server/agent.js` |
| RA-15..19 | 数据视窗/沙箱相关五项 | 平台 | 转 M2 | 数据出口唯一化未做（OP-01） |
| RA-20..24 | 外部引擎围栏五项 | 平台+agent | 转 M2 | 外部前提实测未做（OP-07） |
| RA-25 | 沙箱内看不到原始数据 | 平台 | 转 M2 | 同 RA-15 |
| RA-26 | 等待确认与执行中是两个状态（四面） | agent | ✅ | 接口/监控/计费/超时四面实测通过<br>`server/agent.js` `server/agent.js` |
| RA-27 | 系统故障后从检查点重建 | agent | 转 M2 | 干净沙箱+重放摘要无沙箱不可测；interrupted 可恢复已在 |
| RA-28 | 预算耗尽不能自动续 | agent | ✅ | 守卫直接返回挂起文案<br>`server/agent.js` |
| RA-29 | 检查点不进记忆召回候选 | agent | ✅ | 检查点是文件，不进 knowledge/kb_search<br>`server/tools/checkpoint.js` |
| RA-30 | 定时任务不套空会话 | agent | ✅ | 专用执行会话驱动<br>`server/scheduler.js` |
| RA-31 | agent 暴露结束原因/用量/能力清单 | agent | ✅ | 四件事齐：结束原因/用量/用了哪些能力/自述不可信<br>`server/capabilities.js` `server/index.js` |
| RA-32 | AgentAdapter 契约成文 + 两家引擎各接一次 | agent | 转 M2/M3 | 契约未写（OP-05） |
| RA-33 | 三家引擎成本基线 | agent | 转 M2/M3 | 当前只有单一引擎真实流量（OP-06） |
| RA-34 | 客户形态机装上、自检过、账本可导出 | 平台 | 转 M2 | **2026-09-16 更正前提**：客户形态机**必然有互联网**（不是"无外网"——旧文档写错，用户当面更正）；因此交付工作是 **Windows Server 适配 + 服务化 + 安装/自检 + 账本导出**，不含离线安装。实测真障碍只有三处硬编码：后台任务日志目录 `/tmp/rw-jobs`、自重启走 `systemctl`、平台目录默认 `/srv/harness-workbench`（可配）。账本导出能力已具备（会话导出 + 迁移链 + 全新库路径均已真机验证）。 |
| RA-35 | 命中率与增量指标 | agent | 需人判 | **判据已于 2026-09-15 更正：不设数值线，改成 4 条 0/1 可机检的不变量 + 只报数的观测量。** 原写的三条数值目标（每轮新增≤1000 / 命中率中位≥98%·P90≥99%）已撤回——短会话上命中率由分母决定（上下文 1.09万 vs DSH 44.3万），算术上不可达；DSH 也没有这三条（它的命中率只在前端显示、计量器明确不支持任何设置）<br>node scripts/cache-fresh.mjs（一条全新对话从 0 开始）· node scripts/toolresult-profile.mjs（每轮新增来源归因）· node scripts/prefix-attribution.mjs（失效归因）· node scripts/metric-parity-compare.mjs（两套口径对照） |
| RA-36 | 升级不丢数据 + 旧账过账清单闭环 + 存储格式带版本与迁移链 | agent | ✅ | 升级演练两次；过账清单已定稿并有取证脚本；**2026-09-15 补齐 G4 的另一半**：结构变更改为带版本号的迁移链（schema_migrations + 序号连续校验 + 只容忍声明过的错误），照 DSH 会话格式迁移链的两条：相邻无缺口、只向前<br>`scripts/migration-rehearsal.mjs` `scripts/audit-ledger.mjs` `server/migrations.js` `test/migrations.test.mjs` |
| RA-37 | 事件契约成文；仅靠事件流可重建全过程 | agent | ✅ | 契约+重建器+真 SSE 三场景；断线续订为只读通道<br>`docs/事件契约.md` `src/eventstream.js` `server/index.js` |
| RA-38 | 系统层+工具层逐字节一致 | agent | ✅ | 同输入同哈希；不同档位必不同哈希<br>`server/prefix.js` `test/prefix-epoch.test.mjs` |
| RA-39 | 同工具同参数第 3/5 次提醒但不阻止 | agent | ✅ | 提醒不阻断；挂起阈值=连续 N 次<br>`server/agent.js` `test/loopguard.test.mjs` |
| RA-40 | 折叠后交付物依据仍在 | agent | ✅ | 原文在 messages（只有 INSERT），可按 id 回查<br>`server/agent.js` |
| RA-41 | 平台作为 MCP server：被别的 agent 当工具调用成功一次 | agent | ✅ | **2026-09-16 落地（D4-B）**。形态选择：MCP over **stdio**（newline-delimited JSON-RPC 2.0），因为 DSH 自己没有 MCP server（它作为 server 的通道是 stdio JSON-RPC / ACP），这一块没有可抄的对象；能抄的是它客户端的**握手与分页纪律**，那部分我们早就在 `server/mcp.js` 里实现过。工具面只有三个**平台能力**（`rw_chat` 跑一轮 / `rw_status` 查状态 / `rw_export` 导出），**不把内部 90+ 个模型工具搬出去**——那些含写盘与执行命令，是我们自己 agent 的工具，不是对外接口。协议面逐条夹具锁住（8 条）：握手回声、工具表、**未知方法 -32601 / 未知工具与缺参 -32602 / 坏 JSON -32700**、**工具执行失败走结果里的 isError（不是 JSON-RPC error）**、通知不回、以及 stdio 的粘包/半行分帧。**真机验证（两个独立客户端 + 一个第三方实现）**：① 平台自己的 MCP 客户端（已在真机连过 GitHub MCP server）连上来跑通 `rw_chat`（真跑一轮：`status=saved`、`content="收到"`、message_id 落定）、`rw_status`（2 条消息）、`rw_export`（`rw-session v1`）；② 手写裸 JSON-RPC 客户端（不共用任何代码）握手/工具表/真实调用/协议错误全通；③ **官方 MCP Inspector（`npx @modelcontextprotocol/inspector@latest --cli`）连上来 `tools/list` 列出三个工具、并按 `tools/call` 真调一次 `rw_status` 拿到了真实会话内容** —— 这条是"被别的实现当工具调"的硬证据。配套修复：为跑通它，撞出并修掉一个**只在 read/write 权限会话上炸**的收尾 bug（见 C-35），selfcheck 因此新增"权限档位"这一轴（13 项）。**仍未做**：没有用真正的第三方 agent 产品（Claude Code / Codex 之类）端到端接过一次；MCP 的 resources/prompts 两类能力没实现（只做 tools）；出站回调（"活干完通知你"）仍未做。<br>`server/mcp-server.js` `server/mcp-server.js` `scripts/rw-mcp-server.mjs` `server/mcp-version.js` `test/mcp-server.test.mjs` `test/mcp-server.test.mjs` |
| RA-42 | 幂等键 / 回调 HMAC / 死信落点 | agent | ✅ | **2026-09-16 三件都落地**（D4 拍板：`proposals/架构文档冲突登记-20260915.md` 的「D4 拍板」一节）。① **幂等键**：`POST /api/chat` 的 `Idempotency-Key` —— 同一（账号+键）的重复请求返回**原始接受结果**、不再落消息不再跑一轮；进行中 409、换请求体 409、上次失败可重发。**不设时间窗口**（键随会话寿命；本仓没有"外部调用重试间隔"的任何数据，拍天数就是莫须有的值），语义照 DSH 的 prompt `requestId`。② **回调 HMAC**：飞书回调入口照官方规范做签名校验（见 RA-56）——那条路由此前是全仓唯一无鉴权的写入路由。③ **死信落点**：新表 `deliveries`（`state ∈ pending\|running\|succeeded\|failed`）+ `GET /api/deliveries?state=failed`；**不设自动"几次算死"的阈值**（我们没有投递重试引擎，编一个数就是编的），重放＝同一个幂等键重发。**真机验证**：服务器上真实 HTTP 走了一遍——首次真跑一轮（消息数 0→2）、同键重发**回放**且消息数不再增长（2→2）、同键换体 409 `IDEMPOTENT_KEY_REUSED`、投递记录可查、`export-full` 返回 `rw-session v1`、旧 `/export` 形状未变；夹具 `test/deliveries.test.mjs` 5 条（含并发同键只放一个进去，靠唯一索引兜底）。**MCP server（RA-41）仍未开工**——那是 D4 的 B 阶段。<br>`server/deliveries.js` `server/index.js` `server/channels/feishu-webhook.js` `test/deliveries.test.mjs` `test/feishu-webhook.test.mjs` |
| OP-03 | 策略引擎失败语义 | agent | ✅ | 注册必填 failure + 超时 + 失败留痕 + 改写审计<br>`server/tools/hooks.js` `test/op03-hooks.test.mjs` |
| OP-08 | 子代理深度实证 | agent | 需人判 | 上限已可配 + 每次派生记录深度；'2 层够不够'仍需真实三层任务样本<br>journalctl -u rw-test \| grep '\[subagent\] depth='（实际深度分布）；settings.subagent_max_depth 可收紧后复测 |
| OP-12 | 经验/复盘层归属 | agent | ✅ | 错题按需进尾巴区；闲聊/不沾边一个字节不注入<br>`server/lessonrecall.js` `test/lessonrecall.test.mjs` |
| OP-16 | 降级语义（enforcement 三值） | agent | ✅ | 三值落成代码名词 + 7 条降级目录；一期如实报 partial<br>`server/capabilities.js` `test/capabilities.test.mjs` |
| OP-17 | 溢出文件保留与清理策略 | agent | ✅ | 按龄 7 天 + 按量 64MB；越界不碰；真删才落账本<br>`server/tools/spill.js` `server/index.js` |
| OP-18 | MCP 工具合流进同一注册表 | agent | ✅ | **2026-09-15 落地（统一装载器）**：外部来源（MCP）的工具不再另立一张表（原 `MCP_EXTRA` + execTool 里按名字模式现造的伪工具），改为注册进**同一个 TOOLS**——同一套条目校验（重名/缺描述/缺 run/缺权限/界限非法一律拒绝整批）、同一个工具面（`toolDefs` 一条路径）、同一个执行口（`execTool` 一次查表）。照 DSH `dsh-mcp-client` 的三条注册纪律：一个失败就回滚整批（保留上一代）、server 列出重名工具直接抛错（不再靠网关静默去重兜底）、`tools/list` 跟随 nextCursor 分页（原来只取第一页＝工具多于一页时**静默截断**）。仍有意保留：MCP 工具不受档位/启用集裁剪（由管理员在壳上按 server 装载）；server 挂掉时工具面**保持不动**（前缀稳定优先），调用时如实报"未连接"。<br>`server/tools/registry.js` `server/tools/registry.js` `server/tools/index.js` `server/mcp.js` `test/mcp-registry.test.mjs` |
| §4.4.1-规则5 | 任何进前缀的组件必须声明缓存影响 | agent | ✅ | 12 个组件逐个声明 where + cacheImpact + 源码锚点；夹具核对锚点还在 + 节点 0 只有一个写入点<br>`server/prefix-participants.js` `test/prefix-participants.test.mjs` |
| RA-43 | 工具级界限：声明在工具定义上 + 协作式截止 + 越界如实改写 | agent | ✅ | 对齐 DSH `dsh-tool-call-timeout-policy`：工具声明 `timeoutMs`，execTool 派生到点即中止的 signal 交给工具自己收口——**不抢跑、不丢弃工具 promise**；工具越界才返回则结果如实改写成 `TOOL_TIMEOUT`（走正常留痕成为 fail 行）。界限不再散落在实现里（`server/tools/` 已无 `AbortSignal.timeout(` 字面量），且**绝不给长任务与等人工的工具编一个数**；65 个工具里有界 8 个，其余如实列在 `scripts/tool-bounds.mjs` 的表里。<br>`server/tools/deadline.js` `server/tools/index.js` `server/tools/index.js` `scripts/tool-bounds.mjs` `test/tool-deadline.test.mjs` |
| RA-44 | LLM 失败分类 + 可取消重试（每轮预算可调，等待时长听服务端） | agent | ✅ | 对齐 DSH `dsh-llm-retry` 的两条：① 只在可恢复失败（限流/网关/网络类）重试，参数/鉴权/模型不存在一类 4xx 不重试，用户已停止绝不重试；② 每次重试**先落事件再做可取消的等待**。退避参数（initialDelayMs/jitterRatio）**有意不引入**（DSH 实际取值在其部署配置里，编一个就是莫须有的值）——等待时长优先用服务端 `Retry-After`，没给就立刻重试；预算来自 settings `llm_max_retries`（默认 1，0=关）。顺带修掉一个真洞：非流式兜底原来不接 signal，"停止"在那段时间无效。<br>`server/llm/gateway.js` `server/llm/gateway.js` `server/llm/gateway.js` `server/agent.js` `server/settingsSchema.js` `test/llm-retry.test.mjs` |
| RA-45 | 数据库连接层：死连接可检测 + 查询可被"停止"中断（不设业务超时） | agent | ✅ | 挂死的真因不是"查询慢"，而是**对端已经没了而我们还以为它在跑**（隧道断/网络抖动时本地 socket 仍可写）。两条结构手段：连接层 `keepAliveInitialDelay` 复用本项目既有"120s 无动静即判异常"口径（不设＝OS 默认 7200s，等于发现不了）+ 把用户"停止"接进查询（中止即销毁连接）。**有意不设业务超时**：慢查询归慢查询日志。实测：12 条并发挂死查询中止后连接池 `all/free/queue` 归零、不泄漏；且中止后 mysql2 可能以"成功"返回半截结果，故一律以 signal 为准抛中止错（不把半截数据当结果）。<br>`server/db.js` `server/db.js` `server/tools/index.js` |
| RA-46 | 统一失败分类：失败带码、一律落账、可统计 | agent | ✅ | **2026-09-15 落地**：失败从"一句自由中文"改成**带码**（码表唯一出处在 `server/failures.js`，用表外的码 `fail()` 直接抛错；夹具交叉核对 server/ 里出现的码都在表里）。DSh 的做法是全线结构化 `error.info.code`，我们照做其中最小可用的一份。同一轮还修掉一个真问题：`execTool` 里 `checkPerm`/`validateArgs`/db_write 只读闸原本是**在 try 之外 throw 或提前 return** —— throw 会让 agent 的 `Promise.all` 拒绝、**整轮中断**且该次调用**一行账都不落**（"工具调用必须落账"这条不变量在拦截路径上直接失效）。现在全函数**单出口**：所有拦截汇成 `{error, code}` → 同一段留痕 → 账本 `tool_calls.error_code` 有码（迁移 0002）。码按真实失败分布定，不拍脑袋：近 14 天 96 次失败里 db_query「Unknown column」22 次、edit_file「old 不匹配」6 次、ENOENT 9 次 → 因此单列 `TOOL_INPUT_REJECTED`（模型改参数即可，平台没坏），与 `TOOL_ERROR`（要工程师看）分开。统计入口：`node scripts/failure-report.mjs [天数]`。<br>`server/failures.js` `server/failures.js` `server/tools/index.js` `server/tools/index.js` `server/migrations.js` `scripts/failure-report.mjs` `test/failures.test.mjs` |
| RA-47 | 事件账本（append-only，可回放）—— 确定性投影的前提 | agent | ✅ | **2026-09-15 落地（下一步"确定性投影"的增量一）**：摸底发现事件流此前只活在内存环里（最多 300 条、重启即忘），库里没有任何事件表 ⇒ "把运行态当成事件流的投影"（对齐 DSH `sessionProjections`）**没有源**。现在：`events` 表（迁移 0003 + 新库 SCHEMA 两处）只追加；**唯一写入点**挂在 `/api/chat` 的 `send`（对外事件契约的唯一出口——run 边界与 agent 侧事件都经过它；第一版挂在 agent.js 的 emitEv 上漏掉了 run 边界，是跑真会话查账本当场发现的）；纯流式增量 think/delta 不入账（最终文本在 messages 可查，§7.3 双投影），该名单是唯一出处且夹具反向锁住"状态事件不得被跳过"；落账失败出声不阻断。读回放：`node scripts/replay-events.mjs <convId>`。**归档器未实现**（保留口径沿用审计账本 90 天，登记为未闭环，不另发明天数）。<br>`server/eventlog.js` `server/eventlog.js` `server/index.js` `server/migrations.js` `scripts/replay-events.mjs` `docs/事件契约.md` `test/eventlog.test.mjs` |
| RA-48 | 凭据单一访问路径（配置只放引用，值不进业务表） | agent | ✅ | **2026-09-16 落地（C-18）**：`settings.mcp_servers[].env` 里的明文 PAT 搬进专用凭据文档（默认平台目录 `.credentials.yaml`，0600，`RW_CREDENTIALS_FILE` 可改），settings 只留 `__CRED__:NAME` 引用。照 DSH `dsh-credentials` 的四条：配置只放引用、单一读写路径、每次现取不缓存、`describe()` 永不含值；不照搬跨进程文件锁/watcher/records（无第二个使用者）。**真机验证**：服务器上迁移成功（文件权限 `-rw-------`）、settings 已改写为引用、**重启后真实 GitHub MCP 仍连上（26 工具）**；日志与返回值里无明文（stderr 泄漏路径实测被替换成 `[REDACTED:<名>]`）。**待人工**：曾明文入库的那把 PAT 建议轮换（机器无法代做）。<br>`server/credentials.js` `server/credentials.js` `server/credentials.js` `server/mcp.js` `scripts/migrate-c18.mjs` `test/credentials.test.mjs` |
| RA-49 | 会话导出/导入 + 格式版本（自描述、可搬家） | agent | ✅ | **2026-09-16 落地**：照 DSH `dsh-session-format` 的**语义四条**（版本字段 / 相邻步 / **不认识的版本显式拒绝** / 当前版本不走迁移），不照搬其物理形态（JSONL + 文件名带版本 + ZIP 会话树）。导入**默认 dry-run**，真写时一个事务整份落地且绝不覆盖源会话；列清单取自真库 information_schema，导出 SELECT 与导入 INSERT 共用一份清单。真机：导出会话 718（12KB）与 184（3.7MB）成功；写路径用 `EXPLAIN` 交真 MySQL 解析（**一行未写**，前后五张表行数完全一致）。**2026-09-16 补（v0.3 §7.1 ⑪「迁移链」那半句）**：此前只有「版本不认识就拒绝」，没有任何地方能写「v0 怎么变成 v1」；现补上注册点 `registerSessionMigration`（注册时强制相邻步 `to===from+1`）+ 启动校验链连续无缺口（缺步即抛，口径同 `migrations.js` 的 `validateChain`）+ 导入时**先按链逐步迁移再导入**（链缺步显式报错，绝不按当前版本硬读）。当前只有 v1 ⇒ **链为空是合法状态**（首版、无迁移步）。夹具 `test/session-format-migration.test.mjs`（11 条，含合成 v0→v1 证明迁移通路与跳步/缺口/多余步负例）。<br>`server/session-export.js` `server/session-export.js` `scripts/session-export.mjs` `test/session-export.test.mjs` `server/session-export.js` `test/session-format-migration.test.mjs` |
| RA-50 | 确定性投影：运行态 = 事件账本的投影（可重放、可对账） | agent | ✅ | **2026-09-16 落地（架构增量二）**：对齐 DSH `session-projection` 的 `register({key,stateVersion,init,apply})` + 纯函数折叠；三个**有真实读数**的投影（runStats / toolFace / outcomes）+ 只读对账（✓/✗/—，—＝无法判定不冒充 ✓）。配套新增事件 `prefix_face`：把每段第一轮**真实发出的** toolsHash/sysHash/nTools 入账本——此前账本只能投影"被调用过的工具"，无法回答"当时那条面是什么"。**口径如实**：toolFace 与 `prefix_tools_hash` 不同源（调用集 ⊆ 可用面），只判包含关系，哈希只作参考。真机：会话 722/718 对账 ✓5/✗0。**下一增量**才是"运行时读投影"（要先有长期一致的证据）。<br>`server/projection.js` `server/projection.js` `scripts/projection-replay.mjs` `test/projection.test.mjs` `server/agent.js` |
| RA-51 | 事件账本保留与归档（只追加 + 按龄搬迁，先搬后删） | agent | ✅ | **2026-09-16 落地（RA-47 登记的未闭环项）**：`events` 只追加；到期行**先搬进 `events_archive` 再删原表**，删的是"刚搬过的那批 id"（不是再按时间条件删一遍——那样在部分插入失败时会删掉没搬走的行）；任一步失败原样抛出，events 一行不动。**90 天沿用 `audit_log` 那条既有口径**（不自己发明天数；DSH 做的是按窗口的语义压缩，按龄清理只有 spill 文件 30 天，所以这个天数只能取本项目自己的审计口径）。接线照 runSpillCleanup（启动一次 + 每 6h + unref）；真库端到端用**自己造的 100 天前行**验证并清理，**绝不动真实事件**；CI 无 MySQL 时该用例按可达性探测**跳过并说明**（本地能跑、CI 判红＝没人看 CI）。<br>`server/eventlog.js` `server/migrations.js` `server/index.js` `test/eventlog-archive.test.mjs` |
| RA-52 | 提示注入防线（外部来源声明 + 不可信注入块来源句 + 策略写入可识别 + 如实能力声明） | agent | ✅ | **2026-09-16 落地**（方案：`proposals/提示注入防线-方案-20260916.md`，含 DSH 对照：它只对 **web** 用 `EXTERNAL_WEB_CONTENT_NOTICE`、消息带 `source{kind}`、技能/AGENTS 以 **user 角色**注入；**没有** taint、没有按来源的工具门禁、"策略只能人改"没有强制点）。做了四件：A1 外部来源（网页/飞书/MCP）结果加**不可信内容声明**（结果头 + 工具描述一句，只加给外部来源——标记多了等于没标记）；A2-a 不可信注入块加来源句 + 知识/技能写入带归属；B-①② 策略类写入必须**可识别 + 单独审计**（不是硬拦——硬拦是安全剧场：挡不住 write_file 路径，且与"模型可自改+reload"主线冲突）；D 能力清单**新增一等字段**如实声明"提示注入未防住"，进用户可见面但**不进模型上下文**。**明确不做**：A2-b 角色降级（缓，先做真机对照实验）、C 按"本轮含外部内容"给高危工具加门禁（可绕 + 误伤 + 无真实样本）→ 转观测项。**最锋利的两条发现**（只报告，未改）：`kb_add` 是 `permission:'read'`（只读会话即可写 `scope=global`、对全会话以 system 注入的长期记忆）；`db_write` 是 `permission:'global'`（checkPerm 对 global 恒真）⇒ write/full 会话可无审批改 settings 任意策略键。<br>`server/tools/index.js` `server/capabilities.js` `test/injection-trust.test.mjs` `test/policy-write.test.mjs` |
| RA-53 | 策略变更的审计不分发起人（模型经工具 / 人经设置页同一条账） | agent | ✅ | **2026-09-16 落地（C-29）**：人工经 `PUT /api/settings` / `/api/access-rules` 改策略此前**一行账都不落** ⇒ "策略什么时候被谁改了"答不出来，C-28 的漂移检测也因此不成立（人工改动会被误报成漂移）。现在 `setSetting()`（唯一收口）对策略键落同一条 `policy:settings-write`，只把 `actor`/`via` 分开（模型＝`model-via-tool`/`db_write`，人＝`human-via-api`/`PUT /api/settings`）。**真机取证**：服务器上登录后 PUT 一个护栏键 → 账本新增 #13168 `actor=human-via-api keys=["progress_stall_n"] from=null to="10"`，与模型路径那行同形状并列。另：`scripts/tools-face-diff.mjs`（工具面反向核对）已接进 CI（A3）——工具描述/参数是进前缀的字节，改一个字就是一次换纪元，以前只靠"记得同步快照"。<br>`server/index.js` `server/index.js` `server/tools/hooks.js` `test/policy-write.test.mjs` |
| RA-54 | 换纪元的代价在决策点可见（模型重启 / 人工部署两条路） | agent | ✅ | **2026-09-16 落地（A0，C-31 实测驱动）**：实测 214 次"整段重建"集中在一条 432 轮的会话上（量级 ≈14M tokens），成因是**在活跃会话上反复部署**——改工具面/系统提示会换纪元，所有活跃会话的前缀整段作废。先问了 DSH：**它不做这件事**（核心包里 `prompt cache / cached prefix` 零命中，没有预热/失效预警/延后变更；它的会话是持久日志 + 投影，重建只花钱不影响正确性）。我们的决定：**只做描述性的一半**——判据用现成的 `agent_runs.status='running'`（**不发明时间窗口**），两处提示位：① `reload_platform` 无论被拦还是放行都带代价说明（既有 F2 防撞不动）；② `scripts/release.mjs` 预检按 status=running 列出活跃会话并给"攒批发布"的建议，读不到库如实跳过、不阻断发布。**真机两条分支都验过**：无活跃会话 → "发布的好时机"；造哨兵 running 会话 → 正确列出（含轮次/最后活动/目标摘要）。<br>`server/tools/index.js` `server/tools/index.js` `scripts/release.mjs` `test/epoch-cost.test.mjs` |
| RA-55 | 同一份代码在 Linux 与 Windows Server 上都跑得起来（D2′ 客户机交付） | agent | ✅ | **2026-09-16 落地主体（D2′，背景见冲突登记 C-33）**：平台原先照"部署在 /srv 下的 Linux 机器"写，换到客户机（Windows Server）时不会报错、只会**悄悄走错分支**。两个真根因已按结构修掉：① 用 `RW_FS_ROOT`（平台所在盘的盘根）替掉兼作"无边界"哨兵的根斜杠字符串，`inside()` 改用 `path.relative`（顺带修掉"根下任何路径都判在外"的既有错）；② 新增 `server/shell.js` 作为"命令串怎么执行"的唯一出处（Linux `bash -c` / Windows `powershell.exe -Command` + UTF-8 前言——**选型是实测的**：`cmd.exe /c` 会吞掉内层引号、返回空输出且 code=0，而 PowerShell 的引号/中文/emoji/退出码都对），`run_command`/`run_test`/`run_long_task`/契约验收钩子/MCP 全部走它。另：环境事实由代码位置推导（平台目录＝`server/` 的上级、工作区＝兄弟目录，**与现行 /srv 部署逐字节相同**）；临时目录用 `os.tmpdir()` 并留 `RW_JOBS_DIR`（Windows 会清 %TEMP%，KB4506040）；`RW_RESTART_CMD` 让 Windows 侧的重启可配（WinSW 的 `restart!` 是唯一为"进程内自我重启"设计的入口，`sc stop`/`nssm stop` 会把服务停死）；安全网（系统区写入/危险命令）补齐 Windows 判据（原先只认 Unix 路径 ⇒ 在客户机是空网）。**已在真机验证的**（本机 Windows + 部署后的 Linux 服务器 + 一次性空库）：① Windows 上**端到端起过服务**（独立库、不碰 rw_test）：真模型对话、真工具写盘/读回、`run_command` 跑 Windows 命令返回**真实输出**（`echo WINE2E-ISHFAG` → `stdout="WINE2E-ISHFAG\r\n" code=0 cwd=E:\`，`tool_calls.result_bytes=74`、`audit_log` 与 `events` 载荷完整）、**selfcheck 12/12 与 agent-smoke 12/12**、4 轮前缀指纹齐全、命中率中位 92.36%；② env/shell/restart 三层：三条推导、shell 的引号/中文/emoji/管道/含空格路径/退出码/命令不存在、restartPlan 四条臂、**"本机没有可用重启方式"时经真实 reload_platform 工具如实报错**；③ 后台任务链路端到端、边界判据五种形态、安全网在 Windows 形态下确实拦得住、write 级白名单不可被连接符绕过。夹具 14 条（`test/portability.test.mjs` 11 + `test/schema-sync.test.mjs` 2 + `test/asyncwrap.test.mjs` 2 里的 Windows 相关部分）。**这一跑还撞出并修掉了一个装机阻断**（见 C-34 / RA-58：全新库建表与迁移链漂移 11 列 ⇒ 新机器装完第一次建会话就挂），以及一个放大器（Express 4 不接 async 拒绝 ⇒ 出错时请求永久挂住而不是 500）。**尚未验证的（如实记）**：① 真正的 **Windows Server**（本次是 Windows 桌面版）与 `scripts/windows/` 的安装/升级/卸载脚本——只做过 PowerShell 5.1 解析器语法校验 + `-WhatIfOnly` 干跑，**没真装服务、没下载 WinSW、没动防火墙**，`restart!` 在 LocalSystem 身份下的自我重启要按手册 §4.2 实测一次；② 除 `selfcheck.mjs`/`agent-smoke.mjs`/`kpi.mjs` 外的运维脚本未在 Windows 上验证（手册已如实列为"未交付能力"）；③ 全新库路径的验证是在 Linux 上做的（Windows 上验过但当时带着 C-34 的缺列，补列后才通过）。<br>`server/env.js` `server/exec/local.js` `server/restart.js` `test/portability.test.mjs` |
| RA-56 | 回调入口来源校验（飞书签名校验 + 未配置时如实告警） | agent | ✅ | **2026-09-16 落地（RA-42 的 HMAC 那一半；另两半"幂等键/死信"待拍板）**。缺口是真的：`POST /api/feishu/webhook` 是全仓**唯一没有 requireAuth 的写入路由**，原实现"先看 challenge、没有就当真事件"⇒ 未配加密策略时任何人 POST 一个 `im.message.receive_v1` 就能让平台真跑一轮 Agent（花模型费 + 让机器人往它指定的 chat_id 发消息）；配了 Encrypt Key 时 challenge 是加密下发的，而原代码在**解密之前**看 `body.challenge`，网址校验永远失败（这条不是安全问题是功能坏了）。修法**照飞书开放平台官方文档**（逐条核对原文，不是自己拍规范）：配 Encrypt Key ⇒ 签名校验 `sha256(X-Lark-Request-Timestamp + X-Lark-Request-Nonce + encrypt_key + 原始请求体)`，官方明确 body 要在反序列化**之前**取 ⇒ `express.json` 的 verify 钩子留一份 rawBody；只配 Verification Token ⇒ 比对事件里的 token；都没配 ⇒ 不阻断但启动时明确告警"本入口不校验来源"。challenge 官方口径**不在签名校验范围**，单独按 token 比对且必须先解密。顺序照 DSH 的 GitHub webhook 适配器（解析 JSON 之前验签、失败 401）。**真机验证**（服务器上，用本机真实密钥算签名、不打印密钥）：未带凭证 → 401「缺少签名头」；正确签名 → 200 `{ok:true}`；签名错一位 → 401「签名校验失败」；selfcheck 12/12。夹具 4 条起真 express + 真 HTTP；**反向核对过**：把签名与 token 两处判定短路 → 对应用例立刻报红。<br>`server/channels/feishu-webhook.js` `server/channels/feishu-webhook.js` `server/channels/feishu-webhook.js` `server/index.js` `test/feishu-webhook.test.mjs` |
| RA-57 | 会话 API 契约固化（对外形态 A：写下来 + 补齐真缺的） | agent | ✅ | **2026-09-16 落地**。契约文档 `docs/会话API契约-v1.md`（199 行，16 条端点，每条事实带文件:行）把**现在真实存在**的对外接口写死：认证、端点表、SSE 帧序（`intent → run_start → … → done → run_end`）、`seq` 是进程内存值故续订只对带 `id:` 的帧成立、断连即中止本轮、幂等/重试、错误码表、变更规则（只增不改）、已知限制、以及两个既有调用脚本的逐条断言（改哪些会让它们报红）。**如实登记的偏离**：不加 `/api/v1` 路径（118 条既有路由 + 已发布前端 + 目前只有我们自己的脚本在调；触发条件＝出现第一个第三方调用方）——版本号落在流协议 `v` 上。补齐的两处真缺：`stream_hello` 带 `v`；鉴权 401/500 与 Express 兜底 500 带 `code`。**顺带修掉一个会让请求永久挂住的真问题**（Windows 端到端实测撞出来）：Express 4 不接 async 处理器的 rejection ⇒ 任一 handler 里 await 抛错时客户端挂到超时而不是 500。修法 `server/asyncwrap.js`（装配后统一包一层，240 个处理器），夹具用真 express + 真 HTTP 断言"抛错必须换来 500，不是超时"。**仍未做**：`stream_gap` 帧（接丢一段没有信号）、登录/停止/续订/消息/活动/审批/问询的 4xx 还没有 `code`（契约 §5 已逐条列出）。<br>`docs/会话API契约-v1.md` `docs/会话API契约-v1.md` `server/index.js` `server/asyncwrap.js` `server/index.js` `test/asyncwrap.test.mjs` |
| RA-58 | 全新库装机路径：两条建库路径一致（否则新机器装完就挂） | agent | ✅ | **2026-09-16 落地（C-34，Windows 端到端实测撞出来的装机阻断）**。实测：全新空库上服务能起、`/api/health` 200，但 `POST /api/conversations` **永久挂住**（客户端 240 秒超时），服务端只有一行 `unhandledRejection: Unknown column 'provider'`。根因两条且与操作系统无关：① `db.js` 的建表语句与 `migrations.js` 的迁移链**漂移了 11 列**；② 全新库走"按最终形状建表 + 整条链标记为已应用"的特例 ⇒ 那些 ALTER 一条都不跑。**任何新机器装机都会撞上。**修法：补齐 11 列（类型逐字抄自 0001_baseline）；新增**静态一致性夹具** `test/schema-sync.test.mjs`（链里加过的每一列/每张表都必须在 db.js 的建表语句里 —— 两条路径从此由机器对齐）；启动自检的列清单**改为从迁移链推导**（原来是手写的一小段，实测漏了 5 列）。**真机验证**：服务器上建一次性空库 `rw_fresh_probe`、起第二个实例、缺列数 0、建会话 28ms 返回（不再挂住）、**selfcheck 12/12（不改库）**、日志里 `Unknown column`/`unhandledRejection` 0 次，验完停实例删库（库残留 0）。<br>`server/db.js` `server/db.js` `test/schema-sync.test.mjs` `server/asyncwrap.js` |
| RA-59 | 数据出口边界：注入层按账号过滤 + 边界如实声明（D3/OP-01） | agent | ✅ | **2026-09-16 落地（D3/OP-01）**。做法是**先量后改**：把"平台数据进模型上下文"的每条通道过一遍——知识库块（`kbVisibleWhere` 按账号+范围过滤）、会话内数据（早期摘要/目标/现场/后台任务通知，都限本会话）、技能/壳/项目 AGENTS.md（平台配置，共享是设计）、运行时快照（本会话自己的用量）都成立；**只有一条没有边界**：错题召回（OP-12）的候选 SQL 写在 index.js 里、`SELECT ... FROM reviews WHERE result='bug' ...` **没有任何账号过滤** ⇒ 全平台所有账号的错题标题会被注进任意会话，而同一份数据在 HTTP 面（`GET /api/reviews`）是按 `account_id=?` 过滤的——两处口径不一致，模型上下文成了绕过账号边界的数据出口。**修法照 HTTP 面那一条口径**（不发明新规则）：SQL 与过滤条件收进 `lessonrecall.js:recallLessons`（缺账号就不召回、且不发查询），index.js 改走它；跨**会话**召回保留（OP-12 原意），跨**账号**召回不行。另加一等字段 `capabilities.dataEgress`，如实写出"注入层按账号过滤 / 工具出口按会话权限放行 / `db_query` 是全库只读、不做行级账号隔离"，边界报 `partial`（没有行级租户隔离——工作台形态的有意选择，但要写出来，不能让人以为已经隔离好了）。**真机验证**（真库）：造一条别的账号的、与问句强重叠的错题 → 管理员账号召回 **0 条**（不泄漏）；同一账号自己问 → 召回到（说明过滤没过头）；闲聊 → 0 条（原口径未退化）；清单字段就位；验完删除哨兵数据。夹具 6 条，**反向核对过**（去掉 account_id 过滤 → 前两条报红）。**沙箱本身随 D5 一起搁置**（不接外部引擎）。<br>`server/lessonrecall.js` `server/lessonrecall.js` `server/index.js` `server/capabilities.js` `test/data-egress.test.mjs` |
| RA-60 | JSON-RPC 形态：通用方法门面（v0.3 §7.1 ⑳，P3） | agent | ✅ | **2026-09-16 落地核心 + stdio 适配器（HTTP 挂载未做）**。v0.3 §0.4 的对外形态是 headless → MCP server → JSON-RPC 三级；我们原先停在第二级（`server/mcp-server.js` 就是 stdio 上的 JSON-RPC 2.0，但**绑死 MCP 的方法名与 `content[].text` 结果形状**，且已有真实第三方消费者不许改）⇒ 本轮补的是第三级的**通用门面**：`server/jsonrpc.js`（`Registry.register(name, handler, {description,contract,params})` + 单一 `dispatch` + `serveStdio`，方法名取 `资源.动作`）+ `scripts/rw-jsonrpc.mjs`（stdio 适配器，复用 MCP 适配器的后端封装：登录/建会话/chat SSE/messages/export-full/activity）。**照 DSH `dsh-sdk-jsonrpc-server` 的"协议层薄、业务在核"**：协议层只做分帧、分派、四个标准错误码（-32700/-32600/-32601/-32602），**一行业务判断都没有**；业务失败走结果里的 `isError`（与 MCP 门面同口径：调用方要能区分"你不会说协议"与"这次没干成"）。**不新造对外能力**：6 个方法逐条映射到 `docs/会话API契约-v1.md` §3 已冻结端点（`system.capabilities`→`GET /api/health`、`session.chat`→`POST /api/conversations`+`POST /api/chat`、`session.stop`→`POST /api/chat/stop`、`session.status`→`GET /api/conversations/:id/messages`、`session.export`→`GET /api/conversations/:id/export-full`、`session.activity`→`GET /api/conversations/:id/activity`）。夹具 11 条（`test/jsonrpc.test.mjs`，含**解析契约文档端点表**核对映射表无孤儿、粘包/半行/UTF-8 跨 chunk 分帧、通知不回、握手方法表从注册表出），`test/mcp-server.test.mjs` 8 条未受影响。**反向核对过**（6 处破坏各对应报红的用例）：错误码写错→③、去掉多余参数校验→④、业务失败写成 JSON-RPC error→②⑥、未知方法也回帧→⑦⑧、用 `toString()` 替掉 `StringDecoder`→⑧、映射表指向不存在的端点→①。**用桩平台（临时脚本，不入库）跑通全部 6 个方法**：握手（含方法表）/ chat（真解 SSE：`status=saved`、`runId`、`messageId`、`finishReason`、`usage`）/ status / export / activity / stop，并验证 `idempotencyKey` **真的进了 `Idempotency-Key` 请求头**（桩平台侧读到 `idem=k-1`）。**顺带撞出并修掉一个真缺陷（在适配器自己的代码里）**：读完 `run_end` 后在 `for await` 循环体内 `ac.abort()` 会让那个 `next()` 当场以 AbortError 拒绝、落进"超时"分支 ⇒ **流明明跑完却报 `status:'timeout'`**（内容靠回读落库消息兜住，所以肉眼不易发现）。改法是读满即 `break`、abort 只留给超时；两条分支都实测：桩平台正常收尾→`saved`，桩平台只发一帧就挂着→`timeout`（waitSeconds=5 时约 5 秒返回）。**同一写法在 `scripts/rw-mcp-server.mjs:102` 也有**（本轮按"只碰必须碰的"未改它，只报告）。**仍未做**：没有 HTTP 挂载（`server/index.js` 本轮被别的代理改，留到后续）；没有对**真平台**跑通一轮会话（需要活着的平台 + 账号，只到桩平台这一层）；审批/问询的裁决方法（`POST /api/approvals/:id`、`POST /api/asks/:id`）未纳入——缺 `GET /api/approvals`、`GET /api/asks` 两个列表方法，只给 id 的裁决口对调用方没用，要加先改契约。<br>`server/jsonrpc.js` `server/jsonrpc.js` `scripts/rw-jsonrpc.mjs` `test/jsonrpc.test.mjs` |

## 怎么用

1. 改完代码后跑一次 `node scripts/acceptance-status.mjs --write`，状态表自动更新（**不用再手工同步三份文档**）。
2. 新增验收项：先在 `验收锚点.json` 里加一条并给它挂源码锚点；挂不上锚点的，说明它还不能算"做完"。
3. `需人判` 的条目**不许**在别处写成"已完成"——机器判不了的事情，就要人出示证据。
