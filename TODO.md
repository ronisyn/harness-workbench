# Roni Workbench · 迭代待办（用户实测反馈）

> 记录时间：2026-09-02（用户 880 实测反馈）｜状态：待修复
> 用户出门办事，回来后按指令修复。

## 🔴 P0 · 4 项明确改进（用户实测反馈）

### 1. UI 整体样式协调性
- 现状：配色 OK（太阳大地色），但按钮/字体/字号/字间距不协调
- 要求：**参照 3080（DSH）整体样式重新设计结构，渲染 UI**
- 参考：DSH 的按钮、字体层级、间距、整体质感

### 2. 对话自然度
- 现状：用户感觉 RW 仍"被设定条条框框"、不够自然
- 要求：像 3080 对话一样自然
- 已做：双路径（普通对话不带 tools → 模型出厂认知）——用户测试自我介绍已自然
- 待做：进一步排查"条条框框"来源（工具描述/提示词残留/前端渲染），对照 3080 对话体验

### 3. Markdown 渲染
- 现状：**对话里显示 ## 等原始符号**（前端 Chat.jsx 未接 Markdown 渲染库）
- 要求：像 3080 一样渲染 Markdown（#、**、表格、代码块等）
- 方案：react-markdown + remark-gfm（+ shiki 高亮/KaTeX 数学，二期）

### 4. 设置页完善
- 现状问题：
  - **厂商只显示了 4 个**（模型市场 4 源），已接入厂商（D1-D9）没展示 → 需要"已接入厂商"列表（providers/models 表）
  - **能力只显示编号**（b_tool_1 等）→ 需要显示真实能力名称（如"读取文件"）
  - **缺"轨迹"能力**（工具调用/审计记录展示 tool_calls/audit_log）
  - **缺"终止对话"**（停止生成按钮，abort 进行中的 SSE）
  - **缺"选择权限"**（会话权限 read/write/full 选择 UI）
- 要求：参照 3080 界面结构 + 能力设计

## 🟡 待验证（用户回来后）

- 微信实测（给机器人发消息）
- 飞书真实文档测试（feishu_doc_read）
- 880 全功能体验反馈

## 备注

- 880 已外网可用：http://47.106.205.196:880（Ronisyn 登录）
- 服务器同步方式：本机 git push + scp 覆盖关键文件（服务器直连 GitHub 网络不通）
