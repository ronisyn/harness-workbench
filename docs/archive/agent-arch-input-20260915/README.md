# agent 架构输入批次（2026-09-15 归档 · 只读参考）

> **来源**：用户与另一个对话整理的 00–50 编号文档集 + `architecture.md`（平台总纲 v0.1），共 52 份。
> **性质**：**输入参考，不是执行文档**（用户 2026-09-15 明确）。本目录不参与 RW 执行体系，不占 `docs/` 主位。
> **处置原则**：去冗余 / 补缺陷 / 调冲突（三条）。**已采用的内容已写入定版架构** `proposals/RW-Agent架构-v1.1.md`；本目录只作溯源、数值出处与待验证清单的知识参考。
> **读法**：要结论看定版文档；要原始论证、数值来源、开放项看本目录。

## 索引（按用途分组）

| 组 | 文档 |
|---|---|
| 会话过程 | `00` 交接 · `21` 阶段总结 · `41` 独立评审诊断 · `42` 独立评审处置 |
| 需求与决策 | `40` 需求真源 · `11` 决策清单 · `43` 接口与字段规范 · `45` 能力与场景 |
| agent 侧 | `07` 引擎/运行时边界 · `08` 上下文 · `09` 子代理 · `10` 对外接口 · `12` 技能插件 · `13` 会话运行 · `15` harness 评估 · `16` harness 服务器化 · `17` roni 现状 · `18` 抽边界 · `19` 平台/agent 边界 · `20` 移植零件 · `22` 编排 · `23` 零件细节 · `24` 按需召回 · `25` 总纲 · `49` 快速汇总 · `50` 完整版 |
| 岗位/数据侧 | `03` 岗位包 · `04` 连接器 · `05` 沙箱与部署 · `32` 技能插件商店 · `33` 知识层 · `34` 数据视窗 · `35` 跨边界授权 · `37` 岗位包编写 · `39` 集成标准 |
| 平台侧 | `01` 身份隔离 · `02` 合规 · `06` 审批与执行器 · `14` 混合部署 · `26` 治理 · `27` 服务器资源 · `28` 交付物 · `29` 经验管理 · `30` 模型网关与计费 · `31` 平台总览 · `36` 数字员工生命周期 · `38` 交付送达 · `44` 装载标准 · `46` 客户数据与 SLA · `48` 产品与服务 · `architecture.md` |

## 一、已采用（内容已进定版架构，此处仅列出处）

| 采用项 | 出处 |
|---|---|
| 引擎可换 + 运行时自研；三条不可让渡边界；四条易混边界 | `07`·`19`·`25` |
| 上下文五层按稳定度排序、三条纪律、注入防御三层 | `08` |
| 会话四寿命、运行状态机（"等待人工确认"独立态）、三种中断、检查点内容、**等人复核时销毁沙箱**、验收钩子 | `13` |
| 工具 manifest + 装载器 + 一次性声明化 | `18`·`23` |
| spill / 修剪 / 压缩的机制与数值 | `23`（已独立复算，见 `50` 补充 补-0） |
| 按需召回三件（技能只放目录/知识按需查/结构化数据走精确查询）+ FTS5 优先、向量留位 | `24` |
| 子代理价值判据、四条硬约束、派活→结论接口、预算四条 | `09`·`22` |
| 循环检测 `[3,5,8]` 分级提醒不熔断 + 预算软着陆三档 | `25`·`08` |
| 沙箱五不变量 S1–S5、网络默认全断、gVisor/microVM 定位 | `05` |
| 围栏 > 接管（含 Codex execpolicy、DS fs-sandbox 证据） | `50` §3 |
| 数据物化进沙箱（物理隔离而非君子协定） | `50` §3.4 |
| 对外接口六形态、先 REST+SSE+Webhook、幂等、回调安全 | `10` |
| 技能/插件/连接器三分 + `denied_by_default` | `12` |
| 平台/agent 归属表、岗位包九块（作为接口引用） | `19`·`03` |
| 开放项：待验证 V1–V10、评审缺陷 P1 清单 | `50` §11·`42` §4.1 |

## 二、未采用（仅存档，附理由）

| 未采用项 | 理由 |
|---|---|
| `49`（快速版）、`architecture.md` §5、`45` §A 的 agent 架构章节 | 与 `50` 重复的第二/三/四份副本 → 定版取代之（去冗余） |
| `11`:L53"一期实际只用 roni 一个" | 与 `architecture`:L742-757 及 `49`/`50` 的"三个并行"矛盾（C1），以三并行为准 |
| `49` §3.3 的"工具调用必须经过平台"（无条件） | 与 `50` §3 围栏结论冲突（C2）；定版改为"统一注册表 + 双通道"（见定版 R-01） |
| `45`:L320-326"外部 agent 自带工具不能绕过平台" | 同上，旧"接管"前提 |
| `architecture.md` 的"循环检测熔断""集成 Claude" | 已被 `25`/`50` 与 ADR-5 推翻（C8） |
| `05` §10"开发工作区规格第一批不做" | 与 `05`:L125 自身修正矛盾，以修正为准 |
| 沙箱"四层隔离"与 `05`:L252"沙箱+网络+权限+检测"两套说法并存 | 定版合并为一套（见定版 R-02） |
| `49` 的验收编号 A1–F5 与 `50` 的同号不同措辞、`50` 另加 G1–G7 | 编号撞车（C7）→ 定版统一为 `RA-*` 前缀 |

## 三、本目录纪律

1. **不再修改**本目录内文档（只读参考）。发现错误 → 改定版文档并在其"裁决记录"留痕。
2. 引用本目录内容必须给"**文件名 + 行号**"（沿用 `42` §3 举证纪律：实读源码/核实文档/我拍，三者必须分开标注）。
3. 本目录**不参与** `docs/README.md` 的权威体系；权威见定版架构与《总方案》《引擎改造计划》。
