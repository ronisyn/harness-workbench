# RW 平台层 · 3080 对照差距清单（目标 goal-b840159d 跟踪文件）

> 目标：补齐全部平台能力差距，按 构建→测试→修bug→复测→自审→继续 循环，直到自审无剩余差距。
> 清单随实测/审计更新。✅=已上线并E2E验证  🔄=开发中  ⬜=未开始

## 已完成（✅ 已部署+E2E 验证）
- ✅ 导出对话（P1-F1：/api/conversations/:id/export，前端顶栏按钮）
- ✅ 快捷键（P1-F2：Ctrl+N 新建 / Ctrl+E 聚焦输入 / Enter 发送）
- ✅ 轨迹文件打开（P1-F3：GET /api/file?path= 前端点路径打开）
- ✅ edit_file 精确编辑工具（P1-F4：old→new 局部替换）
- ✅ 后台任务管理（P1-F5：run_long_task + job_list/job_output/job_kill，/tmp/rw-jobs）
- ✅ 结构化问询（P1-F6：自然对话承接，不单独造工具）
- ✅ 图片理解（P1-F7：view_image → deepseek-v4-flash-vision-exp）
- ✅ 长上下文压缩（P1-F8：>40 条摘要 + 最近 30 条，conv_summaries 懒生成）
- ✅ 任务规划（P2-F9：plan_tasks/plan_done + 前端 PlanCard 实时进度）
- ✅ 目标系统（F10：set/update/get_goal + goals 表 + 每请求注入 active 目标；E2E 通过）
- ✅ 自动路由（P2-F11：model=auto → 视觉走豆包/其他 DeepSeek）
- ✅ 高级参数/温度（F12：settings.temperature + 前端滑杆）
- ✅ 用量与费用统计（F13：usage_stats.cost + estCost + 统计条）
- ✅ 定时任务（F14：scheduler + tasks API + 前端定时 Tab + cronToNext 支持 */n）
- ✅ 技能系统（F15：skills/<名>/SKILL.md 挂载机制；skills_list/skill_load/skill_save；
      conv_skills 跨轮+跨请求注入；E2E：保存→载入→下一条自然消息带出技能指令）
- ✅ Agent 实时轨迹（thinking/agent_thinking/tool_start/tool_done/plan SSE 流式；
      前端 think 折叠区 + 工具卡片，与 3080 一致在对话内直播）
- ✅ 会话权限选择（read/write/full 映射 limitPath，默认 full）
- ✅ 双路径对话（普通=无工具自然认知；工具意图→Agent function calling）

## 待开发（⬜ 未开始）
- ⬜ （无——其余能力项已全部完成，见上）

## 已补齐（最近一轮 ✅ 已上线+E2E 验证）
- ✅ 子代理（F16/F17：subagent sync/async + subagent_output 轮询 + subagent_join 收口；
      子代理独立 runAgent 循环、工具实时直播"子:"前缀、tool_calls 留痕、3 层嵌套上限、8 并发护栏；
      E2E：两个 async 并行→join→各自工具步数与耗时汇报）
- ✅ 批量编排（F18：subagent_fanout {{item}} 模板、每批 6 并发、逐条结果对齐；
      E2E：2 条目并行各自 list_dir 并汇总）
- ✅ 知识/记忆（F19：kb_add/kb_search/kb_del；scope=global/conv；每请求注入条目索引+前5条摘要；
      E2E：写入全局偏好→下一条自然对话自动回忆出"简短直接不要客套"）
- ✅ 审批（F20：会话权限 guard（前端下拉"需审批"）；delete_file/db_write/git_push/run_command 受控；
      执行前 SSE 发 approval 卡→用户批准/拒绝（POST /api/approvals/:id）→继续或被拒；5 分钟超时兜底；
      E2E：guard 会话删除文件→审批卡→批准→删除成功）
- ✅ 服务端停止生成（前端停止按钮现在真正中止服务端 Agent 轮：/api/chat/stop + ctx.__signal 轮头检查 +
      审批等待可中止；停止后不落 assistant/统计消息；E2E：审批挂起时点停→SSE stopped→文件未删→无 assistant 行→可继续新对话）

## Round 3 自审修复（✅ 已上线复测）
- ✅ 思考过程持久化：messages.reasoning 列（含存量库 ALTER 迁移）；普通/Agent 两路径累积 reasoning 落库；
      历史回打开折叠"思考过程"可见；导出含 🧠 引用块（E2E：两路径 reasoning 均落库并由 GET 回显）
- ✅ 根因修复：默认模型 deepseek-chat 流式不返回 reasoning_content → 全局默认改 deepseek-v4-flash
      （providers 默认模型/渠道 wechat/feishu/启动时 DB 修正：停用 deepseek-chat 别名、确保 v4-flash 首位启用）
      （探针：chat 流式 think=0；v4-flash 流式 think=16 → 修复后两路径 think>0）
- ✅ 审批门禁名单对齐真实工具名（git_push→git_pull_push，补 kill_process）
- ✅ 子代理审批事件转发（guard 会话中子代理触发受控工具，approval 卡经父级 SSE 透传，避免超时悬挂）
- ✅ 清理自动测试数据（E2E 会话/技能/知识测试条目）

## Round 4 自审修复（✅ 已上线复测）
- ✅ 图片理解实锤 E2E：生成 96×96 渐变 PNG → view_image → 模型准确描述"红→绿→洋红渐变"（此前仅部署未实测）
- ✅ 长对话压缩真 E2E：种子 92 条消息 → 懒生成摘要链路修复——**根因 findProvider 未导入且错误被 catch 静默吞掉**；
      catch 改为 console.error 留痕；摘要成功落库（81 字，准确概括独角兽话题）+ 日志确认
- ✅ 清理 Round 4 测试数据

## Round 5 自审修复（✅ 已上线复测）
- ✅ 目标系统自然路径注入 E2E：active 目标落库 → 无工具普通对话准确复述目标并遵守"GOALOK 标记"指令
- ✅ 定时任务真实执行 E2E：cron 每分钟任务到点创建专用会话并产出 SCHEDOK 回复（调度器→runAgent 全链路）
- ✅ 自动路由加固：目标厂商（如 ark）未配 Key 时回落 DeepSeek 主力，避免 auto 把对话带到不可用厂商；
      E2E：auto 普通问→deepseek 422；含"图片"纯文本问→ark 视觉模型 222，双分支均无错
- ✅ 前端剩余代码走查（顶栏/左栏/能力/厂商/市场/轨迹/定时页签）未发现缺陷

## Round 6 自审修复（✅ 已上线复测）
- ✅ 系统提示词落地（能力清单"系统提示词"由开关变为可设置）：settings.systemPrompt + 设置抽屉编辑框，
      每轮对话注入【用户自定义指令】；E2E：设置后普通对话回答按要求追加 SYSOK
- ✅ 修复 getSetting JSON 解析 Bug（温度等设置存为 JSON 字符串，读回是 string → 请求体类型错误 +
      UI 重开显示 1.0）；GET /api/settings 同步解析；E2E：读回 temperature=0.3 number

## Round 7 自审修复（✅ 已上线复测）
- ✅ 并发限制落地（能力清单"并发限制"由展示变为真实约束）：每账号最多 3 条对话并行，超出返回
      HTTP 429 友好提示，请求结束/停止即释放槽位；E2E：3 路 sleep 占用 → 第 4 路 429 → 结束后恢复
- ✅ 上传→Agent 文件操作链 E2E：/api/upload → read_file→append_file→edit_file(hello→HELLO diff)→复核，
      落盘内容精确验证（HELLO rw upload test 第1行第二行-追加成功）

## Round 8 自审修复（✅ 已上线复测）
- ✅ 停止中止不再留孤儿 fail 轨迹：execTool 在 ctx.__signal.aborted 时跳过 audit_log/tool_calls 写入，
      避免该行被下一条 assistant 消息回填造成误导（拒绝/超时等正常留痕不受影响）；
      E2E：审批中点停 → SSE stopped、tool_calls=0、audit=0、仅剩用户消息
- ✅ 轨迹抽屉 duration NaN 防护；10 项 API 全量回归 200

## Round 9 自审修复（✅ 已上线复测）
- ✅ 服务端对话导出端点补齐（此前仅前端 Blob 导出）：GET /api/conversations/:id/export →
      Markdown 含标题/思考引用块/工具轨迹；E2E：内容含 🧠 推理与 EXPORT-OK 口令
- ✅ 新增 docs/880实测指南.md（32 步分模块验收步骤 + 3080 对照 + 反馈格式），供用户最终实测

## Round 10 自审修复（✅ 已上线复测）
- ✅ 内存护栏：后台任务与子代理注册表无界增长修复——已完成子代理 2h/300 条、已退出任务 12h/200 条自动修剪
- ✅ 飞书凭证链路实测：tenant_access_token 获取成功（code 0，此前 403 为文档权限而非凭证问题）
- ✅ 模型市场接入链路 E2E：选未接入模型 → /api/market/connect → models 行 enabled=1 → 回滚删除
      （source=market 归属正确）

## Round 12 自审修复（✅ 已上线复测）
- ✅ 审批裁决审计：POST /api/approvals/:id 落 audit_log（account/action=approval:approve|reject/detail=id+结果）；
      E2E：批准删除 → 文件删除成功 + audit 行（approval:approve, ap-1-mtkc1w3f）
- ✅ 多会话隔离不变量 E2E（Round11 记录）：B 会话 1s 应答不阻塞、A 可单独停止、槽位释放

## Round 13 自审修复（✅ 已上线复测）
- ✅ 修复文档解析隐性缺陷：extract.js 依赖 fflate 但 package.json 未声明且服务器未安装 → 真实解析 docx/pptx
      必然 ERR_MODULE_NOT_FOUND；docx 改用已有依赖 mammoth，fflate 补入依赖并服务器安装；
      E2E：fixture docx→HELLO-DOCX-EXT、pptx 两页→HELLO-PPTX-SLIDE1/2 解析成功
- ✅ 全模块导入冒烟（auth/scheduler/market/extract/feishu/wechat/gateway 等 9 模块）通过；
      cronToNext 单测通过（固定值/步进/周一约定/非法输入 null）；scheduler 移除无效 maxRounds 传参

## Round 14 自审修复（✅ 已上线复测）
- ✅ OCR 工具重构：实测发现 tesseract.js 语言包在服务器不可用（错误语言名/CDN 下载失败）→
      弃用 tesseract，改为 DeepSeek 视觉模型识别（与 view_image 同链路，Round4 已验证可靠）；
      E2E：96px 图 → "没有文字。" 正常返回（无崩溃、无 CDN 依赖）
- ✅ 模型市场刷新实测：4 源全部拉取成功（openrouter 423 / dashscope 250 / tokenhub 121 / siliconflow 95）

## Round 15 自审（✅ 已上线复测）
- ✅ RW 自我开发闭环压测：Agent 自主用 edit_file 给自身平台加 GET /api/health（含 grep 定位/语法检查/多轮修改），
      部署后 curl 返回 {ok:true,service:'rw',ts}；改动已同步本地仓库（cf10259）并清理测试痕迹
- ✅ 清理 OCR 测试遗留 traineddata 文件与测试会话

## Round 16 自审（✅ 已上线复测）
- ✅ 平台自检脚本 scripts/selfcheck.mjs（health/登录/9 项基础 API/会话增删/真实 SSE 对话流），
      服务器执行 12/12 全绿——目标循环的"自测"环节从此可一键重复

## Round 20 用户实测反馈修复（✅ 已上线复测）
1. ✅ 护栏运行时化：10分钟/200轮/4次 硬编码 → settings 可调可关（默认 120分钟/2000轮/6次，0=不限）；
   agent 每次运行动态读取（免重启）；set_limits 工具 + 设置→能力→高级参数 输入框；
   E2E：Agent 自主把三项改为 0 并落库（带工具意图词时）
2. ✅ 轨迹呈现：思考区移到工具卡上方（灰色斜体、流式中自动展开）；工具卡紧凑单行
   （● 红点脉冲 → ✓/✕+耗时，参数/结果可展开，文件工具可"打开文件"）；ENV_MAP 增加
   "小步快跑"引导（一次一个小工具调用、逐步反馈循环）
3. ✅ 回复截断修复：根因① chatStream 的 AbortSignal.timeout(60s) 挂在整条连接上——思考+生成长输出必断
   → 改为"首字节 60s + 流空闲 120s"；根因② max_tokens 4000→8000（三处）；finish_reason 透出，
   达 length 上限时回答与落库均追加 "⚠️ 已截断，可继续" 标注（E2E：流式与落库内容一致）
4. ✅ 自我开发闭环：reload_platform 工具——Agent 改码→syntax_check→调用→【本回复结束后 2s】自动
   systemctl restart（detached 不中断当前回复）；日志实证 "[rw] 自我重启请求 → 已触发 systemctl restart"
5. ✅ 引导说明：ENV_MAP 明确"护栏可调可关（set_limits），无需改代码；改自身代码后调用 reload_platform，
   不要手动 restart"

## Round 20b 追加（✅ 已上线复测）
- ✅ 工具意图词扩展：护栏/限制/轮巡/轮次/时间预算/set_limits/reload_platform/技能/知识库/记忆/子代理/定时任务/目标
      等词加入 TOOL_INTENT_RE——用户直接说"把10分钟护栏和4次轮巡护栏去掉、全放开"即走 Agent 路径；
      E2E：口语请求 → set_limits 调用 → 时间预算/轮次归 0 落库（未提及项保持不变）

## Round 21 复测（✅）
- ✅ 长输出链路回归：3000 字级散文请求 → 完整 2793 字自然收尾落库（含主动邀请"需要更长可继续"），
      无 60s 中止、无静默截断、流式与落库一致；自检 12/12
- 注：纯数字枚举类请求（1..8000）上游模型不稳定（偶发空答/早停），属模型行为而非平台截断——
      平台侧已完成 done/标注/落库一致性验证

## Round 22 复测（✅）
- ✅ 超长单次输出实测：5000 字要求 → 完整 8535 字落库（自然收尾、无截断标注、done 一致）——再次证实
      "看不到全文"的 60s/4000token 双根因已除
- ✅ 测试数据清理，会话列表恢复 32 条用户历史；自检 12/12；默认护栏 120分/2000轮/6次 复原

## Round 26 用户实测反馈修复（✅ 已上线复测）
- ✅ 轨迹平铺行（3080 式）：整段收进细边框容器，内部极矮行：✓ 工具名 ×N ｜末次结果一句话预览｜耗时；
      连续相同工具自动合并 ×N；点击行才展开分步参数/结果；失败行浅红；文件工具可打开
- ✅ 空答兜底：工具执行完但模型最终输出为空 → 自动生成执行摘要（步数/各工具×N/最后结果摘要），不再"无声停"
- ✅ 长任务可持续执行外壳（用户与 RW 对话中提出的第 2 点，已落地）：
      agent_runs 现场落盘（goal/status/rounds/last_step/tool_counts/heartbeat）；
      每轮心跳+现场；重启自检把遗留 running→interrupted；下轮注入【上次任务现场】提醒；
      循环检测改造：soft 提示"换策略"→ 仍无效则 paused 挂起（不销毁现场），"继续任务"复用现场恢复且保留原目标；
      E2E：多步→completed 落盘；模拟中断+重启→interrupted(现场已保存)；"继续任务"→completed 且 goal 原样
- ✅ 环境类话题走工具路径：代码库/自审/断点/心跳/挂起/继续任务/恢复任务/现场/shell/环境信息/长任务 加入
      TOOL_INTENT_RE——RW 不再在自然路径里误称"我没有 shell/权限"（真实环境问题会带工具如实回答）
- ✅ find_file 描述更正（子串匹配、无需通配符）

## Parity Batch1（3080 机制平移 · 首批 ✅ 已上线复测）
- ✅ 蓝图：docs/3080机制对照与RW适配方案.md（启动/鉴权层服务器化映射 + 机制族对照 + settings 键位标准）
- ✅ 上下文修剪：工具结果入上下文改为 head60%/tail30%+中段注明（对齐 tool-result-pruner 语义）
- ✅ subagent_list（注册表清单）；subagent_fork（携带会话种子，排除触发指令）
- ✅ **修复递归漏洞**：fork 曾把触发指令自身作为种子 → 子代理照指令连环 fork（实测出现 子:子: 三层爆炸）；
      排除最新用户指令后复测：单层执行、无嵌套、正常完成

## Parity Batch5（ralph 工具 + 稳定性审计修复 ✅ 已上线复测）
- ✅ ralph 工具：多轮全新 Agent 共享工作区记忆文件推进同一目标（STATUS: DONE/BLOCKED 收敛、轮次上限、禁嵌套）；
      实测首轮即产出高质量真实审计（见下）；追加"快速模式"提示限制单轮步数
- ✅ 依 ralph 审计结果修复 4 处真实缺陷：
  ① stopped 早退泄漏 inflight/abortMap → 改为 skipStore 走统一清理路径
  ② SSE 客户端断连不中止 Agent → req/res close → abort（E2E：断连后 run 标 interrupted、无泄漏、可继续对话）
  ③ 无进程级兜底 → unhandledRejection/uncaughtException 记录保活 + Express 错误中间件（路由后）
  ④ 定时任务并发无上限 → 信号量 ≤2

## 成本事件处置（✅ 已上线复测）—— 1 亿+ token/小时 归因与修复
- 归因：会话87 失控长跑 936 工具步/149 轮 + 护栏被放开(0/6000) + **内部每轮 LLM 调用不入账**(仅记每请求最后轮) + 无运行中压缩
- ✅ 止血：护栏恢复 120分/2000轮/6次；会话87 现场 abandoned 封存；遗留 running 清理
- ✅ 全量计量：agent.js 每一轮 LLM 调用写 usage_stats（kind=round），子代理/渠道/定时同源覆盖；
      E2E：4 轮任务 → 4 行 kind=round（tokens/cost 可见）
- ✅ 运行中压缩：单任务 msgs>170 时早期 tool/assistant 内容归档为占位（防上下文平方膨胀）
- ✅ 停止/断连留痕：中断也落"现场已保存"assistant 消息（修"刷新后消息消失"）；E2E 验证
- ⏳ 待用户：DeepSeek 控制台按模型/Key 对账（含 3080 是否同 Key）

## 自审渠道（每轮迭代后对照）
- 3080(DSH) 界面与能力逐个对照（子代理/编排/目标/规划/技能/记忆/看图/结构化问询/job/edit/快捷键/导出/文件打开/自动路由/审批）
- 用户在 880 实测反馈（TODO.md 记录）
- E2E 冒烟：登录→建会话→工具意图→断言行→轨迹流→DB 落库核对
