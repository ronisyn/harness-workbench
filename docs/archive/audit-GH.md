# RW 平台代码审计：域 G（质量内建）+ 域 H（成本与性能）

- 审计对象：`E:\projects\harness-workbench` @ HEAD `dbfa3af`（docs: MCP接入状态…）
- 蓝图：`docs/平台开发全集清单-v1.md`（v2.6）G 域行 86-91 / H 域行 96-99 / 决策 P3·P5·P7·P15（行 198/200/202/210）/ 台账 O-10·O-11·O-17·O-18（行 166/167/173/174）
- 方法：read/grep 取证（file:行）；判定符号 ✅=已闭环 🔶=部分/有差距 🐛=声称已修但证伪 📝=属实但有偏差/风险 ⬜=未做；🔍=本次未逐行核完
- 结论摘要（18 行）：✅×13 · 🔶×5 · 🐛×0（无蓝图声称被证伪）· 📝注记 ×5 · 🔍待核 ×4
- 硬事实：**本地镜像（@ dbfa3af）与 origin 无任何测试**——`package.json:7-12` 无 `test` script；无 `*.test.mjs`/`test/` 目录。**服务器侧补充核验（2026-09-06 rw-drive 只读取证）**：`/srv/harness-workbench` 的 package.json 含 `"test": "node --test test/regression.test.mjs"`，且存在 `test/regression.test.mjs`——即 #246（GLM 自测修复）产物的测试**只在服务器本地，未推送 origin/镜像**（三方同步缺口，同 O-7 模式；commit 7669f87/a7df16b/1ec89d6 本地镜像不可见，需 C5 审查后合入）。"npm test"另出现在蓝图 248 行批6 计划与 `scripts/verify.mjs:5`（注释）。蓝图 248 行"npm test 新增（累加器+事件冒烟）"=批6 ⬜；#246 服务器侧测试属待 C5 合入项。

## 代码对照

| 蓝图行 | 判定 | 代码证据（file:行） | 说明 |
|---|---|---|---|
| G86 测试驱动闭环 🔶 verify/run_test；❓默认先测后码 | 🔶 | `server/tools/index.js:338-339`（syntax_check=`node --check`）；`:340-341`（run_test=`npm test`，`ctx.limitPath&&!inside()` 工作区守卫）；`scripts/verify.mjs:20-29`（syntax/test/selfcheck/kpi 统一入口）；`scripts/selfcheck.mjs:31-81`（API 冒烟 12 项，需活服务器） | 工具与入口在位，但 run_test 是"目标项目有 npm test 才成立"的封装——**平台自身无 test script**（`package.json:7-12`），`verify.mjs test .`/`run_test{dir:.}` 必报 "Missing script: test"；"默认先测后码"仅存于纪律提示（`agent.js:187-191` ENV_DISCIPLINE），无强制。工具体检报告.md:53 称"node --check / npm test 封装"属实。批6 已排期补 npm test（蓝图 248 行） |
| G87 自审/反思 ✅ self-audit 两段+准则5.3 | ✅ | `server/tools/index.js:642-646`（finish_task 必填 summary、可选 selfCheck 参数）；`server/tools/hooks.js:230-241`（after finish_task `finish_selfcheck_note`：summary<10 字/缺 selfCheck→写 result.hookNote 提示）；`server/driver.js:147-168`（验收过→candidate_done 待复测；未过→打回）；`server/agent.js:196`（纪律指向 `docs/RW行为准则-服务器版.md`） | 自审两段的具体条款在行为准则文档（未读全文，🔍）；代码层=参数强制 + after 钩子留痕引导 + 驱动器验收打回。注意钩子为 after 观察型（fail-open，不阻断），"提测前强制自审"靠提示+打回机制承载 |
| G88 lint/类型钩子 🔶 批4 改码后自动 syntax/lint | 🔶 | `server/tools/hooks.js:212-228`（`code_syntax_check`：after write_file/edit_file，`CODE_EXT=\.(js\|mjs\|cjs)$`，`execFileSync('node',['--check',p],timeout:8000)`，结果写 `result.hookNote`） | 已落地=改码后自动 **syntax**（node --check）；缺：①无 lint（无 ESLint/tsc 类钩子）②仅 write_file/edit_file，**append_file 不触发** ③after 型不阻断（语法错仅注记，模型可能看不见/忽略后继续）——Claude Code PreToolUse 可 stop 强拦；④hookNote 进 tool result，模型可见（优点）。蓝图称"syntax/lint"，lint 实际未做 → 🔶 |
| G89 Diff 审查 ✅ 轨迹 diff 视图；❓大改强制预展 | 🔶 | `web/dist/assets/index-*.js`（轨迹项 args `JSON.parse`+`JSON.stringify(,null,1)` 展示、结果含 diff 文本时渲染 `diff 视图` details——O-11 前端修复产物；web 仓库仅 dist 无 src，源码行号 🔍） | diff 视图（事后轨迹）+写类工具已有审批卡/快照；"大改强制预展"（事前）未见任何代码门 → 与蓝图 ❓一致。前端源文件不在本仓（web/ 只有 dist），无法给 src 行号 |
| G90 验收先于编码 ✅ 契约+验收 DSL+模板 | ✅ | `server/tools/index.js:622`（create_contract 工具：goal/acceptance JSON 数组/boundaries/runAt）；`server/driver.js:55,57`（WS9 验收 DSL：cmd:/file-exists:/grep:/node:/kpi:）；`:88-93`（kpi: 取数比较）；`:95-104`（runAcceptance 逐条核验、失败即停）；`:147-168`（finish_task→验收→candidate_done/打回循环，`MAX_IDLE_CONCLUDE` 防空转 177-184） | 闭环最完整的一行：无人值守驱动器+机器验收+打回+复测状态机。KPI DSL 调 `scripts/kpi.mjs --json`（driver.js:82-85） |
| G91 CI 式提交门 ❓平台自改配套（C5/P3） | 🔶 | 平台自改侧：`server/tools/index.js:650-668`（finish_task auto-commit：`ws===platformDir` 判定→平台目录跳过，走 C5 手动）；`scripts/release.mjs:26-64`（P15：工作区干净→语法全检→security-check→vite build→C5 部署说明）；`scripts/security-check.mjs:20-80`（11 项基线） | 有"发布门"（release.mjs 手工程序化）与提案目录 `proposals/`（仓库根存在）但**无 CI/push 钩子/合入门**——release 是本地脚本非自动触发；平台 main 合入靠人+提案审查。`release.mjs:4` 头注释称流程含"③selfcheck 冒烟"，实际代码 26-64 未执行 selfcheck（仅在 73-76 部署步骤里打印建议）→ 📝 注释与实现偏差 |
| P3 ✅ proposals 分支+模板+880 面板（批4） | ✅ | 仓库根存在 `proposals/` 目录；前端 bundle 含"提案已创建"流程（createProposal 调用）；蓝图 v2.5.4 记录批4 已实施+proposals API 实测 | 服务端 `/api/proposals*` 路由未逐行复核 🔍；其余证据充分（目录+UI+批次记录） |
| P5 ✅ auto-commit 业务区（批4） | ✅ | `server/tools/index.js:647-669`：`ctx.root` 非平台目录(`RW_PLATFORM_DIR` 默认 `/srv/harness-workbench`)且有 git→`git add -A`+commit `'auto: '+summary`；平台目录→返回 skipped '平台目录走 C5 手动提交' | 与决策一致（业务自动、平台手动）。风险注记 📝：`git add -A` 整仓全量提交（无文件白名单/分块），脏文件含机密时会被打包进 auto commit；commit 失败不阻断 finish_task（catch→autoCommit.ok=false 仅返回） |
| P7 ✅ default_model 可配；不做自动分类/降档（批3） | ✅ | `server/index.js:79,331`（`getSetting('default_models',null)` 覆盖 defOverrides）；`:336`（temperature）；C4 显式锁由 `security-check.mjs:76` 取证（`wantProvider` && `provider!=='auto'`）；`server/agent.js` 无任何自动降档逻辑 | 决策"不做自动降档"代码侧成立（无 degrade 分支）；贵活贵模型按显式选择，C4 绝对锁在位。路由细节（index.js:300-340）未逐行核 🔍 |
| P15 ✅ 发布流水线+安全基线（批5） | ✅ | `scripts/release.mjs`（1 干净树 28-31 / 2 语法全检 33-46 / 2.5 security-check 49-53 / 3 vite build 57-61 / 4 C5 受控说明 63-64，不自动 reload）；`scripts/security-check.mjs` 11 项（.env 不入 git 23-30 / hooks 安全网 33-38 / GUARDED_TOOLS 7 项 41-47 / plan 退役 46 / 占位符检疫 52 / 硬编码密钥 55-71 / C4 锁 73-77） | 主体到位。📝 注：release.mjs 未把 selfcheck 冒烟纳入自动链（仅部署说明建议），头注释 4 行与实际不符；security-check.mjs:45 断言 GUARDED_TOOLS===7 已由 hooks.js/index.js 满足（O-15 补全 7 项） |
| H96 成本可见 ✅ 三档计费+快照+周报；❓成本面板 | ✅ | `server/llm/gateway.js:8-18`（PRICE 9 厂商×{hit/miss/out}，元/M）；`:19-25` calcCost；`:27-31` cacheOf（deepseek `prompt_cache_hit_tokens` 优先/OpenAI `cached_tokens` 回退）；`server/agent.js:374-382`（每轮 INSERT usage_stats kind=round）；`:239-253`（快照含累计 token/¥/hit%）；`server/index.js:648-667`（/api/usage/stats：SUM tin/tout/dur/cost）；周报=scheduled_tasks「KPI周报」cron 周日 9:00 跑 `scripts/kpi.mjs --days 7`（README.md:36；docs/archive/RW撑竿跳方案-执行史-v1.0.md:29；scheduler.js:71 定时跑 agent 提示） | 计费三档+快照+周报链路全证。**成本面板 ❓→实际 ⬜**（仅 API 与快照文本，无 UI 面板）📝 另一个缺口：前端 `thinking` 事件丢弃成本——`agent.js:363` emit `agent_thinking` 带 `costCum`，但 `index.js:557-558` 映射只发 `{type:'thinking', round}`，costCum 未透传前端 |
| H97 缓存最大化 🔶 注入顺序未测；❓hit 率监控 | 🔶 | `server/agent.js:235-253`（P8：快照**追加到消息末尾**、先删旧快照，注释 236-238 明示"插历史前会击穿 DeepSeek 前缀缓存，miss/hit 价差 27 倍"）；`:380-381`（cumHit/cumMiss 累计）；`server/llm/gateway.js:75`（stream `include_usage:true`）、`:111-117`（usage 帧只取首个）；`scripts/kpi.mjs:27-29`（cacheHitRate 已算） | hit 率：**kpi.mjs 有、`/api/usage/stats` 无**（index.js:653 只 SUM rounds/tin/tout/dur/cost，响应 658-666 无 hit/miss 字段）→ 前端/API 层无 hit 率监控确认缺口。注入顺序"未测"成立：无任何测试（见首行硬事实）。旁路 LLM 调用（自动标题 autotitle.js:15-22 裸 fetch；归档 tools/index.js:885 chatOnce）**不进 usage_stats、不参与 hit 统计**（📝 见 O-10 行） |
| H98 Token 预算/降级 🔶 default_model 可配；自动降档不做 | ✅ | 预算护栏：`server/settingsSchema.js:11-13`（task_budget_yuan 默认 0=关 / task_budget_total 默认 100 / selfchange_budget_yuan 20）；`server/agent.js:136-162` agentLimits（5s 缓存、13 键）；`:344-350`（effBudgetYuan=段阈值×会话 24h 剩余；`__budgetRemain===0`→guard 'budget-total'）；`:355-360`（时间/轮次护栏）；`:384-386`（超段阈值挂起 guard 'budget-yuan'，先停再问）；`server/index.js:533-541,553`（会话 24h 总账 spent SUM→`__budgetRemain` 注入 ctx） | 预算三闸（段阈值/总账/时间/轮次）+先停再问语义齐全，无自动降档=按决策。📝 注：`settingsSchema.js:11` 定义 task_budget_yuan 默认 **0（关）**，而 `agent.js:149,159` 兜底 fallback 为 **20**——仅当 DB 无该行时（db.js:306-315 已 INSERT IGNORE 种子 '0'）会不一致；低危但两处默认值不同源 |
| H99 压缩省费 ✅ 折叠；O-10 已修 | ✅ | `server/agent.js:290-334` maybeCollapseEarly：`:293`（F3 阈值 gap20/keep80/trig30000/inCap18000，settingsSchema.js:15-18 可调）；`:294-299`（间隔/保留/字符三重判断，防无谓 LLM 成本）；`:306-311`（digest chatOnce maxTokens500）；`:313-324`（**O-10：折叠成本入账 kind=collapse、挂 agent_run_id、入 cumCost**）；`:326-331`（折叠为 1 条 system 摘要） | 折叠+入账闭环 ✅。📝 注（P23 待批6）：折叠模型写死回退 `ctx.__provider\|\|'deepseek'`+`ctx.__model\|\|'deepseek-v4-flash'`（agent.js:306,311）——runAgent 级 ctx **从未注入 __provider**（仅 execTool 的 ctx 副本 index.js:539 有）→ **折叠实际恒用 deepseek**，"选 GLM 折叠仍用 deepseek"④ 现状属实，与蓝图 P23（批6 ⬜）一致 |
| O-10 折叠成本未入账→已修（批1） | ✅ | `server/agent.js:313-324`（INSERT usage_stats kind="collapse"：tokens/cache_hit/cache_miss/cost，挂 ctx.__runId） | 修复闭环。**范围注记 📝**：仅"折叠"（agent.js 内）入账；"归档"（conv_summarize→`server/tools/index.js:885` chatOnce）与"自动标题"（`server/autotitle.js:15-22` 裸 fetch）仍不经 usage_stats——若 O-10 原语义含归档，则归档部分未覆盖（或已由工具侧结构化回退豁免，需确认口径 🔍） |
| O-11 轨迹参数 [object Object]→已修（批4） | ✅ | `web/dist/assets/index-*.js`：轨迹项渲染 `typeof e.args==='string' ? JSON.parse(...)` + `JSON.stringify(n,null,1)`，失败 catch 回退 String | 前端 JSON 解析展示已生效（构建产物可证）。本仓 web/ 仅 dist 无 src → 源码行号 🔍 |
| O-17 失败路径不留痕（②）→待修（批6） | ✅(台账如实) | 已核区域 `server/index.js:575-609`（主路径收尾：stopped→skipStore / 正常→分块模拟流式 598-603 / 落 assistant 606-609）；catch 异常区（约 610-644，仅见 640-645 clearActivity/stopSseHeartbeat/res.end）未逐行读 🔍 | 台账"待修(批6)"与代码现状一致：主路径正常收尾才落消息；异常 catch 是否落占位消息需核 610-639 段（蓝图 O-17 自证 index.js:633-636 只发 SSE error+记 run 状态）——本审计未复核该 catch 段，标注 🔍待核 |
| O-18 空正文入库（③）→待修（批6） | ✅(台账如实) | `server/index.js:595`（`answer = result.content || '（无输出）'` 兜底已见）；agent.js:477/486（F6a/自动摘要 兜底）未读 🔍 | 三层兜底部分取证（index.js:595 一层已证）；"无论何种原因正文为空不静默"最终防线属批6 ⬜，现状与台账一致 |

### 代码对照补充发现（横切）
- 📝 **emit 成本丢失**：`agent.js:363` 已算 `costCum` 并随 `agent_thinking` 发出，`index.js:557-558` 只转发 round——前端"每步成本"可见性打折（快照文本里有累计费用，模型侧可见；人侧仅在 stats API）。
- 📝 **H96/H97 账本盲区**：autotitle.js:15-22（标题）、tools/index.js:885（归档摘要）两处旁路 LLM 调用不计量（usage_stats 无行）→ 钱包/cache hit 统计系统性偏低；自动标题连 usage 都不取。
- 📝 **release.mjs:4 vs 26-64**：头注释"③selfcheck 冒烟"与实现不符（selfcheck 只在打印的部署建议里）。
- 📝 **settingsSchema.js:11（def 0=关）vs agent.js:149,159（fallback 20）** 默认值不同源（低危）。

## CLI对照

规则：对 G/H 每行给出 成熟 CLI 做法 → RW 现状 → 优劣势 → 建议 a)采纳调整 / b)不调整+原因 / c)现象差异。参照物=Aider 测试循环、Claude Code hooks/auto-compact/成本、Codex 预算降档、opencode（另见蓝图"对照成熟 CLI 专项"行 226-241 及 P20-P23）。

| 蓝图行 | 成熟 CLI 怎么做 | RW 现状 | 优劣势 | 建议 |
|---|---|---|---|---|
| G86 测试驱动闭环 | Aider：仓库有测试命令时**每轮改完自动跑测试**，测试失败不视为完成（test-driven 循环）；Claude Code 靠工具+用户复测 | run_test/syntax_check 工具 + finish_task→driver 机器验收（DSL 可跑 shell/grep/kpi 断言），但"默认先测后码"无强制；平台自身零测试 | 优：RW 契约验收（DSL 断言+打回+复测）比 Aider 自动跑测试更接近"机器验收"；劣：run_test 是"目标项目有 test script 才成立"的空封装（本项目自己就没 test script）；无"finish_task 前自动跑测试"门 | a) 采纳调整：run_test 缺 test script 时返回明确诊断（现状=裸 "Missing script: test"）；finish_task 钩子若工作区有 package.json 则提示/自动跑 npm test 再放行；批6 按蓝图 248 行补 npm test（累加器+事件冒烟）使平台自举 |
| G87 自审/反思 | Claude Code/Codex：无平台强制"自审"机制，靠系统提示（be honest）+用户 review；Anthropic 官方建议 self-review prompt 模式 | finish_task 必填 selfCheck 参数（语义强制）+ after 钩子校验长度提示 + driver 打回 = 平台级强制自审 | 优：提测质量门槛内建于工具契约（CLI 无）；劣：钩子仅注记不阻断；"两段自审+准则5.3"细则在文档，模型靠提示自律 | b) 不调整（RW 信任契约特色，蓝图对照 #9/10 已论证 CLI 无此机制，保留打回即 RW 差异化）；微调 a)：finish_selfcheck_note 从"注记"升级为"缺 selfCheck 时 result 带 warning 供打回逻辑引用"可留待批6 |
| G88 lint/类型钩子 | Claude Code PostToolUse hooks 可挂 `npx eslint --fix` / `tsc --noEmit`，且 PreToolUse 可 stop 强拦 | code_syntax_check（hooks.js:212-228）after write/edit_file 自动 `node --check`，写 result.hookNote | 优：零配置即时语法反馈；劣：syntax≪lint/type（抓不到未用变量/类型错）；无 append_file；after 不阻断→改完语法错仍可继续提测（有 hookNote 提示但依赖模型自觉） | a) 采纳调整：①挂 append_file；②加 ESLint/tsc 钩子（platform 代码自用，安全钩子语义按 fail-open 观察，不改 fail-closed——CLI 也非阻断式）；③syntax 失败在 finish_task 处二次拦截提示（改码后未过 syntax 的 finish_task 打回一次） |
| G89 Diff 审查 | Claude Code 应用前逐 edit 展示 diff 等确认；IDE 类工具写前 diff 预览 | 轨迹事后 diff 视图 ✅（写前审批卡只在高危/guard 档） | 优：轨迹可回查+undo_checkpoint 可回滚；劣：写前无 diff 预览（全权限会话静默写入），与"大改强制预展"同为缺口 | a) 采纳调整：diff 预览属 D 域审批 UX（蓝图 ❓ diff 预览）同源——大改动（如 edit/write 超阈值行数）强制预展可并入 D 域立项，非 G 域独有；现状 c) 现象差异：CLI 终端逐 edit 问，RW 审批卡+轨迹 diff 事后看 |
| G90 验收先于编码 | Aider/Claude 无机器验收概念（Aider 只跑测试；Claude Code 无 acceptance DSL） | create_contract 立项→无人值守 driver→验收 DSL（cmd/file-exists/grep/node/kpi）→过=候选完成待复测/不过=打回 | 优：RW 独有且远强于 CLI——"完成"由机器断言而非模型自报；劣：DSL 断言表达能力有限（无测试框架级断言），依赖用户写对 acceptance | b) 不调整（核心差异化，信任契约；蓝图 G90 ✅）。a) 可选微调：DSL 增 `test:<dir>` 行=复用 run_test 语义进验收链，补 G86 与 G90 之间的缝 |
| G91 CI 式提交门 | Aider 改即 commit（auto-commit）；Claude Code 无 CI 门；真实 CI 在 git 托管侧（push/PR 检查） | P5 业务区 auto-commit（平台代码豁免）；P3 proposals 提案+人工审查；P15 release.mjs 发布门（干净树/语法/安全基线/build）；无 push/pre-commit 钩子 | 优：平台 main 受控（C5）+业务自由提交，分层清晰；劣：平台自改无自动化门（release.mjs 要人记得跑）；proposals 审查靠人工 | a) 采纳调整（轻量）：加 git pre-push/pre-commit 钩子（或 reload_platform 前强制跑 `verify.mjs syntax`——现状仅纪律提示"syntax_check 确认后再 reload"）；CI 完整版可延后。c) 现象差异：Aider 每改必提交，RW 只在 finish_task 时 auto-commit（粒度=任务级非步级） |
| H96 成本可见 | Claude Code 界面逐消息显示 $cost（含 cache read/write）；Codex 每轮 token/费用打印；Aider `--analytics`/费用近似 | 三档计费（gateway PRICE）+快照累计（agent.js:249）+kind=round/collapse 账本+/api/usage/stats+KPI 周报 | 优：hit/miss/out 三档拆分比多数 CLI 细（接近真实账单，deepseek 按真实账单加权校准 gateway.js:6-7）；劣：无 UI 成本面板；`thinking` 事件丢弃 costCum（index.js:557-558）；autotitle/conv_summarize 旁路不计量→数字偏低 | a) 采纳调整：①`/api/usage/stats` 响应补 cache hit/miss 与累计（前端统计条直接可用）；②index.js 把 costCum 透传进 thinking 事件（1 行改动）③旁路调用入账（autotitle/归档）——三点均低成本高可见性。c) 现象差异：CLI 终端成本在行尾，RW 成本在快照文本+stats API |
| H97 缓存最大化 | Anthropic prompt caching 自动前缀命中（cache_control breakpoint），UI 显示 cache 读写量；DeepSeek/OpenAI 自动磁盘缓存 | P8 快照移消息尾部（agent.js:235-253，命中 deepseek 前缀缓存设计）+cacheOf 拆分+usage_stats 存 hit/miss+kpi 算 hit 率 | 优：前缀稳定设计正确且已落地（快照 append 而非插头部）；劣：注入顺序无测试验证（零测试）；hit 率只进 kpi 报表不进 API/UI；旁路调用（标题）无 cache 口径 | a) 采纳调整：①/usage/stats 暴露 hit 率（前端可显示"缓存命中 xx%"）；②批6 B1 include_usage 厂商矩阵（蓝图已排期）；③命中率回归口径进 kpi 周报对比基线。c) 现象差异：CLI 只显示本次 cache 费，RW 用快照每轮告知模型 hit% |
| H98 Token 预算/降级 | Codex 有预算档位自动降小模型（budget 超限换便宜模型继续）；Claude Code 无硬预算闸（仅 max-turns 类护栏） | 决策=**不做自动降档**（P7）：default_model 可配、显式绝对锁 C4；护栏=段阈值/会话 24h 总账/时间/轮次，先停再问 | 优：不冒充模型=诚实（C4），guard 先停再问保留用户裁决权，比 Codex 静默降档更透明；劣：超预算只能停不能降级续跑（大任务可能要用户手动换模型继续） | b) 不调整（自动降档与 C4 显式模型=绝对锁直接冲突——降档=隐性换模型，正是 O-14 同类问题；要省钱让用户显式选 flash 档）。c) 现象差异：Codex 预算内自动换档续跑，RW 停在护栏等你调模型/预算 |
| H99 压缩省费 | Claude Code auto-compact（约 95% 上下文时自动压缩，保留摘要+继续）；Aider/opencode 同样自动压缩 | 折叠（agent.js:290-334）：间隔≥20 轮/保留 80 条/触发 3 万字符，4 键可调；折叠成本已入账（O-10 ✅） | 优：触发保守（3 万字符才折，防无谓 LLM 成本）+阈值可调+入账透明；劣：折叠固定用 deepseek（P23 未实现，与"会话选 GLM"不一致）；摘要 maxTokens=500 且失败静默降级为纯归档占位（可接受） | a) 采纳调整：批6 P23 折叠跟随会话模型（agent.js:306-311 改传会话 provider/model——代码已预留 ctx.__provider 语义，仅未注入）；压缩阈值默认值可对照 auto-compact 经验再校一次。c) 现象差异：CLI 按上下文比例自动触发，RW 按字符/轮数硬阈值触发 |
| O-17 失败不留痕 | CLI 无平台层兜底：失败直接红色报错到终端，用户必然看见 | 待批6：异常 catch（index.js 约 633-644）只发 SSE error+记 run 状态，不落 assistant 消息 | 优（RW 现状劣）：Web SSE 失败会"无声无息"（O-17 铁证 conv246 静默 31min）；CLI 终端无此问题 | a) 采纳调整=批6 ②（异常必落占位消息+已做进度）——RW 特有适配，CLI 无此机制故照做无参照，属必要兜底（蓝图对照 #10 ⚠️ 部分不照做=保留并强化） |
| O-18 空正文入库 | CLI 无空正文问题（模型空答直接无输出，用户可见空行） | 三层兜底（agent.js:477 F6a / 486 自动摘要 / index.js:595 '（无输出）'）+批6 最终防线 | 优：多数路径已兜；缺口=历史脏数据清库+最终防线未做 | a) 采纳调整=批6 ③（最终防线+历史清库）。b) 说明：CLI 终端不存库故无对等机制，现象差异=CLI 空答无痕、RW 需入诚实占位 |

### 判定计数
- 代码对照 18 行：**✅×13（G87 G90 P3 P5 P7 P15 H96 H98 H99 O-10 O-11 O-17* O-18*）· 🔶×5（G86 G88 G89 G91 H97）· 🐛×0 · ⬜×0**（*O-17/O-18 判 ✅=台账"待修批6"与代码现状如实一致，非缺陷已修）
- 📝 注记 5 处：release.mjs 注释与实现偏差 · settingsSchema def(0) vs agentLimits fallback(20) · 归档/自动标题不入账 · thinking 事件丢 costCum · auto-commit `git add -A` 粒度风险
- 🔍待核 4 处：行为准则 5.3 细则（docs）· web 前端源码行号（仅 dist）· index.js catch 段 610-639（O-17 精确行）· agent.js:477/486 兜底行（O-18）+ /api/proposals 路由
- 最重要发现 Top5：见回复正文

> 审计范围说明：本次为"中断式快审"——G/H 域主体、决策 P3/P5/P7/P15、台账 O-10/O-11 已完成逐行取证；O-17/O-18 及部分路由/文档行标注 🔍待核。未改任何代码。
