# 权限与沙箱（服务器版）· harness 三层映射（5.6 交付件，隶属平台设计蓝图 v2.5 D6 域）

> 目标：把 harness(3080) 的"文件策略分层 + 单次升级审批"语义映射到 RW 服务器环境，
> 针对服务器特点调整（路径体系 /srv、MySQL、systemd、多用户渠道），不一概照搬本机模型。
> 隶属：设计决策以《平台开发全集清单-v1.md》（蓝图）为准；本文件存权限机制细节。
> v1.1（2026-09-05）：蓝图 P2/P6 已决策（门禁迁 hooks 统一层+allow/deny 规则层，批2）——见第 4 节核对表更新。

## 1. 层级映射（harness → RW 服务器版）
| harness | RW 会话级 | 服务器版语义（与 harness 的差异已标注） |
|---|---|---|
| read-only | permission=read | 只读：read/list/find/grep/db_query/web 等。**差异（更严）**：本地路径限定 /srv/rw-workspace（limitPath），防服务器上读 /etc、密钥、他人目录；harness 本机版无此全局风险面 |
| workspace-write | permission=write | 工作区读写：write/edit/append/mkdir/copy/run_test/skill_save/kb 等；路径限定工作区；**差异**：写平台代码/DB/系统一律拒绝（服务器上工作区≠平台目录，天然隔离，无需 harness 的 cwd 技巧） |
| danger-full-access | permission=full | 全服务器：平台代码、MySQL、systemd、任意路径。等同 harness danger 级 |
| ask/never（审批策略） | permission=guard | guard=full 级能力 + **受控工具审批门禁**（GUARDED_TOOLS（代码实测 5 项）：delete_file/db_write/git_pull_push/run_command/kill_process；契约档位另要求 reload_platform/set_limits 先问——现状未入代码审批集，差异见 O-15/批2 P6 规则层；plan_mode/exit_plan_mode 已按 C3/P4 退役（批1：请求级只读意图替代会话模式，见 execTool __readonlyIntent）） |

## 2. 单次升级审批通道（= harness "升级该次操作"）
- guard 会话中调用受控工具 → createApproval 弹确认卡（前端红卡：批准/拒绝/超时）；
- **批准仅对该次调用生效**（会话权限不提升）——与 harness 单次升级语义一致；
- 无人值守（driver/定时）：审批自动排队 need_input，任务挂起等用户裁决；
- 用户点"停止"= 取消挂起审批并中止。

## 3. 服务器特有边界（不能照搬本机模型的点）
1. **路径体系**：工作区=/srv/rw-workspace；平台=/srv/harness-workbench；密钥在 .env/root/.rw-keys.env——read/write 会话的 limitPath 必须覆盖这两类敏感根（已实现：限工作区）。
2. **数据库**：db_query=global（SELECT 可查全部库，含平台库）→ 只读会话也保留（审计需要）。db_write 代码 permission=global（checkPerm 恒放行）→ 现状防线=启用集门禁（db_write 不在 TOOL_META/默认 25 项 → GUI 无勾选项，read/write/full/guard 会话均不可达；仅 driver/无人值守等无启用集限制的上下文可达）+ GUARDED_TOOLS（若被启用则 guard 弹卡）；原文档"仅 full/guard"为目标语义，待批2 P6 规则层收紧（见 O-15）。
3. **服务重启**：只读/写会话不可 reload_platform（MUTATING 已有）；full/guard 走 reload_platform 协议（回复后重启），禁止手动 systemctl（中断自身）。
4. **进程面**：run_command/kill_process 仅 full/guard 且入 GUARDED_TOOLS（guard 审批）；run_long_task 现状 permission=full、未入审批集（差异见 O-15/批2）；后台 job 属平台管理对象（非用户会话私有）。
5. **多渠道**：wechat/feishu 会话默认 read（权限最小化），需要提升走 guard 审批卡（渠道内确认）。
6. **工具启用集（5.3c）独立于权限层**：permission=能不能执行该能力；启用集=要不要提供该工具；两层叠加（full 但未启用工具→指引去勾选；启用但权限不足→权限拒绝）。

## 4. 现状核对表（代码依据）
| 机制 | 位置 | 状态 |
|---|---|---|
| 会话 permission read/write/full/guard | conversations.permission + execTool checkPerm | ✅ |
| 工作区边界 limitPath（read/write 限 root=WS） | execTool（inside() 校验） | ✅ |
| guard 受控工具审批（含无人值守排队） | GUARDED_TOOLS（现状 5 项，见 L14）+ approval.js + driver needInput | ✅（5 项；reload_platform/set_limits 契约要求先问未入集→O-15/批2） |
| 只读意图门禁（P4 批1） | execTool __readonlyIntent + MUTATING_TOOLS；请求级只读规划替代会话 mode=plan | ✅（批1 已实施；plan_mode/exit_plan_mode 退役） |
| 工具启用集双层门禁 | toolDefs/enabled + execTool __enabledTools | ✅（5.3c 本批） |
| allow/deny 规则层（P6） | 蓝图批2：模式匹配规则+UI+执行层命中免拦/免审批 | 🔶 已决策待实施 |
| 门禁→hooks 统一层（P2） | 蓝图批2：读型/启用集/意图 gate 迁 before hooks | 🔶 已决策待实施 |
| 升级通道文档化 | 本文件 | ✅ v1 |

## 5. 待办/风险
- read 级对 db_query 全库可读的收窄选项（未来可按需加"只读会话仅限 rw_* 库"）；当前保持（账号单一）。
- db_write 现状 permission=global（checkPerm 恒放行），原文档声明"仅 full/guard"为目标语义；实际防线=启用集门禁 + GUARDED_TOOLS 审批（guard 会话）+ 纪律承诺。reload_platform/set_limits 未入代码 GUARDED_TOOLS，与契约档位"确认或先问"的差距登记 O-15（批2 P2/P6 统一层处理）。
- 渠道（wechat/feishu）权限默认 read 已在代码（conv.permission||'read'）。
